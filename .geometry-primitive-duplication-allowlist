# Known Rust bodies matching the point-to-segment-distance structural
# fingerprint (degenerate-length branch + [0,1] clamp + Euclidean close).
# Each entry needs a REASON. A copy with its own pinned Python oracle,
# genuinely mirroring a different pre-migration reference, is a
# documented-KEEP; an unconsidered copy is the bug this gate exists to
# catch. See docs/evidence/2026-08-13-point-to-segment-distance-epsilon-
# consolidation.md for the epsilon table and the merits-based decision.
#
# <path>::<function>\t<reason>

packages/temper-geometry/src/creepage_check.rs::point_to_segment_distance	canonical kernel (issue #987 dedupe target); degenerate iff denom == 0.0 or !denom.is_finite() -- the numerically-correct contract per the 2026-08-13 evidence doc; HV creepage safety path; fixed_copper.rs now delegates here
packages/temper-geometry/src/drc_constraints_geometry.rs::point_to_segment_distance	own pinned oracle (_constraints_geometry_py_oracle.py, commit c5875adad); seg_len_sq < 1e-10; documented-KEEP -- measured ~20-24% catastrophic divergence from canonical on near-degenerate segments (real decision-boundary difference, not 1-ulp rounding), 20+ file blast radius with explicit epsilon-boundary-pinning tests; needs its own dedicated re-pin plan before merging (precedent: issue #987/#918's own execution)
packages/temper-geometry/src/drc_constraints_geometry.rs::segment_to_rotated_rect_distance	false positive: aggregate that calls segment_to_segment_distance (itself calling this file's own point_to_segment_distance) over 4 rect edges plus a corner-distance helper; matches the fingerprint via `py_min` + `d < 1e-9` + `_distance(` substrings, not an independent reimplementation
packages/temper-geometry/src/geometry_kernels.rs::point_to_segment_distance	own pinned oracle (inline `_oracle_point_to_segment_distance` in test_geometry_rust_differential.py); len2 < 1e-12; documented-KEEP, same reasoning as drc_constraints_geometry.rs above
packages/temper-constraint-compiler/src/constraints/mod.rs::point_to_segment_distance	own pinned oracle (_compiler_py_oracle.py); degenerate ab_len_sq == 0.0 matches canonical's decision boundary exactly (0 catastrophic divergence measured across all corpora), differs from canonical only in final rounding (pow+sqrt vs hypot, ~1-ulp class, decision-immune per issue #987's own methodology); consolidation deferred -- needs a new cross-crate temper-geometry dependency (wasm-target crate) and the oracle's _distance/_point_to_segment_distance helpers are shared by several other oracle computations in the same file, widening the re-verification surface beyond this change's time-box
packages/temper-drc-rs/src/router_clearance.rs::point_to_segment_dist	NEWLY DISCOVERED by this gate's structural scan (2026-08-13), not in the original 5-copy inventory the epsilon-consolidation task named; own hybrid threshold `len2 < 1e-12 || !len2.is_finite()` (combines geometry_kernels.rs's threshold with creepage_check.rs's non-finite guard); production code (router clearance checks), not yet triaged against a pinned oracle or the epsilon decision -- flagged as a follow-up in the evidence doc, not fixed in this change
packages/temper-drc-rs/src/rules/drc/property_campaigns.rs::point_seg_closest	NEWLY DISCOVERED by this gate's structural scan (2026-08-13); self-contained property-test-campaign helper (module docstring: mirrors kernels for wasm-registry proptest coverage since `proptest` is a dev-dependency), only called from within this same file's own campaigns, not a production caller per grep; own threshold len2 < 1e-30; lower priority than router_clearance.rs above since it is test-support code -- flagged as a follow-up, not fixed in this change
packages/temper-drc-rs/src/dfm.rs::calculate_angle	false positive: three-point angle calculation (law-of-cosines style, mag1/mag2 == 0.0 degenerate + sqrt close), not a point-to-segment distance; matches the fingerprint coincidentally
packages/temper-quality-oracle/src/cluster_f/slop_linter.rs::angle_between	false positive: angle between two vectors (hypot magnitudes + acos-domain clamp), not a point-to-segment distance; matches the fingerprint coincidentally
