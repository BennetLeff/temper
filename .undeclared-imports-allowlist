# Undeclared-import gate allowlist (scripts/check_undeclared_imports.py).
# See docs/evidence/2026-07-27-undeclared-import-gate.md.
#
# Format: <module_name>::<repo-relative file glob>  # justification
# (required -- unjustified entries, or entries with no file-glob scope,
# are a hard error; see load_allowlist() in the gate script). The file
# glob is required so an entry can only exempt the specific file(s) it
# names, never every future use of that module anywhere in the scanned
# trees.
#
# Entries here are known, real, pre-existing gaps pending an owner
# decision -- not permission to keep importing an undeclared module
# forever. Remove an entry once its module is properly declared (or the
# importing code is fixed/retired).

# `jax` is imported top-level by these two scripts but is declared in no
# pyproject.toml anywhere in the workspace and is not imported anywhere in
# packages/temper-placer/src (i.e. not a transitive dependency of
# production code) -- both scripts would ModuleNotFoundError on any
# `uv run`. Discovered by this gate itself on 2026-07-27, pre-existing,
# same failure shape as the jinja2/sympy incidents that motivated this
# gate but out of this task's scope to fix (needs an owner decision:
# declare jax as a dependency, or retire the script). See
# docs/evidence/2026-07-27-undeclared-import-gate.md. TODO: temper-xxx
jax::scripts/check_perf_regression.py  # TODO: temper-xxx -- undeclared jax, pre-existing (make perf-regression is continue-on-error)
jax::scripts/internal_route.py  # TODO: temper-xxx -- undeclared jax, pre-existing (make route)
