# Unmerged worktree triage — 2026-07-27

Reference branch: `origin/docs/methodology-loop-discipline` @ `95b2dfc6` (2026-07-27 16:42:44 -0600).
Method: read diffs and commit bodies directly (not just subjects), `git merge-tree --write-tree`
against the reference tip for conflict surface (no branch/worktree state touched), `git log`/`git
show`/`diff` against reference to check for supersession, and cheap `python3 -m py_compile` syntax
checks where applicable. **No git state was modified; no worktree was touched.** Rust builds were
not attempted anywhere (explicitly out of scope as too expensive) — flagged below wherever that
leaves a gap.

## Summary table

| Cluster | What it is | Stale by | Conflict risk | Superseded? | Recommendation |
|---|---|---|---|---|---|
| `py-bridge-build` | Shared `temper-py-bridge` PyO3 crate + migrate geometry/quality-oracle/drc-rs/constraint-compiler/router bridges to it; completes NetClassRules codegen; benchmarks + arch docs | merge-base `36c6b8ac` (2026-07-22), reference **453 commits** ahead | 8 conflicts: 2 add/add on new crate files (expected/cheap), 2 in placer core stages (`clearance_grid.py`, `phased_component_assignment.py` — cheap content, expensive location: file was since split into submodules), rest in Rust bridge files already touched by the landed version | **~90% YES** — landed as PR #284 (`788bf108`, on reference). Crate, geometry migration, quality-oracle migration, codegen completion, benchmarks, arch docs, uv-workspace exclude all present on reference verbatim. **drc-rs / constraint-compiler / rust-router extractor migration described in the PR body did not actually land** (verified: those 3 crates' `Cargo.toml` never gained a `temper-py-bridge` dependency; `board_py_bridge.rs` still has local `extract_*` helpers) | **SUPERSEDED-DELETE** — but the drc-rs/constraint-compiler/router migration slice is real unlanded work; redo it fresh in a new small PR rather than reviving this branch |
| `pyo3-bridge-framework` | Earlier, independent prototype of the same shared-bridge-crate idea; migrates drc-rs/rust-router/constraint-compiler to typed `PyDict` extraction; removes committed `target/` (203 files) | merge-base `b5c2f5f9` (2026-07-22), reference **503 commits** ahead | 9 conflicts, all real Rust source (not generated): `pyo3_bridge.rs`, `board.rs`, `board_py_bridge.rs`, `lib.rs`, `loop_extractor_rs.py`, `loop_extractor/bridge.rs`, plus add/add on the new crate | **YES, fully** — different patch-ids from `py-bridge-build` (independently written), but same idea, started/finished earlier the same day, and functionally superseded once `py-bridge-build`'s superset was written and then partly landed via PR #284. Its `target/`-untrack commit (203 files) is also subsumed: reference already untracked **all 472** committed Rust build artifacts repo-wide (`efd78c0d`/`6f5a71f2`, PR #357) | **SUPERSEDED-DELETE** — no salvageable unique content |
| `cross-language-domain-codegen` | NetClassRules manifest YAML as SSOT; Jinja2 codegen (`gen_domain_models.py`) generating matching Python + Rust structs; CI regen-check | merge-base `b5c2f5f9`, reference **503 commits** ahead | 8 conflicts, several **add/add on identically-named files** (`netclass_rules_manifest.yaml`, `gen_domain_models.py`, `manifest.yaml`, both `.j2` templates) | **YES, fully and directly** — reference already has this exact system, under the exact same filenames, landed as its own U1/U3/U5/U6 unit sequence (`d02eba85`, `db9e5929`, `2abcd3aa`, `904f59f4`) | **SUPERSEDED-DELETE** — pure duplication, zero unique value |
| `pydantic-config-migration` | Migrate `temper-placer`'s `_constraint_types/*.py` dataclasses to Pydantic `BaseModel` (U1 leaf types, U2 sub-modules) | merge-base `b5c2f5f9`, reference **503 commits** ahead | 9 conflicts — every one of the 8 `_constraint_types/*.py` files plus the test file | **YES, fully** — reference's `_constraint_types/config.py` (and all siblings) is already Pydantic `BaseModel`-based, landed via `e76aaf64` "refactor(U1-U4): migrate 34 @dataclass constraint types to Pydantic BaseModel" — same U-numbering, same files, broader scope (34 types vs this branch's smaller set) | **SUPERSEDED-DELETE** |
| `coverage-gate-phase2` | Widen coverage gate from `temper_placer.core` to all of `temper_placer` (Phase 2): CI `--cov` flags, allowlist population via `--init --append`, docs | merge-base `b5c2f5f9`, reference **503 commits** ahead | 5 conflicts: `.coverage-allowlist`, workflow yaml, `AGENTS.md`, `check_coverage_gate.py` (content); `pyproject.toml` files auto-merge clean | **YES, fully** — reference already has Phase 2 landed (`1911c977` "feat(U4): add coverage gate script, allowlist, and CI step", `6b869752` "...populate Phase 2 allowlist... (U4)"); current allowlist header states "Phase 2 baseline: 1927 entries", ~62% paydown already tracked | **SUPERSEDED-DELETE** |
| `agent-ad829d18dbc460ec2` | Two bundled sub-features: (a) fail-closed CI gate requiring every `docs/evidence/` artifact to declare its measurement commit+dirty-state (4 commits, 46 files, matches allowlist/shrink pattern of the existing physics-provenance gate); (b) worktree-hygiene report + `assert-base.sh` base-commit assertion (3 commits) | merge-base `41151552` (2026-07-26), reference **164 commits** ahead | Only **2 conflicts total** across all 7 commits: workflow yaml (wire-in point moved) and `scripts/manifest.yaml` (registration list drifted). Everything else — all evidence JSON files, all simulation harnesses — auto-merges clean | **SPLIT**: sub-cluster (b) is **byte-identical** to what's already on reference (`scripts/worktree_report.py`, `scripts/assert-base.sh` diff = 0 lines) — fully superseded. Sub-cluster (a), the evidence-provenance gate itself, does **not** exist anywhere on reference — verified genuinely novel | **REBASE-THEN-PR** for the evidence-provenance slice (commits `adae0a06`..`fc96e4ff`) — cheap conflicts, real unlanded work, matches an established repo pattern. **SUPERSEDED-DELETE** the trailing 3 hygiene commits (already landed identically) |

## Per-cluster detail

### py-bridge-build
Worktree: `/Users/bennet/Desktop/temper/.worktrees/fix/py-bridge-build` (detached HEAD `eb7ecd27`; one staged file, `packages/temper-geometry/src/constraints.rs`, verified byte-identical to reference — pure leftover, not new content). This branch creates a shared `temper-py-bridge` crate (`catch_unwind`/`panic_to_err` helpers, a `DictExtract` trait, `FromPyDict`/`ToPyDict` derive macros) and migrates `temper-geometry`, `temper-quality-oracle`, `temper-drc-rs`, `temper-constraint-compiler`, and `temper-rust-router` onto it, along the way fixing a real bug (`pins=[]` silently dropped due to `#[serde(default)]`), completing the NetClassRules cross-language codegen SSOT (generated markers in `board.rs`, 8→17 fields), adding benchmarks and `docs/architecture.md`, and excluding the new pure-Rust crates from the `uv` workspace glob. The reason this is worth flagging rather than a clean delete: the squash-merge commit `788bf108` (PR #284, already on reference) has a commit body that reproduces every one of this branch's commit messages verbatim, plus rebase-reconciliation notes referencing "PR #324" — this branch (or a rebased twin of it) **is** PR #284. But diffing reference confirms the drc-rs/constraint-compiler/rust-router migration steps described in that body never actually landed (those 3 crates still have no `temper-py-bridge` dependency and still carry local `extract_*` helpers) — apparently dropped during conflict reconciliation. That slice is real, un-shipped work; it should be redone as a small fresh PR rather than resurrecting this branch, given the placer-core conflicts elsewhere are non-trivial to re-thread.

### pyo3-bridge-framework
Worktree: `/Users/bennet/Desktop/temper/.worktrees/feat/pyo3-bridge-framework` (`ebb2d7a2`, clean working tree). Same underlying idea as `py-bridge-build` — a shared PyO3 bridge crate with derive macros — but an earlier, independently-written prototype (confirmed via non-matching `git patch-id` on every commit) that also migrates the drc-rs/router/constraint-compiler bridges and separately removes 203 committed `target/` build artifacts. It diverged from the same ancestor (`4f28adf3`) as `py-bridge-build` and finished several hours before `py-bridge-build` started, meaning it was superseded in-house before `py-bridge-build`'s superset was ever written, which in turn mostly landed via PR #284. Its build-artifact cleanup is now moot too — reference's `efd78c0d`/`6f5a71f2` untracked all 472 committed Rust build files repo-wide, a superset of this branch's 203. No unique content survives.

### cross-language-domain-codegen
Worktree: `/Users/bennet/Desktop/temper/.worktrees/feat/cross-language-domain-codegen` (`edfd9c4c`, clean). Builds a NetClassRules manifest YAML as single source of truth and a Jinja2 codegen pipeline that emits matching Python and Rust structs, plus a CI check that the generated artifacts are up to date. This is the cleanest supersession case in the set: reference has the identical system, under the identical filenames (`netclass_rules_manifest.yaml`, `gen_domain_models.py`, `scripts/manifest.yaml`, both `.j2` templates), landed as its own U1/U3/U5/U6 sequence. The `merge-tree` conflicts are literally add/add on same-path files because both sides invented the same design independently. Pure duplication — delete without regret.

### pydantic-config-migration
Worktree: `/Users/bennet/Desktop/temper/.worktrees/feat/pydantic-config-migration` (`0a1c5dc9`; one untracked new test file, `packages/temper-placer/tests/constraint_types/test_placement_constraints.py`, not part of any commit). Migrates the 8 `_constraint_types/*.py` dataclasses in `temper-placer` to Pydantic `BaseModel`. Reference already did this — and more — via `e76aaf64`, which migrated 34 `@dataclass` constraint types (using the same U-numbering convention this branch used for its own 2 commits). Every file this branch touches now conflicts because both sides rewrote the same classes independently. Superseded; the untracked test file's premise (testing dataclass-era behavior) should be checked against reference's already-migrated version before assuming it still has value, but the underlying refactor itself is done.

### coverage-gate-phase2
Worktree: `/Users/bennet/Desktop/temper/.worktrees/feat/coverage-gate-phase2` (`fb4dcd4d`; two untracked files, `.coverage` and `coverage.json` — local coverage-run artifacts, not source, and not meaningful to preserve). Widens the coverage gate from `temper_placer.core` to all of `temper_placer` (CI `--cov` flags, allowlist populated via `--init --append`, docs). Reference already shipped Phase 2 under the same U4 tag (`1911c977`, `6b869752`); the live `.coverage-allowlist` header even documents "Phase 2 baseline: 1927 entries" with ~62% paydown tracked since. Fully superseded.

### agent-ad829d18dbc460ec2 (evidence-provenance gate)
Worktree: `/Users/bennet/Desktop/temper/.claude/worktrees/agent-ad829d18dbc460ec2` (`22dd1f4a`, clean working tree). **This is the one cluster whose loss would discard real, unlanded work — treat it differently from the other five.** The unmerged range vs reference is actually 7 commits (not the 4 named in the task brief), splitting cleanly into two pieces:
- **Evidence-provenance gate** (`adae0a06`, `8cf16ece`, `80ea8a28`, `fc96e4ff`; 46 files, 764 insertions vs its true base `41151552`, roughly matching the "4 commits / 36 files" description): a fail-closed CI gate (`scripts/check_evidence_provenance.py` + `scripts/_lib/provenance.py` + `.evidence-provenance-allowlist`) requiring every `docs/evidence/` artifact to declare the commit and dirty-state it was measured at, following the same monotonic-shrink-allowlist pattern as the repo's existing `check_physics_provenance.py` gate. Motivated by a concrete incident cited in the commit message (four stale-checkout measurements reported as current state). Verified genuinely absent from reference — no file of that name exists anywhere in the tree. The script runs standalone (`--help` works, `py_compile` clean). One gap: unlike the physics-provenance gate it mirrors, it ships with no dedicated unit test.
- **Worktree hygiene / assert-base** (`6aca4fb8`, `c4e02578`, `22dd1f4a`): `scripts/worktree_report.py` and `scripts/assert-base.sh`. Confirmed **byte-identical** to what's already committed on reference (0-line diff both files) — this slice is pure duplicate scaffolding that landed through some other channel.

The `merge-tree` conflict surface for the whole 7-commit range is only 2 files (a workflow-yaml wire-in point and `scripts/manifest.yaml` registration), both mechanical, both outside any core placer/PCB path. Recommendation: rebase and PR just the evidence-provenance commits; drop the trailing 3 hygiene commits as already-landed duplicates.

## What could not be verified
- **No Rust build or test run was attempted** for any cluster (`py-bridge-build`, `pyo3-bridge-framework`, and the Rust-touching parts of `cross-language-domain-codegen` all modify `.rs`/`Cargo.toml` files) — explicitly out of scope as too expensive. Supersession conclusions above rest on source/file comparison against reference, not on a green build.
- Python-side changes (pydantic migration, coverage gate, evidence-provenance gate, codegen script) were only checked with `python3 -m py_compile` (syntax-level) — no pytest run was performed anywhere.
- The evidence-provenance gate's own correctness (does it actually fail-closed the way the docstring claims, across the real `docs/evidence/` tree on reference) was not exercised end-to-end — only confirmed it parses, argparses, and is absent from reference.
- Did not investigate the other ~20 worktrees outside the 6 named clusters (e.g. `wt-bundle`, `wt-cores`, various `agent-a*` and `refactor/merge-*-core` worktrees) — out of scope per the task.
