---
provenance: commit=373688e337f8df75b1d3dfa05a7f9ec2fcb6b00d dirty=true

title: CT07/T2 U8 canonical verdict and sensing handoff
date: 2026-09-01
status: stopped-indeterminate
construction: ct07-t2-u6-candidate
---

# CT07/T2 U8 canonical verdict and sensing handoff

U8 is implemented as a producer-side boundary around the ISO U8 shared
receipt contract. It binds the CT07 construction, internal decision,
transform-aware construction projection, allowed-transform policy, and the
full SHA-256 of the ISO-owned `isolation_joint/contract.json`. The Rust
evaluator owns schema validation and lifecycle classification; the Python
runner only reads bytes, computes identities, and performs sealed publication.

The current package is `stopped-indeterminate`. U7-A identity/source
eligibility, U5 representative electrical captures, U6 construction release,
U7-B owner closure, U9 environmental evidence, A5 manual verification, and an
A7 preliminary ruling are pending. OCP-02 remains DNF. No favorable
`joint_handoff.json` is published, no aggregate timing is computed, and no
`eligible-for-refloorplan` or production authorization can be inferred.

The producer contract has synthetic coverage for stopped, rejected, favorable,
missing-ruling, unresolved-limitation, digest mismatch, signer-role, exact
integer timing, and aggregate-forbidden cases. Favorable output requires a
reusable construction ruling, immutable signature and A5 verification
digests, all seven exact `ct07.*` roles, the projection and transform-policy
digests, and the exact CT07 threshold-to-latch timing term. The handoff shape
contains no joint total, joint verdict, microsecond alias, or production
authorization.

Reproduce the current stop with:

```text
python3 scripts/check_ct07_u8_handoff.py --output power_pcb_dataset/qualification/ct07_t2/authority/preliminary_decision.json
```

The command requires the repository-supported fresh pyo3 extension before
replay. Real A7 signing, real CT07 receipt publication, joint-candidate
consumption, and the combined 5000 ns decision remain downstream work owned
by the ISO U9 campaign.
