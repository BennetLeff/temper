# Phase 1 U1 — R19 Verdict Baseline

**Date:** 2026-08-07  
**Commit:** `14979d6330c463f78e04597f7872e979aca06cee` (`origin/main` at time of measurement)  
**Tooling commit:** Same SHA (U0 PRs #879/#880 already merged at this HEAD)  

## Protocol

Per the Phase 1 plan §U1:

1. Record commit SHA.
2. Run `cargo test --no-default-features -p temper-drc-rs` natively. Capture per-test pass/fail.
3. Build wasm32 module via `cargo build --release --target wasm32-unknown-unknown --no-default-features -p temper-wasm-test-runner`.
4. Run `node tools/wasm/run_wasm_tests.mjs <wasm> --json` to capture wasm32 per-test results.
5. Join lists on test name. Classify: agree-pass, agree-fail, expected-fail, disagree, native-only, wasm32-only.
6. Compute agreement rate.

## Native Results

```bash
cargo test --no-default-features --manifest-path packages/temper-drc-rs/Cargo.toml
```

- **95 tests, 95 passed, 0 failed.**
- All 95 tests pass under `--no-default-features` on `aarch64-apple-darwin`.
- The 61 python-gated tests are structurally absent (disabled by `--no-default-features`) and correctly excluded from comparison scope.

## WASM32 Results

```bash
cargo build --release --target wasm32-unknown-unknown --no-default-features -p temper-wasm-test-runner
node tools/wasm/run_wasm_tests.mjs target-shared/wasm32-unknown-unknown/release/temper_wasm_test_runner.wasm --json /tmp/wasm_test_results.json
```

| Metric | Value |
|--------|-------|
| Module size | 1,183,875 bytes |
| Imports | 0 (deployable to bare isolate) |
| Registered tests | 95 |
| Distinct names | 95 |
| Executed | 95 |
| Passed | 91 |
| Failed | 0 |
| Expected-fail | 4 |
| Unexpected-pass | 0 |
| Cold compile | 2.983 ms |
| Cold instantiate | 0.366 ms |
| Cold start | 3.348 ms |
| Total test time | 45.867 ms |
| Mean per-test | 0.4828 ms |
| Median per-test | 0.0194 ms |
| P95 per-test | 1.5333 ms |
| Max per-test | 23.9424 ms |
| Peak linear memory | 1.75 MiB |
| Reinstantiations | 4 |
| Mean reinstantiate | 0.111 ms |
| Instance callable after trap | true |

## R19 Verdict Matrix

Generated by `tools/wasm/r19_compare.py`.

| Classification | Count |
|----------------|-------|
| agree-pass | 91 |
| agree-fail | 0 |
| expected-fail | 4 |
| disagree | 0 |
| unexpected-pass | 0 |
| native-only | 0 |
| wasm32-only | 0 |
| **Agreement rate** | **1.000000** |

## Expected-Fail Tests (4)

All four expected-fail tests are in the manifest (`tools/wasm/wasm_expected_failures.json`):

| Test | Class | Native | WASM32 |
|------|-------|--------|--------|
| `pymath::tests::host_libm_symbols_actually_resolve` | `no-dynamic-loader` | pass | expected-fail |
| `pymath::tests::pow_is_not_a_multiply_or_a_sqrt` | `b7-pow-divergence-absent` | pass | expected-fail |
| `dfm::tests::thermal_via_side_round_absorbs_the_pow_vs_sqrt_divergence` | `b7-pow-divergence-absent` | pass | expected-fail |
| `dfm::tests::via_annular_area_uses_r_times_r_not_pow` | `b7-pow-divergence-absent` | pass | expected-fail |

All four pass natively (the properties they assert hold on native libm) and fail on wasm32 (libm properties unavailable). Zero unexpected-passes — the manifest is not stale.

## Disagreements

None. All 95 tests are accounted for and 100% agree.

## Reproducibility

The exact commands that produce this baseline:

```bash
# 1. Native
cargo test --no-default-features --manifest-path packages/temper-drc-rs/Cargo.toml 2>&1 | grep "^test " > /tmp/native.txt

# 2. WASM32 build
cargo build --release --target wasm32-unknown-unknown --no-default-features -p temper-wasm-test-runner

# 3. WASM32 run
node tools/wasm/run_wasm_tests.mjs \
  target-shared/wasm32-unknown-unknown/release/temper_wasm_test_runner.wasm \
  --json /tmp/wasm.json

# 4. Compare
python3 tools/wasm/r19_compare.py \
  --native-file /tmp/native.txt \
  --wasm-json /tmp/wasm.json \
  --expected-failures tools/wasm/wasm_expected_failures.json \
  --commit $(git rev-parse HEAD) \
  --output docs/evidence/data/r19_baseline_14979d63.json
```

## Verdict

**U1 COMPLETE.** The R19 verdict baseline is established for commit `14979d633`. Agreement rate: 1.000000 (91 agree-pass + 0 agree-fail + 4 expected-fail = 95, denominator = 95, 0 disagreements). This serves as the reference point for all subsequent R19 sustained-agreement measurements per U6.
