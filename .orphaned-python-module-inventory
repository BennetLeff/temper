# Production Python modules with zero PRODUCTION importers anywhere
# (packages/*/src, scripts/, tools/, excluding tests -- test-only
# references do not count, matching check_unwired_kernels.py's own
# production_references() convention).
#
# Shrink-only. A new entry is a hard failure -- wire the module, delete
# it, or add it here WITH A REASON. An entry that becomes referenced is
# also a failure, so paid-down debt shows up in a diff instead of
# rotting on the books.
#
# Generated by scripts/check_orphaned_python_modules.py --write-inventory
# <dotted.module.path>\t<reason>

temper_placer.placer.cp_sat.creepage_repair_frontier	Experimental campaign API retained for its focused test/evidence harness; not wired into production placement.
temper_placer.placer.cp_sat.creepage_replay_resume	Test-only replay/resume contract retained to validate deterministic experiment recovery; not a production stage.
temper_placer.placer.cp_sat.displacement_deletion_scheduler	Experimental campaign scheduler retained for focused search-policy tests; not wired into production placement.
temper_placer.placer.cp_sat.progressive_creepage_cuts	Experimental comparison implementation retained for differential tests while grouped cuts are evaluated; not wired into production placement.
