"""Routing Benchmarks.

Run the benchmark suite with:

.. code-block:: bash

    cd packages/temper-placer
    uv run python benchmarks/routing_benchmark.py

Results are saved to ``benchmark_results.json``.
"""
