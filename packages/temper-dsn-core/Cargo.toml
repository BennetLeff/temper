[package]
name = "temper-dsn-core"
version = "0.1.0"
edition = "2024"
description = "Pure-Rust DSN (Specctra) format utilities — extracted from temper-dsn"
license = "MIT"

[lib]
crate-type = ["rlib"]

[dependencies]
regex = "1"
sha2 = "0.10"
serde_json = "1"

[profile.release]
panic = "unwind"
