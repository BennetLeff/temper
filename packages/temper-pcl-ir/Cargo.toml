[package]
name = "temper-pcl-ir"
version = "0.1.0"
edition = "2024"
description = "Shared typed PCL intermediate representation"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
