I will start by listing the files in `temper-placer/src/temper_placer/optimizer/` to understand the current implementation of the optimizer and scheduler.
I will read `temper-placer/src/temper_placer/optimizer/train.py` around line 337 to see how the optimizer is currently initialized.
I will search for the definition of `create_optimizer` in `temper-placer/src/temper_placer/optimizer/train.py`.
I will read the `create_optimizer` function in `temper-placer/src/temper_placer/optimizer/train.py`.
I will read the `train` function in `temper-placer/src/temper_placer/optimizer/train.py` to see how the learning rate is handled during training.
I will read `temper-placer/src/temper_placer/optimizer/scheduler.py` to understand how the learning rate is currently calculated.
I will read `temper-placer/src/temper_placer/optimizer/config.py` to see the `LearningRateSchedule` definition.
I will read the `train_multiphase` function in `temper-placer/src/temper_placer/optimizer/train.py`.
I will check `temper-placer/uv.lock` to see the version of `optax`.
I will read `temper-placer/uv.lock` around line 535.
I will read the imports in `temper-placer/src/temper_placer/optimizer/train.py`.
I will read the `TrainingState` definition in `temper-placer/src/temper_placer/optimizer/train.py`.
I will search for the definition of `TrainingState` in `temper-placer/src/temper_placer/optimizer/train.py`.
