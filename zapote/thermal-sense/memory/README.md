# Memory use and retained lessons

`attempt-001` preserves Rust selection from `voltage-sense-lessons-v1`: portable model-qualification, land-pattern and independent-identity procedures. The exact RTD result was excluded because this attempt has no matching RTD source identities. The circuit-audit agent reported reading and using the three portable notes.

Construction used native Codex Luna subagents with explicit read instructions. The preparation receipt establishes selection; it is not a provider transport receipt. The host conversation contains the original dispatches, but the initial full outbound messages were not captured into this repository. This is a recorded evidence gap, not a claim of complete dispatch capture. `run-record.json` records the actual agent identities, task scopes, outcomes and the complete later hardening request retained at its dispatch boundary.

The notes did not prevent partial connectivity checks, incomplete corner reporting or a stale rating caveat in drafts. Coordinator review and counterexample tests corrected those issues. There is no controlled evidence that memory made this construction faster or more correct.

The durable lesson is `docs/solutions/logic-errors/thermal-model-rating-and-connectivity-boundaries.md`. The new immutable `thermal-sense-lessons-v1` revision preserves previous entries and adds a portable complete-connectivity/rating-scope procedure. Future attempts must select it through the memory gate and retain actual outbound bytes. No next-unit dispatch is implied by publishing the catalog.
