# Native backend receipt

Host: arm64 macOS, M2 Pro. Date: 2026-09-12.
Elmer official source commit: `a19504ac53ec222e3355e182b08f2ff280c2203a`
(annotated tag `release-26.2.1`). Source was not patched.

`configure.log`, `build.log` and `install.log` retain the successful GNU
compiler build. `configure-appleclang-failed.log` retains the initial OpenMP
discovery failure. Executables are identified in `executable-sha256.txt`.
See [installation instructions](../../INSTALL.md) for commands and dependencies.

| Upstream reference | Direct installed serial solve |
|---|---|
| ConstantUnknownPotStatCurrent | PASS (`TEST.PASSED` = 1) |
| HeatAniso | PASS (`TEST.PASSED` = 1) |
| ThermalActuator | PASS (`TEST.PASSED` = 1) |

Each directory retains the complete installed-solver log and native result flag.
The upstream inputs and test scripts are retained in `upstream-cases.tar.gz`.
Meshes were generated by each upstream CMake test script before the direct solve.
Direct invocation was `OMPI_MCA_btl=self OMP_NUM_THREADS=1
/Users/bennet/.local/opt/elmer-26.2.1/bin/ElmerSolver` in the generated case
directory, using its upstream `ELMERSOLVER_STARTINFO`.

`ctest-mpi-failed.log` and `mpi-error.log` retain the unsuccessful CTest launch:
the installed `mpiexec`/PRRTE encountered a pre-existing PMIx ABI mismatch.
The CTest invocation did not run the solver and is not a numerical failure.
Direct serial results above do not certify multi-process MPI operation.

`openmp/` retains the finest bar mesh and inputs plus one-thread and four-thread
solver logs. The four-thread run reports its actual thread count; both scalar
files are byte-identical. Elmer-reported wall times were 1.12 s (one thread)
and 1.09 s (four). This is a runtime compatibility check, not a speedup benchmark.

These selected upstream tests and the Zapote bar reference do not establish
correctness for all Elmer physics, elements, materials or boundary conditions.
No PCB thermal acceptance decision is made here.
