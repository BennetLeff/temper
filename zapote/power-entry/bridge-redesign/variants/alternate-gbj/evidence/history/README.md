# Superseded evidence

Files in this directory are retained for audit history only. They were
captured during intermediate polarity, routing, or library-sync iterations and
refer to earlier board hashes. They are not acceptance inputs. Consumers must
use the current `native-fresh.json` and `manufacturing-fresh.json` beside this
directory, whose board hash is recorded in `source-manifest.json` and
`validation-note.md`.
