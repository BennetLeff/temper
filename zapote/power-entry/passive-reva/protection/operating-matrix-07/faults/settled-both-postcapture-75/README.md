# BOTH-SHORT bounded post-capture packet

Mechanical derivative of parent-reviewed postcapture-69 (`run.sh` base
SHA-256 `6d697294d4aa710c1b7cb87f20fc54a81af7f59fa745b6dc072a3e7091d88a73`).
Only case/source/output roots, BOTH-SHORT manifest/case hashes, and
`both-short` runner/validator literals were substituted. The packet keeps
the complete-capture/export/raw-hash gates, 10 GiB floor, all five bounded
passes, exact normal-prefix endpoint, and crest phase interval.

BOTH-SHORT is a failed-channel categorical experiment. It cannot be a
protection PASS; node/detector failures precede any `PROTECTION_GAP` label.
F2 remains closed, and measured channel current includes the failed branch.
The separate `legacy-strict.sh` records the strict timestamp-checker result after the full diagnostic packet completes; it has not run.

