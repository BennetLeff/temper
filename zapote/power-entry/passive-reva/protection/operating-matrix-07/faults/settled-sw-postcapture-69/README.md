# SW-SHORT saved-archive checks

Prepared and parent corrected; no solver or archive scan has been launched.
The input is settled-sw-short-59/full-SW-SHORT and the fresh output is
settled-sw-postcapture-69/full-SW-SHORT. Run only after the existing capture
wrapper has terminal runner.exit and monitor.exit files.

The wrapper retains the reviewed postcapture58 structure: exact source manifest,
all materialized source hashes, run parameters, successful stage-1 host/pigz,
complete native export and row count, gzip integrity, raw size/hash, pinned tools,
and a 10 GiB disk check before each pass. A failed initial electrical or transport
pipeline does not prevent independent diagnosis of a complete archive; its
failure is retained and never waived. The output directory cannot be reused.

Five complete passes record every child status and stderr: own prefix selection,
event-aware normal audit, normal metrics, source crest phase, and the reviewed
post-injection observation tool55. Prefix cutoff is650ms with a maximum1us gap;
normal checks use the actual selected endpoint and190ohm load. Phase uses the
preselected interval[.65,.6583333333333], expected marker.6541666666667,
endpoint.662 and unchanged1% all-choice criterion. The three selector reports
must agree. Observability must agree on total rows, endpoint and injection time.
The raw archive hash is checked before and after the whole analysis packet.

The final receipt remains accepted:false. Parent review must distinguish a
healthy prefix from a failed-short protection outcome and retain node/detector
failure precedence. i(Vchannel) includes the failed parallel branch here.
Sampled off and current counts are independent; the exact shared-row overlap
bound and limitations are in ../failed-switch-observability-70.md. Neither a
normal prefix nor low gate voltage can confer a protection PASS on SW-SHORT.
The separate legacy checker witness is not included in this script.

Parent corrections restored source/hash/count/endpoint/selector-agreement,
disk guards, explicit terminal analysis receipt and the ability to diagnose
complete stage-1 data after a failed stage2. Original worker files are retained.
