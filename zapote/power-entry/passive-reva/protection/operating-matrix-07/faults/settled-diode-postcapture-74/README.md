# DIODE-SHORT bounded post-capture packet

Mechanical derivative of parent-reviewed postcapture-69 (`run.sh` base
SHA-256 `6d697294d4aa710c1b7cb87f20fc54a81af7f59fa745b6dc072a3e7091d88a73`).
Only case/source/output roots, DIODE-SHORT manifest/case hashes, and
`diode-short` runner/validator literals were substituted. The packet keeps
the 10 GiB guard, complete-capture/export/raw-hash gates, all five bounded
passes, observation pass, exact endpoint selector, and phase crest interval.

DIODE-SHORT is a shared-terminal graph experiment. It cannot establish
individual diode-die allocation, package current sharing, thermal/SOA, or physical protection. The frozen checker may return a model-level PASS if its latch/current screens pass; that does not qualify the diode package or its branch stresses. F2 remains closed. The separate `legacy-strict.sh` records the strict timestamp-checker result after the full diagnostic packet completes; it has not run.

