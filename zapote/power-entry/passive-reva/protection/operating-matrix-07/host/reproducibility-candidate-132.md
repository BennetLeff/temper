# Reproducibility candidate 132

Status: **PENDING_BYPASS_FINAL_REVIEW**; `objective_complete: false`. This candidate merges the parent-corrected index 87, addendum 119, and stable reviewed artifacts through 130. It is not the final index and does not claim all-case checks complete.

Actual SHA-256 and byte sizes were computed for 293 deduplicated regular files at or below 20 MiB. FIFOs, non-regular files, and raw archive bytes were not read or rehashed. Prior recorded small-file hashes were compared; `0` drift records were found.

Raw and partial archives are listed by source-bound receipt digest and current `stat` size only. The archive identity is not revalidated by hashing. Completed normal cases and DIODE/BOTH remain modeled dispositions; SW-SHORT remains INDETERMINATE with an accepted own prefix but missing full endpoint. BYPASS-NEG capture 130 is complete but has a VD node-screen failure (651.173 V > 500 V); its postcapture, legacy, own-prefix/phase, and detector-independent negative-control review remain pending.

The model-gap authorities are included as small evidence files: saved ISENSE scan/observability 50, controller binding 54, clamp limiting-envelope 13 and clamp datasheets, inductor inventory/audits 72/73, and vendor availability records. These documents bound the **claim scope and conditional authored model**, not the selected Vishay BAV23C low-current VF envelope itself; that guaranteed temperature/lot characteristic remains physically unbounded and is retained as a named hardware question. The encrypted/average TI assets do not become an open ngspice transient model.

The final append list is deliberately explicit: complete and parent-review BYPASS postcapture/negative-control evidence, then append the final report/index and full-goal audit. No solver, numerical scan, raw read, or model edit was performed while preparing this candidate.

JSON companion: [reproducibility-candidate-132.json](reproducibility-candidate-132.json).

Parent review 135 corrected two guessed archive receipt paths to actual verdict98/118 and refreshed the command states; the original worker files are retained.
