# Reproducibility addendum 119

This addendum supplements [reproducibility-index-87](reproducibility-index-87.md);
it is not the final campaign index. I rechecked all 222 regular files listed
by index87: none drifted, and index87 was not rewritten. No FIFO or raw
archive was read or rehashed.

The JSON companion records measured hashes and byte sizes for the new evidence:
normal margins, DIODE verdict and coincident-observation receipts, BOTH's
campaign verdict plus accepted own normal-prefix and crest-phase receipts,
preparation109/parent110, reference inventory102/105/107/111, the two small TI
source PDFs, and completion audits114/115. The completed DIODE and BOTH raw
archives are represented only by their already source-bound receipt hashes and
recorded sizes; they were not rehashed in this pass.

The later parent-both-result-review-120 diagnostic receipt and its small
receipt, observations, pipeline, analysis, and preflight files are also
included. They preserve the all-original55-field identity and same-row
diagnostic scope; they do not change the validator's earlier node-screen
`FAIL` or add hardware acceptance.

DIODE remains a complete source-bound capture with its detector-window failure
and same-row diagnostics. BOTH now has an explicit modeled `FAIL` for the
all-row Lboost screen, accepted own normal-prefix and phase evidence, and the
legacy strict rejection retained as expected repeated-time behavior. These
facts are reproducible evidence, not protection or hardware acceptance.

The final index still must incorporate the completed BYPASS negative-control
capture and its own prefix/phase, legacy, and waveform review, then bind final
commands, receipts, hashes, margins, and failure reasons. Files associated with
the changing116/117 work and live outputs are intentionally excluded.
