# Retained failed model attempt

The first assumed model used `Is=1e-9 N=1.9 Rs=2` (other parameters as in
the final `clamp.inc`).  At the −0.438 V PCL probe it produced
`V(ISENSE)=-0.436419 V` and `I(DCLAMP)=7.187e-6 A`, which was rejected because
the nominal model conducted too much at the PCL threshold for this bounded
screen.  The run was not counted as evidence.  The final model is still only
typical/assumed; tuning it does not establish a vendor guarantee.
