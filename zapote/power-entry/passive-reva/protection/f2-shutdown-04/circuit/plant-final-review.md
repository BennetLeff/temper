# Final read-only plant review

The current canonical extractor has a coherent, explicitly bounded verdict boundary.  It now parses the 30-column trace including `i(vbody_sense)`, uses Cds=344 pF plus Cgd=112 pF, integrates body and boost diode absorption separately, counts gate-port work, uses the exact 992.82-kΩ divider, and computes post-open residual against local 19.8-uF headroom at the actual open voltage.  The 13-case expectation and trace-gap guard are present.  The reported rows show 12 PASS and the deliberate `slowed_gate_negative` FAIL; 40/45/50-A channel currents are approximately 41.66/46.23/51.10 A, peak VD remains below 500 V (about 477.45 V worst row), and the 1-ns refinement is stable.  The terminal settling field is null when reverse ringing persists through the observation endpoint, which is the honest result.

The boundary is stated in `energy_balance_window`: post-open line plus gate-port work, device absorption, explicit stored energy, and local losses.  Whole-interval residual intentionally omits F2's pre-open 15-mΩ loss and logic/aux rail circuits; post-open `fault_energy_residual` is the quantity compared with local headroom.  This is a valid bounded claim as long as published text keeps that distinction and does not call the full-interval table a closed conservation proof.

I found no remaining must-fix in the current extractor equations.  `terminal_current = sw_mid/1 mΩ` is a true source-terminal current because the body-diode and channel paths meet at `sw_mid` before the Kelvin resistor; the separate `i_switch` remains the positive channel metric.  The body-diode term has the correct source-to-drain voltage sign, and gate source work is counted as `gate_cmd*(gate_cmd-gate)/10`.  Remaining limitations are appropriately declared: the plant uses source-bound surrogates rather than vendor SPICE, controller logic/aux power is outside the energy boundary, and terminal ring may outlast the simulated window.  Preserve those declarations in the final evidence rather than converting null terminal settling into a pass claim.
