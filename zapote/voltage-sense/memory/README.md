# Memory use and next-unit learning

`attempt-001` preserves Rust selection from `current-sense-lessons-v1`. The selected portable RTD model-qualification and current-sense land-pattern notes were read during construction; the board-specific RTD result was excluded because this attempt supplied no matching RTD source identities. Luna was instructed to read the prepared context. This is evidence of selection and reported/observed review use, not a generic transport receipt proving which bytes a provider consumed.

The notes did not prevent every draft defect. Coordinator review corrected electrical arithmetic, source/native matching, weak negative-test assertions, native footprint metadata and a same-package symbol-name collision. Native oracles and rejecting tests enforce the accepted behavior.

The durable explanation is `docs/solutions/logic-errors/voltage-unit-evidence-boundaries.md`. A new immutable `voltage-sense-lessons-v1` catalog carries the earlier entries unchanged and adds a portable identity/evidence procedure. `next-unit-preparation` records a Rust retrieval check only; it does not start the next unit or claim improved model effectiveness. No controlled comparison was performed.
