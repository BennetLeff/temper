# Memory used and learned

The attempt began with the existing Rust memory selector and `rtd-lessons-v1`. `selected-context/` retains the actual selection and rendered notes. It selected portable `rtd-mem-001`; RTD-specific numerical facts were excluded because this unit had no matching RTD source identities. The selected notes were supplied in the Luna work context; the circuit worker's `../circuit/actual_memory_use.md` records reported use. Selection, delivery context and agent self-report are different evidence levels.

The notes did not prevent incorrect first drafts. Coordinator review still found a false AC impedance interpretation, a recreated connectivity extractor, incorrect model loading signs, stale bias values and native-to-donor shape-code confusion. The revised implementation and regression controls are the enforceable part of the learning. No controlled baseline comparison was performed, so this attempt does not establish a measured memory effectiveness improvement.

The reviewed transformer drawing finding is now a durable document in `docs/solutions/logic-errors/cst3015-land-pattern-edge-gap.md` and a portable note in `zapote/skills/revisions/current-sense-lessons-v1/`. `release-check/` demonstrates that Rust selects the two portable procedures and rejects the RTD-specific fact without matching identities. That check prepared context only; it did not dispatch a future construction agent.

All 54 inputs bound by the prior memory closeout were copied and hash-verified under `zapote/artifacts/memory-integration-before-current-sense/` before shared Rust files changed. The original revision and frozen executable remain available. New source, code and evidence are bound by this unit's acceptance manifest.
