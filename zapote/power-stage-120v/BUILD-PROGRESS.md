# Native-board construction

Source baseline: `ad51823308f3a65d7bfe86351a128af5f237b31e`
(`docs/power-stage-board-plans`, 2026-09-25).
Integration branch: `codex/power-stage-120v-build`.

The requested implementation uses native Codex subagents configured as
`gpt-6-sol`, reasoning effort `xhigh`. Each implementation agent writes only
its own worktree. The coordinator reviews and verifies changes before
integrating them. The original `cooker-120v` plan checkout is retained.

| Work | Ownership | State |
| --- | --- | --- |
| Part 1: native bridge and unit wrappers | `ps-toolchain-sol`, integrated by coordinator | Complete; commit `abe54b375` |
| Part 2: eight local footprints | `ps-footprints-sol`, integrated by coordinator | Complete; initial commit `a420c3804`, followed by corrected CST3015 land pattern |
| Insulation preflight | Read-only Sol worker, reviewed by coordinator | Complete; D5 unresolved |
| Part 3: source-bound native shelf | `ps-native-sol`, regenerated by coordinator | Complete; audited shelf and native/source oracles pass |
| Part 4: stackup, rules, placement review | After Part 3 and mechanical decisions | Pending |
| Parts 5–6: routing and final verification | After written placement approval | Pending |

## Baseline checks

- `scripts/assert-base.sh ad5182330`: PASS.
- `kicad-cli version`: 10.0.4.
- `rustc --edition=2021 --test audit.rs -o /tmp/ps-build-audit-tests`
  followed by the test binary: 18 passed, 0 failed, including the crossed
  choke winding mutation.
- All 91 resolved source components inspected for stock footprint presence:
  no missing stock footprint files. The eight unit-local library files are
  the Part 2 work.
- KiCad 10.0.4 `pcbnew.FootprintLoad` loaded all 80 stock-library component
  instances; every connected source pin has a matching numbered pad.

## Integration checks

The coordinator rebuilt `source-build-01` in the integration checkout: 91
components, 67 nets, audit PASS. Normalized netlist and CSV bytes match
`frozen/`, retaining line endings. The coordinator independently reran all
81 design-bundle tests and verified the installed bridge extension fresh
against the integrated source. See `TOOLCHAIN.md` for the isolated runtime.

The restored Python consumers exposed an omission in
`scripts/check_unwired_kernels.py`: it did not scan `harness-lab/` or
`zapote/`. A regression first failed because those real production roots
were absent. The scanner now includes both while excluding their tests;
filters use repository-relative paths so a checkout directory named
`test_*` cannot hide its entire source tree. All eight scanner tests pass;
all 26 repository-state tests pass. No unwired-kernel inventory was widened.

The import gate reports five kept contracts, zero broken, zero new
violations. It ran with locked import-linter 2.14 and PyYAML 6.0.3 in
`/tmp/ps-check-venv`, with `PYTHONPATH=packages/temper-placer/src` and
`UV_PROJECT_ENVIRONMENT=/tmp/ps-check-venv`.
`python3 scripts/regen_derived.py --check` passes all derived-artifact gates.

A three-reviewer Sol `xhigh` simplification pass covered the new wrappers,
scanner and shelf helper. The source wrapper now reuses the adapter's hash
helper and pinned Atopile version. Its fresh rebuild still passes the audit
and reproduces both normalized frozen files exactly; its receipt pins the
current wrapper. Redundant shelf subprocess environment copying was removed,
and the Rust integer-bound type was renamed to state its micrometre units.
A proposed footprint cache was not added: 91 bounded loads have not shown a
performance problem. The archived adapters were excluded from simplification.

Both unit footprint libraries pass independent KiCad 10.0.4 parsing.
The first CST3015 donor copy was parseable after comment removal, but later
inspection of Coilcraft's drawing exposed wrong primary pad orientation and
row spacing. The unit now uses the corrected current-sense donor: 18.5 mm
primary-to-secondary copper gap. Native generation, source oracles and three
DRC runs were repeated on the corrected board. All 91 parts fit the shelf;
DRC retains 18 explained annotation mismatches and at least 199 unrouted
connections. ERC has 210 warnings and zero errors. Missing physical stackup
is the expected Part 4 gate failure. See `NATIVE-01.md` for exact results,
hashes and limits; `FOOTPRINTS.md` retains assembly qualification limits.

## Decisions

D1–D3 (board size, heatsink, stackup) and D6 (mains/coil exit edges) have been
requested from the owner. The documented defaults are proposals, not recorded
approvals. D5 insulation remains open; `RULES-PREFLIGHT.md` records the
primary-source review and the unresolved full-bridge working-voltage basis.
The owner requested a detailed question for an external oracle;
`ORACLE-INSULATION-QUESTION.md` supplies the circuit context and reading
packet. The owner supplied an external answer proposing a functional earth bond,
a lower voltage band and CT relocation. `ORACLE-REVIEW.md` records the
review: the rectifier-clamp argument does not establish the claimed voltage
bound, and these source/architecture proposals remain unapproved.

## Evidence limits

The archived engineering-memory workflow was read from
`archive/zapote-coil-intake-2026-09-25:zapote/skills/README.md`; its transport
is not present on the current baseline. Native subagent dispatches and their
messages are retained by this Codex task. No archived memory-transport receipt
or claim of provider-side memory consumption is invented.

Physical assembly, energized tests, thermal/EMI measurements, insulation
testing and certification: **NOT RUN**. No fabrication release is claimed.
