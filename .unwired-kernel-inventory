# Registered Rust kernels with no production caller.
#
# Shrink-only. A new entry is a hard failure -- wire the kernel or add it
# here WITH A REASON. An entry that becomes wired is also a failure, so
# paid-down debt shows up in a diff instead of rotting on the books.
#
# Generated by scripts/check_unwired_kernels.py --write-inventory
# <symbol>\t<reason>
#
# TRIAGE (docs/plans/2026-08-11-003-feat-migration-pipeline-wire-and-retire-plan.md,
# unit U2): every reason below carries exactly one of three tags so the
# distinction survives the next reader --
#
#   [NEVER-WIRE-BY-DESIGN]  introspection/PBT surface, typed sub-object
#     consumed anonymously (invisible to the AST scan), a differential-only
#     probe where inventing a caller would serve no one, or a kernel that IS
#     already wired but through a mechanism the AST scan cannot see
#     (cross-extension Rust-to-Rust `PyModule::import`/`getattr`, a pyo3
#     rename alias). Correct as-is; nothing to change.
#   [ORPHANED-DELETE]  the Python (or Rust caller) this kernel served no
#     longer exists -- a shim was retired, a marshaler collapsed into a
#     typed constructor, or a newer kernel supersedes this one outright.
#     Dead weight; a delete candidate for a future cleanup pass (out of
#     U2/U3 scope -- deleting a kernel can touch its oracle/differential,
#     which is U4/U5's job under the retirement bar).
#   [WIRE]  a real stalled migration: production Python still does the work
#     this kernel already does, and a caller can be repointed without
#     inventing new architecture or a behavioural decision.
#
# 2026-08-11 full pass: 107/107 triaged, 0 WIRE / 70 NEVER-WIRE-BY-DESIGN /
# 37 ORPHANED-DELETE. Every candidate that looked plausibly WIRE from its
# name alone (DrillDefinition, evaluate_quality_py, net_currents, py_sum,
# tokenize, HypergraphBuildResult, is_via_position_valid,
# place_via_with_clearance, via_distance, required_clearance_py,
# parse_capacitance_rs, the via_count_* family) was traced to its actual
# production call site and resolved NEVER-WIRE-BY-DESIGN -- see each entry's
# reason for the specific evidence. U3 (drain the WIRE partition) is
# therefore empty for this pass: there is nothing to repoint. See the plan's
# Outstanding-Assumption clause -- the 107-kernel backlog is, on inspection,
# not a backlog of stalled migrations.
#
# 2026-08-12 spike (branch spike/pyany-removal): 4 of the 37 ORPHANED-DELETE
# entries deleted -- PyCompiler, build_adjacency_flat, normalized_bundle_json,
# points_centroid_winding -- the subset with ZERO Python references (prod or
# test). The other 33 ORPHANED-DELETE entries stay: every one is referenced by
# a wave4 differential/PBT suite (the "delete alongside its oracle+differential"
# clause of each entry's reason -- that is U4/U5 retirement work, not a safe
# removal).
#
# 2026-08-12 fanout16 triage (branch fanout16/work-5, off 765859caa): per-entry
# re-verification of every ORPHANED-DELETE claim against live call sites.
#   DELETED (14): monte_carlo family (DistributionParams, ManufacturingVariables,
#     MonteCarloConfig, MonteCarloResult, MonteCarloSimulator -- whole module
#     manufacturing_monte_carlo.rs, dedicated tests+oracle) + footprint family
#     (FootprintBounds, parse_footprint_courtyard, parse_footprint_directory) +
#     golden_serializers family (serialize_boardstate_to_dsn/ses,
#     serialize_connectivity_to_json, serialize_violations_to_json) +
#     config_binding family (extract_config_refs_py, verify_config_matches_netlist).
#     All had zero production callers AND zero retained oracle (repointed tests
#     only) -- whole-module removals incl. wasm_test_registry entries.
#   TRIAGE ERRORS FOUND (4): pin_world_position_at_py / point_distance_py /
#     track_midpoint_py / pad_radius_py were tagged ORPHANED-DELETE but ARE wired
#     at runtime via cross-extension Rust getattr/call_method1 (model_builder.rs,
#     geometry_types_contracts.rs -> temper_geometry). Re-tagged NEVER-WIRE-BY-DESIGN.
#   KEPT (38): the rest, all differential/PBT/oracle-retained or dataclass shapes
#     still wanted for a future stage (DiffPairConfig, tolerances, stackup). Each
#     entry carries a trailing "KEPT 2026-08-12 triage: differential-only" marker.

CopperWeight	[ORPHANED-DELETE] 2026-08-12 spike/deletable-python: sole production caller was the pure-delegation shim manufacturing/tolerances.py (re-exported from temper-design-bundle manufacturing_tolerances.rs), deleted same day as a zero-prod-consumer R21-shaped shim (d8e60a532); kernel remains the implementation exercised only by the repointed test_tolerances.py + retained differential/PBT/oracle suites. Delete alongside its oracle or keep as differential-only. KEPT 2026-08-12 triage: differential-only (see header).
DiffPairConfig	[ORPHANED-DELETE] TRIAGED 2026-08-11: the Python it was built to replace (deterministic/stages/sequential_routing_dataclasses.py, pinned by the still-retained tests/deterministic/stages/_sequential_routing_dataclasses_py_oracle.py) was itself deleted as a zero-importer shim in b1fd7edb6 ("chore(wave4): delete the 3 R21 zero-importer shims"). Not a stalled migration -- the feature this pyclass ports had no production caller before or after. Delete candidate alongside its now-orphaned oracle+differential, or keep as differential-only if the dataclass shape is still wanted for a future diff-pair stage. KEPT 2026-08-12 triage: differential-only (see header).
DrcComponentSnapshot	[NEVER-WIRE-BY-DESIGN] Phase-A U5 (2026-08-10) typed sub-snapshot of DrcBoardSnapshot (drc_marshal.rs); production shims (drc_runner.py/drc_oracle.py) consume DrcBoardSnapshot.to_dict()/run_drc and never name the sub-snapshot types directly -- exposed for typed introspection + PBT.
DrcNetClassRuleSnapshot	[NEVER-WIRE-BY-DESIGN] Phase-A U5 (2026-08-10) typed sub-snapshot of DrcBoardSnapshot's net_class_rules; same production-consumption shape as DrcComponentSnapshot -- introspection/PBT surface only.
DrcTraceSnapshot	[NEVER-WIRE-BY-DESIGN] Phase-A U5 (2026-08-10) typed sub-snapshot of DrcBoardSnapshot's traces; introspection/PBT surface only.
DrcViaSnapshot	[NEVER-WIRE-BY-DESIGN] Phase-A U5 (2026-08-10) typed sub-snapshot of DrcBoardSnapshot's vias; introspection/PBT surface only.
DrillDefinition	[NEVER-WIRE-BY-DESIGN] RESOLVED 2026-08-11 (was: "needs triage" since PR #839). The class itself is never named by production Python, but it is not dead: parse_engine.rs constructs `DrillDefinition` instances internally for every pad carrying a `(drill ...)` token, and those instances flow into `Pin.drill`/`PadData.drill` on the objects `io/_parse_board.py` (production, `_rs = _tdb.parse_engine`) returns -- e.g. `io/kicad_exporter.py:453-456` reads/clears `pad.drill` without ever importing the type name. Same "typed sub-object consumed anonymously" shape as ExtractedLoopWire/HypergraphBuildResult/SkipExpr; the AST scan cannot see field-access-only consumption. Nothing to wire.
ExtractedLoopWire	[NEVER-WIRE-BY-DESIGN] Phase-A U9 (2026-08-10) typed per-loop wire element of LoopExtractionOutput (loop_extraction_contracts.rs); production shims consume it anonymously field-by-field via LoopExtractionOutput.loops[...] (core/loop_extractor_rs.py reads .name/.loop_type/.components/.nets/.max_area_mm2), so no Python file ever NAMES the type -- exposed for typed introspection + PBT, same consumption shape as the U5 Drc*Snapshot entries.
FeatureTolerance	[ORPHANED-DELETE] 2026-08-12 spike/deletable-python: same tolerances shim deletion as CopperWeight above (manufacturing/tolerances.py removed in d8e60a532); test-only today. KEPT 2026-08-12 triage: differential-only (see header).
HypergraphBuildResult	[NEVER-WIRE-BY-DESIGN] RESOLVED 2026-08-11. pyclass return type of HypergraphFactory.build() (design-bundle hypergraph_factory.rs:84); consumed anonymously field-by-field in extraction/hypergraph_factory.py:95-131 (parts.node_refs etc.) so no Python file ever NAMES the type -- the AST scan cannot see it. Confirmed by direct search: extraction/hypergraph_factory.py itself has zero production importers anywhere in packages/temper-placer/src (core/hypergraph.py only mentions it in a docstring); every importer is a test (test_hypergraph_factory_rust_differential.py, test_hypergraph_factory_pbt.py, _hypergraph_factory_py_oracle.py, test_hypergraph.py). The whole HypergraphFactory/HypergraphBuildResult chain is test-driven only today -- wiring it would mean inventing a production caller, not serving one.
LayerConfig	[ORPHANED-DELETE] 2026-08-12 spike/deletable-python: sole production caller was the pure-delegation shim core/stackup.py (re-exported from temper-design-bundle stackup_contracts.rs), deleted same day as a zero-prod-consumer R21-shaped shim (d8e60a532); kernel remains the implementation exercised only by the repointed test_stackup.py/test_ipc2152.py + retained differential/PBT suites. Delete alongside its oracle or keep as differential-only. KEPT 2026-08-12 triage: differential-only (see header).
LayerType	[ORPHANED-DELETE] 2026-08-12 spike/deletable-python: same tolerances shim deletion as CopperWeight above (manufacturing/tolerances.py removed in d8e60a532); test-only today. KEPT 2026-08-12 triage: differential-only (see header).
SkipExpr	[NEVER-WIRE-BY-DESIGN] TRIAGED 2026-08-11: gate blind spot, not a real unwiring -- pipeline/dag_expr.py:94 calls temper_io_types.parse_skip_expr_rs (production, from parse_skip_expr) and consumes the returned SkipExpr/PySkipExpr instance anonymously (handle.to_spec() at dag_expr.py:96, stashed on the ast.Expression via _HANDLE_ATTR); no Python file ever names the class `SkipExpr` literally, so the AST scan cannot see the consumption. Same shape as the DrcComponentSnapshot/HypergraphBuildResult/ExtractedLoopWire entries above.
Stackup	[ORPHANED-DELETE] 2026-08-12 spike/deletable-python: same stackup shim deletion as LayerConfig above (core/stackup.py removed in d8e60a532); test-only today. KEPT 2026-08-12 triage: differential-only (see header).
ToleranceAnalyzer	[ORPHANED-DELETE] 2026-08-12 spike/deletable-python: same tolerances shim deletion as CopperWeight above (manufacturing/tolerances.py removed in d8e60a532); test-only today. KEPT 2026-08-12 triage: differential-only (see header).
ToleranceTable	[ORPHANED-DELETE] 2026-08-12 spike/deletable-python: same tolerances shim deletion as CopperWeight above (manufacturing/tolerances.py removed in d8e60a532); test-only today. KEPT 2026-08-12 triage: differential-only (see header).
apply_damped_position_py	[ORPHANED-DELETE] orphaned 2026-08-08 by retiring the dead router_v6/apply_suggestions.py shim (zero production callers); the kernel regenerates positions internally from congestion regions -- no live caller shape exists. Differential/export-only or delete. KEPT 2026-08-12 triage: differential-only (see header).
apply_suggestions_damped_py	[ORPHANED-DELETE] TRIAGED 2026-08-11: same family as apply_damped_position_py/apply_total_movement_py/apply_update_positions_py below -- orphaned 2026-08-08 by retiring the dead router_v6/apply_suggestions.py shim (zero production callers); no live caller shape remains. Differential/export-only or delete. KEPT 2026-08-12 triage: differential-only (see header).
apply_total_movement_py	[ORPHANED-DELETE] orphaned 2026-08-08 by retiring the dead apply_suggestions.py shim; same fresh-state contract gap as apply_damped_position_py. Differential/export-only or delete. KEPT 2026-08-12 triage: differential-only (see header).
apply_update_positions_py	[ORPHANED-DELETE] orphaned 2026-08-08 by retiring the dead apply_suggestions.py shim; no production caller. Differential/export-only or delete. KEPT 2026-08-12 triage: differential-only (see header).
assign_component_zones	[NEVER-WIRE-BY-DESIGN] CONFIRMED (temper-orchestration zone_assignment_stage.rs:55 `.call_method1("assign_component_zones", ...)`) -- wired from temper-orchestration (Phase D batch D2, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the deterministic leaf kernel stays single-source in design-bundle (the D2 delegation boundary, VERIFICATION.md D2 section).
assign_components_to_slots	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration component_assignment_stage.rs (Phase D batch D4, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (component_assignment.py's _assign_components_to_slots) became a thin FFI delegation to the Rust stage, so the leaf kernel stays single-source in design-bundle (the D4 delegation boundary, VERIFICATION.md D4 section).
build_board_dict_from_parsed_pcb_py	[NEVER-WIRE-BY-DESIGN] Phase-A U5 (2026-08-10) legacy K1-dict kernel superseded by DrcBoardSnapshot.from_parsed_pcb; retained (unchanged) for the existing test_drc_oracle_marshal_rust_differential.py suite and any external dict caller -- production now goes through the typed constructor.
build_board_dict_py	[NEVER-WIRE-BY-DESIGN] Phase-A U5 (2026-08-10) legacy K1-dict kernel superseded by DrcBoardSnapshot.from_netlist; retained (unchanged) for the existing test_drc_oracle_marshal_rust_differential.py suite -- production now goes through the typed constructor.
build_constraints_dict_py	[NEVER-WIRE-BY-DESIGN] Phase-A U5 (2026-08-10) legacy K1-dict kernel superseded by TypedConstraintSet.from_context; retained (unchanged) for the existing test_drc_oracle_marshal_rust_differential.py suite -- production now goes through the typed constructor.
block_exclusion_zone_into_grid_py	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration grid_stage.rs exclusion_zones (Phase D batch D3, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Rust calls; the EXP-13 exclusion-zone write leaf kernel stays single-source in temper-geometry (the D3 delegation boundary, VERIFICATION.md "Clearance-Grid Residual Leaf Compute" section). Its sibling kernels count_blocked_cells_py / grid_cell_available_py are wired through _grid_core.py; this one has no production Python caller because the exclusion-zone loop was already orchestrated Rust-side.
characteristic_impedance_microstrip	[ORPHANED-DELETE] 2026-08-12 spike/deletable-python: same stackup shim deletion as LayerConfig above (core/stackup.py removed in d8e60a532); test-only today. KEPT 2026-08-12 triage: differential-only (see header).
check_routability_regression	[NEVER-WIRE-BY-DESIGN] CONFIRMED 2026-08-11: `ConvergenceChecker` (pipeline/convergence.py:25, `= _rs.ConvergenceChecker`) is never instantiated in packages/temper-placer/src -- every reference is a test (test_convergence.py, test_convergence_pbt.py, test_convergence_rust_differential.py, test_pipeline_feasibility_rust_differential.py) or an oracle. runner.py's PipelineRunner does not reference convergence at all yet. ConvergenceChecker pyclass method from the Phase-1 orchestration migration (2026-08-09); pre-migration Python had no production caller either (only docstring + tests) -- exposed for the not-yet-built Phase-2 PipelineRunner; differential/PBT-only today. Wiring would mean building that consumer, not repointing an existing one.
check_success	[NEVER-WIRE-BY-DESIGN] same family and same 2026-08-11 confirmation as check_routability_regression above -- ConvergenceChecker pyclass method from the Phase-1 orchestration migration (2026-08-09); pre-migration Python had no production caller either; exposed for the not-yet-built Phase-2 PipelineRunner; differential/PBT-only today.
classify_net_type_v6	[NEVER-WIRE-BY-DESIGN] exported probe from the net_classification io-types migration (merged 2026-08-08); router_v6/net_classification.py calls the specific helpers (_rs.is_ground_net / is_power_net_v6 / is_hv_net) and never the combined classifier -- wiring it would mean inventing a caller, not serving one.
classify_nets_py	[NEVER-WIRE-BY-DESIGN] quality-oracle lib.rs:530 batch netlist classifier; only caller is tests/rust_integration/test_quality_oracle.py:57. Production net classification flows through router_v6/net_classification.py predicates wired to temper_io_types (is_ground_net/is_power_net_v6/is_hv_net -- see ledger's classify_net_type_v6/is_signal_net_v6). Differential-only probe superseded by the io-types predicates.
closest_component_for_zone_py	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration grid_hv.rs (Phase D batch D3, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the HV spatial-fallback leaf kernel stays single-source in temper-geometry (the D3 delegation boundary, VERIFICATION.md D3 section).
component_reach_py	[NEVER-WIRE-BY-DESIGN] CONFIRMED (temper-drc-rs req_safe_01.rs:200 `.getattr("component_reach_py")`) -- reached at runtime through a Rust-side getattr from req_safe_01_copper_model_init (req_safe_01.rs:324), which production _copper.py:132 (real_board.py:601 -> _CopperModel) calls. The Python AST scan cannot see Rust getattr call sites, so this entry is a gate blind spot, not a real unwiring; the differential oracle (tests/requirements/clearance_oracle/_copper.py:169) also calls it directly.
congestion_analyze_py	[NEVER-WIRE-BY-DESIGN] DELIBERATE, documented in router_v6/congestion.py: the kernel's contract (see congestion_analysis.rs) binds positions=None/layer_assignments=None always, resolves every net on layer 0, and assumes a (0.0, 0.0) board origin -- while every production caller (metrics/physics.py, router_v6/verifier.py) passes positions= and/or layer_assignments= and real boards may carry a non-zero origin. The kernel's own bottom-side mirror is present (#832); wiring it to analyze_congestion would silently mis-place demand on any board using the excluded shapes. Its per-net demand accumulation is wired through congestion_estimate_net_demand_py instead.
congestion_heatmap_at_py	[ORPHANED-DELETE] TRIAGED 2026-08-11: same family as congestion_heatmap_from_router_py below (congestion_heatmap.rs's own module doc lists all four together) -- orphaned 2026-08-08 by retiring the dead router_v6/congestion_heatmap.py shim (zero importers); no live caller shape remains. Differential/export-only or delete. KEPT 2026-08-12 triage: differential-only (see header).
congestion_heatmap_from_router_py	[ORPHANED-DELETE] orphaned 2026-08-08 by retiring the dead router_v6/congestion_heatmap.py shim (zero importers); kernel rebuilds the grid from raw fields the dataclass doesn't hold. Differential/export-only or delete. KEPT 2026-08-12 triage: differential-only (see header).
congestion_heatmap_hotspots_py	[ORPHANED-DELETE] TRIAGED 2026-08-11: same family as congestion_heatmap_from_router_py above -- orphaned 2026-08-08 by retiring the dead router_v6/congestion_heatmap.py shim (zero importers); no live caller shape remains. Differential/export-only or delete. KEPT 2026-08-12 triage: differential-only (see header).
congestion_heatmap_total_py	[ORPHANED-DELETE] TRIAGED 2026-08-11: same family as congestion_heatmap_from_router_py above -- orphaned 2026-08-08 by retiring the dead router_v6/congestion_heatmap.py shim (zero importers); no live caller shape remains. Differential/export-only or delete. KEPT 2026-08-12 triage: differential-only (see header).
connectivity_validate_net_py	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration connectivity_validation_stage.rs (Phase D batch D6, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (connectivity_validation.py's _validate_net_connectivity) became a thin FFI delegation to the Rust stage, so the UnionFind leaf kernel stays single-source in temper-drc-rs (the D6 delegation boundary, VERIFICATION.md D6 section).
constraint_value_to_plain_py	[NEVER-WIRE-BY-DESIGN] Phase-A U5 (2026-08-10) legacy kernel superseded by ConstraintValue.from_python; retained (unchanged) for the existing test_drc_oracle_marshal_rust_differential.py suite -- production now goes through the typed constructor.
count_connected_layers_py	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration via_validation_stage.rs (Phase D batch D6, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (via_validation.py's _count_connected_layers) became a thin FFI delegation to the Rust stage, so the via-connectivity leaf kernel stays single-source in temper-drc-rs (the D6 delegation boundary, VERIFICATION.md D6 section).
dedup_via_positions_py	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration via_validation_stage.rs (Phase D batch D6, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (via_validation.py's ViaDeduplicationStage.run) became a thin FFI delegation to the Rust stage, so the via-position-dedup leaf kernel stays single-source in temper-drc-rs (the D6 delegation boundary, VERIFICATION.md D6 section).
deduplicate_traces_py	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration drc_sweep_stage.rs (Phase D batch D6, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (drc_sweep.py's TrackDeduplicationStage.run) became a thin FFI delegation to the Rust stage, so the track-dedup leaf kernel stays single-source in temper-drc-rs (the D6 delegation boundary, VERIFICATION.md D6 section).
define_zone_layout	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration zone_geometry_stage.rs (Phase D batch D2, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the deterministic leaf kernel stays single-source in design-bundle (the D2 delegation boundary, VERIFICATION.md D2 section).
dfm_adjacent_layer_py	[ORPHANED-DELETE] TRIAGED 2026-08-11: verbatim twin of temper-geometry's via_clearance.rs adjacent_layer (that comment names dfm::adjacent_layer explicitly) -- via_placement.py's real caller (_get_adjacent_layer) already delegates to temper_geometry.adjacent_layer_py, wired before this dfm.rs twin existed. Superseded duplicate; delete candidate. KEPT 2026-08-12 triage: differential-only (see header).
dfm_power_pour_bounds_py	[ORPHANED-DELETE] TRIAGED 2026-08-11: verbatim twin of temper-geometry's power_pour_strips_py -- power_plane.py's generate_power_pours already delegates to that kernel (wired ahead of PR #749's dfm.rs port of the same pre-migration function). Superseded duplicate; delete candidate. KEPT 2026-08-12 triage: differential-only (see header).
dfm_rect_polygon_py	[ORPHANED-DELETE] TRIAGED 2026-08-11: verbatim twin of temper-geometry's rect_polygon_py -- power_plane.py's _rect_polygon already delegates to that kernel (wired ahead of PR #749's dfm.rs port of the same pre-migration function). Superseded duplicate; delete candidate. KEPT 2026-08-12 triage: differential-only (see header).
dfm_thermal_via_positions_py	[ORPHANED-DELETE] TRIAGED 2026-08-11: verbatim twin of temper-geometry's thermal_via_positions_py -- power_plane.py's _thermal_via_positions already delegates to that kernel (wired ahead of PR #749's dfm.rs port of the same pre-migration function). Superseded duplicate; delete candidate. KEPT 2026-08-12 triage: differential-only (see header).
dfm_via_segment_index_py	[ORPHANED-DELETE] TRIAGED 2026-08-11: verbatim twin of temper-geometry's via_clearance.rs via_segment_index (comment there names dfm::via_segment_index explicitly) -- via_placement.py's real caller path runs entirely through temper_geometry.via_layer_pair_py, which resolves the segment index internally in Rust. Superseded duplicate; delete candidate. KEPT 2026-08-12 triage: differential-only (see header).
dist_py	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration clearance.rs (Phase E batch E3, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (domain_clearance.py's audit_domain_clearance) became a thin FFI delegation to the Rust audit stage, so the Dekker-vector_norm Euclidean-distance leaf kernel stays single-source in temper-geometry (the E3 delegation boundary, VERIFICATION.md E3 section).
escape_is_position_valid_py	[NEVER-WIRE-BY-DESIGN] PR #839 -- #829's kernel defect is fixed, but the signature is a differential-only probe (implicit identity component transform, no rotation/side); generate_escape_vias delegates wholesale so the real _is_position_valid became dead code and was removed. No real caller shape can use this probe contract.
evaluate_quality_py	[NEVER-WIRE-BY-DESIGN] RESOLVED 2026-08-11 (was: "needs triage" since PR #839). Zero production callers of the monolithic (netlist, placement, spec, metrics) -> verdict-dict oracle::evaluate_prepared path; only tests/rust_integration/test_quality_oracle.py calls it. Production quality scoring already goes through a *different*, already-wired, finer-grained set of temper_quality_oracle kernels instead (placement_score_py/drc_score_py/overall_score_py/interpret_score_py, consumed by metrics/quality_score.py's compute_quality_score) and per-metric kernels in metrics/quality.py (thermal_score/zone_compliance_score/etc, also _tqo-backed). Production Python does NOT do this work -- a different Rust API already does. Wiring evaluate_quality_py would mean replacing an already-wired pipeline with a second one, not repointing a caller.
explain_decision_trace_history	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration explainability.rs (Phase-A U8) DecisionTrace.history via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; single-source NL-generation kernel kept in io-types (the U8 delegation boundary, VERIFICATION.md U8 section).
explain_decision_trace_summary	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration explainability.rs (Phase-A U8) DecisionTrace.summary via runtime PyModule::import -- invisible to the Python AST scan; single-source aggregation kernel kept in io-types (VERIFICATION.md U8 section).
explain_decision_trace_why	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration explainability.rs (Phase-A U8) DecisionTrace.why via runtime PyModule::import -- invisible to the Python AST scan; single-source NL-generation kernel kept in io-types (VERIFICATION.md U8 section).
explain_decision_trace_why_not	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration explainability.rs (Phase-A U8) DecisionTrace.why_not via runtime PyModule::import -- invisible to the Python AST scan; single-source NL-generation kernel kept in io-types (VERIFICATION.md U8 section).
explain_render_component_report	[NEVER-WIRE-BY-DESIGN] orphaned 2026-08-10 by Phase-A U8 -- the MarkdownReport renderers were ported to temper-orchestration explainability.rs (render_component_report/render_markdown_report, byte-pinned by the explainability differentials); io-types renderers superseded, deliberately retained for the existing test_markdown_rust_differential.py hasattr arm rather than deleted outright.
explain_render_markdown_report	[NEVER-WIRE-BY-DESIGN] orphaned 2026-08-10 by Phase-A U8 -- the MarkdownReport renderers were ported to temper-orchestration explainability.rs (render_component_report/render_markdown_report, byte-pinned by the explainability differentials); io-types renderers superseded, deliberately retained for the existing test_markdown_rust_differential.py hasattr arm rather than deleted outright.
explain_trace_why	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration explainability.rs (Phase-A U8) Trace.why via runtime PyModule::import -- invisible to the Python AST scan; single-source NL-generation kernel kept in io-types (VERIFICATION.md U8 section).
extract_medial_axis_py	[NEVER-WIRE-BY-DESIGN] Exposed for the differential only. Production calls the SINGLE form extract_medial_axis_single_py (channel_skeleton.py:262); the batch form is invoked internally in Rust across the polygon list. Wiring it would mean inventing a Python caller, not serving one.
fence_samples_py	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration grid_fence.rs (Phase D batch D3, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the U3 fence-sample leaf kernel stays single-source in temper-geometry (the D3 delegation boundary, VERIFICATION.md D3 section).
fixed_copper_local_pad_half_py	[NEVER-WIRE-BY-DESIGN] wired from the design-bundle FixedCopperBuilder (Phase E batch E2, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (fixed_copper.py's _local_pad_half) became a thin FFI delegation to the Rust builder, so the pad half-extent leaf kernel stays single-source in temper-geometry (the E2 delegation boundary, VERIFICATION.md E2 section).
fixed_copper_other_pad_item_geom_py	[NEVER-WIRE-BY-DESIGN] wired from the design-bundle FixedCopperBuilder (Phase E batch E2, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (fixed_copper.py's _other_component_pad_item) became a thin FFI delegation to the Rust builder, so the pinned-pad obstacle-geometry leaf kernel stays single-source in temper-geometry (the E2 delegation boundary, VERIFICATION.md E2 section).
fixed_copper_pin_copper_layers_py	[NEVER-WIRE-BY-DESIGN] wired from the design-bundle FixedCopperBuilder (Phase E batch E2, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python callers (fixed_copper.py's _pin_copper_layers) became a thin FFI delegation to the Rust builder, so the pin-copper-layer leaf kernel stays single-source in temper-geometry (the E2 delegation boundary, VERIFICATION.md E2 section).
fixed_copper_point_rect_distance_py	[ORPHANED-DELETE] orphaned 2026-08-10 by Phase E batch E2 (plan 2026-08-09-001): the fixed_copper.py shim dropped the dead `_point_rect_distance` wrapper one-liners (the pre-migration exact_clearance_mm had already migrated to the combined fixed_copper_exact_clearance_mm_py dispatch, leaving these wrappers unreferenced); no live caller shape remains -- differential/export-only or delete. KEPT 2026-08-12 triage: differential-only (see header).
fixed_copper_point_segment_distance_py	[ORPHANED-DELETE] orphaned 2026-08-10 by Phase E batch E2 (plan 2026-08-09-001): the fixed_copper.py shim dropped the dead `_point_segment_distance` wrapper one-liners (the pre-migration exact_clearance_mm had already migrated to the combined fixed_copper_exact_clearance_mm_py dispatch, leaving these wrappers unreferenced); no live caller shape remains -- differential/export-only or delete. KEPT 2026-08-12 triage: differential-only (see header).
fixed_copper_rect_rect_gap_py	[ORPHANED-DELETE] orphaned 2026-08-10 by Phase E batch E2 (plan 2026-08-09-001): the fixed_copper.py shim dropped the dead `_rect_rect_gap` wrapper one-liners (the pre-migration exact_clearance_mm had already migrated to the combined fixed_copper_exact_clearance_mm_py dispatch, leaving these wrappers unreferenced); no live caller shape remains -- differential/export-only or delete. KEPT 2026-08-12 triage: differential-only (see header).
fixed_copper_rect_segment_distance_py	[ORPHANED-DELETE] orphaned 2026-08-10 by Phase E batch E2 (plan 2026-08-09-001): the fixed_copper.py shim dropped the dead `_rect_segment_distance` wrapper one-liners (the pre-migration exact_clearance_mm had already migrated to the combined fixed_copper_exact_clearance_mm_py dispatch, leaving these wrappers unreferenced); no live caller shape remains -- differential/export-only or delete. KEPT 2026-08-12 triage: differential-only (see header).
fixed_copper_segment_item_geom_py	[NEVER-WIRE-BY-DESIGN] wired from the design-bundle FixedCopperBuilder (Phase E batch E2, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (fixed_copper.py's _segment_item) became a thin FFI delegation to the Rust builder, so the trace-obstacle-geometry leaf kernel stays single-source in temper-geometry (the E2 delegation boundary, VERIFICATION.md E2 section).
fixed_copper_segments_intersect_py	[ORPHANED-DELETE] orphaned 2026-08-10 by Phase E batch E2 (plan 2026-08-09-001): the fixed_copper.py shim dropped the dead `_segments_intersect` wrapper one-liners (the pre-migration exact_clearance_mm had already migrated to the combined fixed_copper_exact_clearance_mm_py dispatch, leaving these wrappers unreferenced); no live caller shape remains -- differential/export-only or delete. KEPT 2026-08-12 triage: differential-only (see header).
fixed_copper_via_item_geom_py	[NEVER-WIRE-BY-DESIGN] wired from the design-bundle FixedCopperBuilder (Phase E batch E2, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (fixed_copper.py's _via_item) became a thin FFI delegation to the Rust builder, so the via-obstacle-geometry leaf kernel stays single-source in temper-geometry (the E2 delegation boundary, VERIFICATION.md E2 section).
fixed_copper_zone_item_rect_py	[NEVER-WIRE-BY-DESIGN] wired from the design-bundle FixedCopperBuilder (Phase E batch E2, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (fixed_copper.py's _zone_item) became a thin FFI delegation to the Rust builder, so the zone-obstacle-bbox leaf kernel stays single-source in temper-geometry (the E2 delegation boundary, VERIFICATION.md E2 section).
hv_lv_area_check	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration hv_lv_partition_stage.rs (Phase D batch D7, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (hv_lv_partition.py's HvLvPartitionStage.run) became a thin FFI delegation to the Rust stage, so the per-bucket area-decision leaf kernel stays single-source in design-bundle (the D7 delegation boundary, VERIFICATION.md D7 section).
hv_lv_classify	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration hv_lv_partition_stage.rs (Phase D batch D7, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (hv_lv_partition.py's HvLvPartitionStage.run) became a thin FFI delegation to the Rust stage, so the safety-category classification + creepage-max leaf kernel stays single-source in design-bundle (the D7 delegation boundary, VERIFICATION.md D7 section).
hypergraph_coo_matvec_py	[NEVER-WIRE-BY-DESIGN] reached at runtime through a Rust-side getattr: temper-design-bundle hypergraph_contracts.rs Coo::__matmul__ does tg.getattr("hypergraph_coo_matvec_py") (the Phase-A U7 typed Coo boundary; hypergraph.py's Python call was collapsed into the Rust pyclass on 2026-08-10). The Python AST scan cannot see Rust getattr call sites, so this entry is a gate blind spot, not a real unwiring -- same shape as component_reach_py.
is_available_py	[NEVER-WIRE-BY-DESIGN] availability probe returning constant true, registered in BOTH temper-quality-oracle lib.rs:921 and temper-constraints lib.rs:427; only referenced by importability smoke tests (tests/rust_integration/test_rust_constraints.py:40, test_quality_oracle.py:30). Probe/export-only; no live caller shape.
is_signal_net_v6	[NEVER-WIRE-BY-DESIGN] exported probe from the net_classification io-types migration (merged 2026-08-08); net_classification.py has no signal-net helper call site (signal classification flows through NetClassSpec.classify_net in core/net_types.py) -- differential/export-only, no production caller.
is_via_position_valid	[NEVER-WIRE-BY-DESIGN] RESOLVED 2026-08-11: renamed probe (pyo3 name, via_placement.rs:106); router_v6/via_placement.py's actual algorithm (place_vias/_place_vias_for_path) never validates or clearance-checks a via position at all -- it either uses the pathfinder's explicit via_positions (already wired to via_layer_pair_py/adjacent_layer_py) or a legacy coordinate-midpoint fallback. There is no live Python position-validity check for this kernel to replace; "does its own thing" means a materially simpler algorithm, not a Python duplicate of this contract. No caller under either name (2026-08-08 unwired-kernel gate fix).
jlc04161h_7628	[ORPHANED-DELETE] 2026-08-12 spike/deletable-python: same stackup shim deletion as LayerConfig above (core/stackup.py removed in d8e60a532); production never passed it as a stackup (grep: zero src references to the name) -- test-only today. KEPT 2026-08-12 triage: differential-only (see header).
net_currents	[NEVER-WIRE-BY-DESIGN] RESOLVED 2026-08-11 (was: "needs triage" since PR #839). `net_currents()` (the whole-dict export, temper-drc-rs ipc_pyo3.rs:71) has zero production callers -- confirmed by direct search. Production IPC-2152 current lookups go through a *different*, already-wired per-net export: `core/ipc2152.py` re-exports `NET_CURRENTS` (the dict constant) and `get_net_current`, and `placer/cp_sat/gates.py:397` calls `get_net_current(net_name)` per net. Wiring the whole-dict function would mean inventing a caller for a shape (all currents at once) nothing needs; the per-net path is already served.
net_priority_key_py	[NEVER-WIRE-BY-DESIGN] PR #839 -- NetPriority is no longer constructed anywhere: order_nets delegates wholesale to order_nets_py and nothing else in this repo builds or compares a NetPriority. Wiring this comparison probe would mean inventing a caller, not serving one.
net_priority_lt_py	[NEVER-WIRE-BY-DESIGN] PR #839 -- same as net_priority_key_py: NetPriority has no real caller now that order_nets delegates wholesale.
normalize_rotation_index_py	[ORPHANED-DELETE] orphaned by the Phase-A U5 marshaler collapse (2026-08-10): the dict-building marshalers (_placement_to_board_dict) were their only callers; DrcBoardSnapshot.from_state builds the snapshot Rust-side. Differential-oracle-only today. KEPT 2026-08-12 triage: differential-only (see header).
numpy_pairwise_sum_py	[NEVER-WIRE-BY-DESIGN] explicit differential-only probe: its own docstring says it exists so the differential can pin np.sum bit-for-bit (quality-oracle lib.rs:648-651); only referenced by tests (test_quality_rust_differential.py:216, test_quality_rust_pbt.py:293). No live caller shape.
pad_radius_py	[NEVER-WIRE-BY-DESIGN] TRIAGE-CORRECTED 2026-08-12: the 2026-08-11 ORPHANED-DELETE tag was wrong -- this kernel IS wired at runtime, invisibly to the AST scan: temper-design-bundle geometry_types_contracts.rs `Pad.radius` does `tg.call_method1("pad_radius_py", (w, h))`, and the `Pad` pyclass is wired via core/geometry_types.py (`Pad = _tdb.geometry_contracts.Pad`). Cross-extension Rust call; do NOT delete.
parse_capacitance_rs	[NEVER-WIRE-BY-DESIGN] RESOLVED 2026-08-11: core/loop_extractor.py's Python `_parse_capacitance` is the deliberate Rust-independent fallback for `classify_component` -- `_classify_component_rust` tries `temper_rust_router.classify_component_rs` first ("R23: fallback", loop_extractor.py:93-97) and only falls through to the pure-Python body (which calls `_parse_capacitance`) on any Rust-side exception. Binding the fallback's capacitance parsing to a Rust kernel (even a different one, parse_capacitance_rs) would defeat the point of an independent safety net. Production capacitance-aware classification already flows through the Rust classify_component_rs kernel in the common case. No production caller (differential-only) by design, not by oversight.
pin_world_position_at_py	[NEVER-WIRE-BY-DESIGN] TRIAGE-CORRECTED 2026-08-12: the 2026-08-11 ORPHANED-DELETE tag was wrong -- this kernel IS wired at runtime, invisibly to the AST scan: temper-design-bundle model_builder.rs calls `PyModule::import("temper_geometry").getattr("pin_world_position_at_py")` at two sites (`pin_world_positions`, `create_layer_constraints`), and `ModelBuilder` is wired via router_v6/net_batching.py + router_v6/_pipeline_route.py. Cross-extension Rust call; do NOT delete.
place_via_with_clearance	[NEVER-WIRE-BY-DESIGN] RESOLVED 2026-08-11: renamed probe (via_placement.rs:123); same finding as is_via_position_valid above -- router_v6/via_placement.py's real algorithm has no clearance-aware via-placement step to replace (via positions come from the pathfinder or a legacy midpoint fallback). No caller under either name (2026-08-08 unwired-kernel gate fix).
placement_find_affected_py	[ORPHANED-DELETE] orphaned 2026-08-08 by retiring the dead router_v6/placement_suggestions.py shim; no production caller. Differential/export-only or delete. KEPT 2026-08-12 triage: differential-only (see header).
placement_suggested_position_py	[ORPHANED-DELETE] orphaned 2026-08-08 by retiring the dead placement_suggestions.py shim; no production caller. Differential/export-only or delete. KEPT 2026-08-12 triage: differential-only (see header).
placement_suggestions_generate_py	[ORPHANED-DELETE] orphaned 2026-08-08 by retiring the dead placement_suggestions.py shim; no production caller. Differential/export-only or delete. KEPT 2026-08-12 triage: differential-only (see header).
point_distance_py	[NEVER-WIRE-BY-DESIGN] TRIAGE-CORRECTED 2026-08-12: the 2026-08-11 ORPHANED-DELETE tag was wrong -- this kernel IS wired at runtime, invisibly to the AST scan: temper-design-bundle geometry_types_contracts.rs `Point.distance_to` does `tg.call_method1("point_distance_py", ...)`, and the `Point` pyclass is wired via core/geometry_types.py (`Point = _tdb.geometry_contracts.Point`). Cross-extension Rust call; do NOT delete. (The 2026-08-11 "superseded duplicate" reading missed the Rust-side caller.)
py_sum	[NEVER-WIRE-BY-DESIGN] RESOLVED 2026-08-11 (was: "needs triage" since PR #839). analysis/_area_sufficiency.py's docstring names `temper_geometry.py_sum` but the actual code (compute_area_sufficiency) calls the combined `_tg.area_sufficiency_compute` instead, which sums internally in Rust; py_sum is exposed purely so tests/analysis/test_area_sufficiency_{rust_differential,pbt}.py can pin CPython 3.12 `sum()`'s Neumaier-compensated semantics bit-for-bit against the internal computation. Same shape as numpy_pairwise_sum_py. No live caller shape.
recompute_plane_assignments	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration power_plane_stage.rs (Phase D batch D7, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (power_plane.py's PowerPlaneStage.run) became a thin FFI delegation to the Rust stage, so the three-pass plane reassignment leaf kernel stays single-source in design-bundle (the D7 delegation boundary, VERIFICATION.md D7 section).
record_loss	[NEVER-WIRE-BY-DESIGN] same family and same 2026-08-11 confirmation as check_routability_regression above -- ConvergenceChecker pyclass method from the Phase-1 orchestration migration (2026-08-09); pre-migration Python had no production caller either; exposed for the not-yet-built Phase-2 PipelineRunner; differential/PBT-only today.
required_clearance_py	[NEVER-WIRE-BY-DESIGN] RESOLVED 2026-08-11: router_v6/creepage_check.py's `_calculate_required_creepage` (the "Python twin" referenced below) is itself a one-line delegation to `_tg.calculate_required_creepage_py` (temper-geometry's creepage_check.rs), confirmed by reading creepage_check.py:446-479 -- production is NOT doing this in Python, it is fully wired already, just to a *different* Rust kernel than this one. quality-oracle lib.rs:544 IPC-2221 required-clearance kernel; only referenced by tests/rust_integration/test_quality_oracle.py:39-41,319 as a differential against router_v6/creepage_check.py's already-Rust-backed twin. Differential-only probe, superseded by the geometry-crate kernel that is actually wired.
scale_zone_bounds	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration zone_geometry_stage.rs (Phase D batch D2, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the deterministic leaf kernel stays single-source in design-bundle (the D2 delegation boundary, VERIFICATION.md D2 section).
slop_angle_between_py	[ORPHANED-DELETE] PR #750 quality-metrics cluster -- metrics/slop_linter.py's public lint_* functions now delegate straight to their aggregate lint kernels (2026-08-06 wiring), which resolve tracing/geometry internally in Rust; the matching private scalar helper this symbol replaced had no caller outside those now-replaced bodies and was removed rather than left dead. KEPT 2026-08-12 triage: differential-only (see header).
slop_distance_mm_py	[ORPHANED-DELETE] PR #750 quality-metrics cluster -- metrics/slop_linter.py's public lint_* functions now delegate straight to their aggregate lint kernels (2026-08-06 wiring), which resolve tracing/geometry internally in Rust; the matching private scalar helper this symbol replaced had no caller outside those now-replaced bodies and was removed rather than left dead. KEPT 2026-08-12 triage: differential-only (see header).
slop_load_traces_by_net_py	[ORPHANED-DELETE] PR #750 quality-metrics cluster -- metrics/slop_linter.py's public lint_* functions now delegate straight to their aggregate lint kernels (2026-08-06 wiring), which resolve tracing/geometry internally in Rust; the matching private scalar helper this symbol replaced had no caller outside those now-replaced bodies and was removed rather than left dead. KEPT 2026-08-12 triage: differential-only (see header).
slop_order_traces_py	[ORPHANED-DELETE] PR #750 quality-metrics cluster -- metrics/slop_linter.py's public lint_* functions now delegate straight to their aggregate lint kernels (2026-08-06 wiring), which resolve tracing/geometry internally in Rust; the matching private scalar helper this symbol replaced had no caller outside those now-replaced bodies and was removed rather than left dead. KEPT 2026-08-12 triage: differential-only (see header).
slop_vector_py	[ORPHANED-DELETE] PR #750 quality-metrics cluster -- metrics/slop_linter.py's public lint_* functions now delegate straight to their aggregate lint kernels (2026-08-06 wiring), which resolve tracing/geometry internally in Rust; the matching private scalar helper this symbol replaced had no caller outside those now-replaced bodies and was removed rather than left dead. KEPT 2026-08-12 triage: differential-only (see header).
summarize_violations_py	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration drc_validation_stage.rs (Phase D batch D6, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (drc_validation.py's _log_summary) became a thin FFI delegation to the Rust stage, so the count-by-type leaf kernel stays single-source in temper-drc-rs (the D6 delegation boundary, VERIFICATION.md D6 section).
threshold_decision_py	[NEVER-WIRE-BY-DESIGN] wired from temper-orchestration drc_validation_stage.rs (Phase D batch D6, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (drc_validation.py's run threshold decision) became a thin FFI delegation to the Rust stage, so the raise-decision leaf kernel stays single-source in temper-drc-rs (the D6 delegation boundary, VERIFICATION.md D6 section).
tokenize	[NEVER-WIRE-BY-DESIGN] RESOLVED 2026-08-11 (was: "needs triage" since PR #839). parse_engine.rs:3096-3097 documents itself as a "Test/conformance surface: tokenize `content` with the kiutils-exact tokenizer" for conformance-testing against kiutils.utils.sexpr.parse_sexp on adversarial inputs -- deliberately not a production entry point. Consistent with the R3/#688 precedent cited in core/netlist.py's docstring: "keep yaml.safe_load on the Python side rather than re-tokenize." No production caller. Hidden by the old text scan: the only production mention is the word 're-tokenize' in core/netlist.py's docstring, and the word-boundary regex treated the hyphen as a boundary.
track_midpoint_py	[NEVER-WIRE-BY-DESIGN] TRIAGE-CORRECTED 2026-08-12: the 2026-08-11 ORPHANED-DELETE tag was wrong -- this kernel IS wired at runtime, invisibly to the AST scan: temper-design-bundle geometry_types_contracts.rs `Track.midpoint` does `tg.call_method1("track_midpoint_py", ...)`, and the `Track` pyclass is wired via core/geometry_types.py (`Track = _tdb.geometry_contracts.Track`); production router_v6/constraints_spatial_index.py:233 calls `t.midpoint()`. Cross-extension Rust call; do NOT delete.
version_py	[ORPHANED-DELETE] orphaned 2026-08-09 by retiring the dead pcl/rust_bridge.py wrapper: its `_call_rust("version_py")` dispatch was the only production reference to this symbol, and the tracked copy is quality-oracle lib.rs:880 (temper-constraints' own version_py at lib.rs:410 is outside this gate's packages/*/src glob and untrackable). Probe/export-only; no live caller shape remains. KEPT 2026-08-12 triage: differential-only (see header).
via_count_get_board_bbox_py	[NEVER-WIRE-BY-DESIGN] RESOLVED 2026-08-11: router_v6/quality/via_count.py's own module docstring documents the decision explicitly -- `classify_vias_from_parse` (the one real production caller, used by validation/human_reference_extractor.py) is handed an already-parsed `ParseResult`, and the kernel boundary (`temper_quality_oracle::cluster_f::bindings::build_view`) accepts only a scenario dict or a `.kicad_pcb` path, with no wire format for a pre-parsed object. Wiring would either break that caller (TypeError) or require hand-marshalling `ParseResult` into a scenario dict, duplicating an unused Rust-side conversion path -- exactly the kind of behavioural/architectural decision this triage does not make unilaterally. classify_vias_from_parse and this symbol's Python counterpart (_get_board_bbox) are left unwired together (2026-08-06 wiring); via_count_classify_vias_py itself IS wired, via the path-taking top-level functions.
via_count_get_component_bboxes_py	[NEVER-WIRE-BY-DESIGN] same ParseResult-vs-path wire-format gap as via_count_get_board_bbox_py above (2026-08-06 wiring); left unwired alongside classify_vias_from_parse -- see that entry for the full 2026-08-11 resolution.
via_count_is_via_in_bbox_py	[NEVER-WIRE-BY-DESIGN] same ParseResult-vs-path wire-format gap as via_count_get_board_bbox_py above (2026-08-06 wiring); left unwired alongside classify_vias_from_parse -- see that entry for the full 2026-08-11 resolution.
via_count_is_via_near_board_edge_py	[NEVER-WIRE-BY-DESIGN] same ParseResult-vs-path wire-format gap as via_count_get_board_bbox_py above (2026-08-06 wiring); left unwired alongside classify_vias_from_parse -- see that entry for the full 2026-08-11 resolution.
via_distance	[NEVER-WIRE-BY-DESIGN] RESOLVED 2026-08-11: renamed probe (via_placement.rs:98); same finding as is_via_position_valid/place_via_with_clearance above -- router_v6/via_placement.py never computes an inline via-to-via or via-to-obstacle distance at all in its current algorithm. No caller under either name -- via distance computed in-line by the router (2026-08-08 unwired-kernel gate fix).
PySafetyValue	[WIRE] 2026-08-15 feat/safety-value-provenance-types: return type of the new safety_value.rs lookups (creepage_table_lookup / clearance_table_lookup), built ahead of the creepage/clearance migration (docs/evidence/2026-08-15-creepage-base-14-verification.md §5: replace the VoltageClass creepage path with the recovered-table lookup). The migration is a deliberate follow-up task -- the dispatch explicitly says "DO NOT replace the existing 14.0 in net_types.rs yet". Production Python (net_types.py get_creepage_mm, isolation_constants.py MIN_BARRIER_WIDTH_MM) still computes these values today; repointing it onto this lookup is the WIRE step. Until then the surface is exercised by 13 unit tests + the wasm registry, not by production callers.
clearance_table_lookup	[WIRE] 2026-08-15 feat/safety-value-provenance-types: recovered Table 16 clearance lookup, built ahead of the migration that will repoint validators/clearance.py and test_clearance_boundary.py's five-standard engine onto it (see PySafetyValue entry for the same dispatch-scoped reason). No production caller today.
creepage_table_lookup	[WIRE] 2026-08-15 feat/safety-value-provenance-types: recovered Table 17/18 creepage lookup keyed on (working voltage, PD, material group) -- the structurally correct replacement for VoltageClass.get_creepage_mm's fabricated base x {0.8,1.0,1.4} model (docs/evidence/2026-08-15-creepage-base-14-verification.md §4-5). Migration deliberately deferred by the dispatch; no production caller today.
legacy_creepage_base_high_voltage_14_0	[WIRE] 2026-08-15 feat/safety-value-provenance-types: Fabricated-labeled marker for the 14.0 mm HIGH_VOLTAGE base (origin commit 418fab757). The migration task that replaces net_types.rs's 14.0 must assert is_fabricated() on it; until that lands it has no production caller by design -- production must NOT use a fabricated value.
