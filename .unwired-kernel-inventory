# Registered Rust kernels with no production caller.
#
# Shrink-only. A new entry is a hard failure -- wire the kernel or add it
# here WITH A REASON. An entry that becomes wired is also a failure, so
# paid-down debt shows up in a diff instead of rotting on the books.
#
# Generated by scripts/check_unwired_kernels.py --write-inventory
# <symbol>\t<reason>

DiffPairConfig	SURFACED by PR #839's AST rescan of this gate -- the previous text scan matched the symbol name in comments and docstrings and reported it wired. Genuinely has no production caller. Reason for being unwired NOT yet established; needs triage (deliberate omission vs a migration that stopped at 'the differential is green').
DrcComponentSnapshot	Phase-A U5 (2026-08-10) typed sub-snapshot of DrcBoardSnapshot (drc_marshal.rs); production shims (drc_runner.py/drc_oracle.py) consume DrcBoardSnapshot.to_dict()/run_drc and never name the sub-snapshot types directly -- exposed for typed introspection + PBT.
DrcNetClassRuleSnapshot	Phase-A U5 (2026-08-10) typed sub-snapshot of DrcBoardSnapshot's net_class_rules; same production-consumption shape as DrcComponentSnapshot -- introspection/PBT surface only.
DrcTraceSnapshot	Phase-A U5 (2026-08-10) typed sub-snapshot of DrcBoardSnapshot's traces; introspection/PBT surface only.
DrcViaSnapshot	Phase-A U5 (2026-08-10) typed sub-snapshot of DrcBoardSnapshot's vias; introspection/PBT surface only.
DrillDefinition	SURFACED by PR #839's AST rescan of this gate -- the previous text scan matched the symbol name in comments and docstrings and reported it wired. Genuinely has no production caller. Reason for being unwired NOT yet established; needs triage (deliberate omission vs a migration that stopped at 'the differential is green').
ExtractedLoopWire	Phase-A U9 (2026-08-10) typed per-loop wire element of LoopExtractionOutput (loop_extraction_contracts.rs); production shims consume it anonymously field-by-field via LoopExtractionOutput.loops[...] (core/loop_extractor_rs.py reads .name/.loop_type/.components/.nets/.max_area_mm2), so no Python file ever NAMES the type -- exposed for typed introspection + PBT, same consumption shape as the U5 Drc*Snapshot entries.
HypergraphBuildResult	pyclass return type of HypergraphFactory.build() (design-bundle hypergraph_factory.rs:84); consumed anonymously field-by-field in extraction/hypergraph_factory.py:95-131 (parts.node_refs etc.) so no Python file ever NAMES the type -- the AST scan cannot see it. The consuming shim module itself has no production importer (only a docstring mention in core/hypergraph.py:24; all real callers are tests), so the whole chain is effectively test-driven.
PyCompiler	stateful pyclass wrapper (constraint-compiler lib.rs:47) with zero references in any Python, prod or test; production wires the stateless compile_pcl_constraints function instead (router_v6/_pipeline_route.py:74,129). Superseded class API; delete candidate.
SkipExpr	SURFACED 2026-08-06 by the path-qualified registration fix. Registered as `wrap_pyfunction!(<module>::<fn>)`, so the old regex recorded the Rust MODULE name and never tracked this symbol at all -- it has been unwatched, not wired. Zero mentions in any production Python. Needs triage: deliberate omission vs a migration that stopped at 'the differential is green'.
apply_damped_position_py	orphaned 2026-08-08 by retiring the dead router_v6/apply_suggestions.py shim (zero production callers); the kernel regenerates positions internally from congestion regions -- no live caller shape exists. Differential/export-only or delete.
apply_suggestions_damped_py	SURFACED by PR #839's AST rescan of this gate -- the previous text scan matched the symbol name in comments and docstrings and reported it wired. Genuinely has no production caller. Reason for being unwired NOT yet established; needs triage (deliberate omission vs a migration that stopped at 'the differential is green').
apply_total_movement_py	orphaned 2026-08-08 by retiring the dead apply_suggestions.py shim; same fresh-state contract gap as apply_damped_position_py. Differential/export-only or delete.
apply_update_positions_py	orphaned 2026-08-08 by retiring the dead apply_suggestions.py shim; no production caller. Differential/export-only or delete.
assign_component_zones	wired from temper-orchestration zone_assignment_stage.rs (Phase D batch D2, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the deterministic leaf kernel stays single-source in design-bundle (the D2 delegation boundary, VERIFICATION.md D2 section).
assign_components_to_slots	wired from temper-orchestration component_assignment_stage.rs (Phase D batch D4, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (component_assignment.py's _assign_components_to_slots) became a thin FFI delegation to the Rust stage, so the leaf kernel stays single-source in design-bundle (the D4 delegation boundary, VERIFICATION.md D4 section).
build_adjacency_flat	flat (n, f32[]) probe in temper-io-types placer_core/pybridge.rs:933 (io-types adjacency.rs is itself a port of core.netlist.build_adjacency_matrix); the production kernel is temper_design_bundle_python.netlist_contracts.build_adjacency_matrix, wired at core/netlist.py:59. Superseded by the design-bundle kernel; delete candidate.
build_board_dict_from_parsed_pcb_py	Phase-A U5 (2026-08-10) legacy K1-dict kernel superseded by DrcBoardSnapshot.from_parsed_pcb; retained (unchanged) for the existing test_drc_oracle_marshal_rust_differential.py suite and any external dict caller -- production now goes through the typed constructor.
build_board_dict_py	Phase-A U5 (2026-08-10) legacy K1-dict kernel superseded by DrcBoardSnapshot.from_netlist; retained (unchanged) for the existing test_drc_oracle_marshal_rust_differential.py suite -- production now goes through the typed constructor.
build_constraints_dict_py	Phase-A U5 (2026-08-10) legacy K1-dict kernel superseded by TypedConstraintSet.from_context; retained (unchanged) for the existing test_drc_oracle_marshal_rust_differential.py suite -- production now goes through the typed constructor.
check_routability_regression	ConvergenceChecker pyclass method from the Phase-1 orchestration migration (2026-08-09); pre-migration Python had no production caller either (only docstring + tests) -- exposed for the Phase-2 PipelineRunner; differential/PBT-only today.
check_success	ConvergenceChecker pyclass method from the Phase-1 orchestration migration (2026-08-09); pre-migration Python had no production caller either (only docstring + tests) -- exposed for the Phase-2 PipelineRunner; differential/PBT-only today.
classify_net_type_v6	exported probe from the net_classification io-types migration (merged 2026-08-08); router_v6/net_classification.py calls the specific helpers (_rs.is_ground_net / is_power_net_v6 / is_hv_net) and never the combined classifier -- wiring it would mean inventing a caller, not serving one.
classify_nets_py	quality-oracle lib.rs:530 batch netlist classifier; only caller is tests/rust_integration/test_quality_oracle.py:57. Production net classification flows through router_v6/net_classification.py predicates wired to temper_io_types (is_ground_net/is_power_net_v6/is_hv_net -- see ledger's classify_net_type_v6/is_signal_net_v6). Differential-only probe superseded by the io-types predicates.
closest_component_for_zone_py	wired from temper-orchestration grid_hv.rs (Phase D batch D3, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the HV spatial-fallback leaf kernel stays single-source in temper-geometry (the D3 delegation boundary, VERIFICATION.md D3 section).
component_reach_py	reached at runtime through a Rust-side getattr: temper-drc-rs req_safe_01.rs:200 (getattr("component_reach_py")) from req_safe_01_copper_model_init (req_safe_01.rs:324), which production _copper.py:132 (real_board.py:601 -> _CopperModel) calls. The Python AST scan cannot see Rust getattr call sites, so this entry is a gate blind spot, not a real unwiring; the differential oracle (tests/requirements/clearance_oracle/_copper.py:169) also calls it directly.
congestion_analyze_py	DELIBERATE, documented in router_v6/congestion.py: the kernel's contract (see congestion_analysis.rs) binds positions=None/layer_assignments=None always, resolves every net on layer 0, and assumes a (0.0, 0.0) board origin -- while every production caller (metrics/physics.py, router_v6/verifier.py) passes positions= and/or layer_assignments= and real boards may carry a non-zero origin. The kernel's own bottom-side mirror is present (#832); wiring it to analyze_congestion would silently mis-place demand on any board using the excluded shapes. Its per-net demand accumulation is wired through congestion_estimate_net_demand_py instead.
congestion_heatmap_at_py	SURFACED by PR #839's AST rescan of this gate -- the previous text scan matched the symbol name in comments and docstrings and reported it wired. Genuinely has no production caller. Reason for being unwired NOT yet established; needs triage (deliberate omission vs a migration that stopped at 'the differential is green').
congestion_heatmap_from_router_py	orphaned 2026-08-08 by retiring the dead router_v6/congestion_heatmap.py shim (zero importers); kernel rebuilds the grid from raw fields the dataclass doesn't hold. Differential/export-only or delete.
congestion_heatmap_hotspots_py	SURFACED by PR #839's AST rescan of this gate -- the previous text scan matched the symbol name in comments and docstrings and reported it wired. Genuinely has no production caller. Reason for being unwired NOT yet established; needs triage (deliberate omission vs a migration that stopped at 'the differential is green').
congestion_heatmap_total_py	SURFACED by PR #839's AST rescan of this gate -- the previous text scan matched the symbol name in comments and docstrings and reported it wired. Genuinely has no production caller. Reason for being unwired NOT yet established; needs triage (deliberate omission vs a migration that stopped at 'the differential is green').
connectivity_validate_net_py	wired from temper-orchestration connectivity_validation_stage.rs (Phase D batch D6, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (connectivity_validation.py's _validate_net_connectivity) became a thin FFI delegation to the Rust stage, so the UnionFind leaf kernel stays single-source in temper-drc-rs (the D6 delegation boundary, VERIFICATION.md D6 section).
constraint_value_to_plain_py	Phase-A U5 (2026-08-10) legacy kernel superseded by ConstraintValue.from_python; retained (unchanged) for the existing test_drc_oracle_marshal_rust_differential.py suite -- production now goes through the typed constructor.
count_connected_layers_py	wired from temper-orchestration via_validation_stage.rs (Phase D batch D6, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (via_validation.py's _count_connected_layers) became a thin FFI delegation to the Rust stage, so the via-connectivity leaf kernel stays single-source in temper-drc-rs (the D6 delegation boundary, VERIFICATION.md D6 section).
dedup_via_positions_py	wired from temper-orchestration via_validation_stage.rs (Phase D batch D6, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (via_validation.py's ViaDeduplicationStage.run) became a thin FFI delegation to the Rust stage, so the via-position-dedup leaf kernel stays single-source in temper-drc-rs (the D6 delegation boundary, VERIFICATION.md D6 section).
deduplicate_traces_py	wired from temper-orchestration drc_sweep_stage.rs (Phase D batch D6, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (drc_sweep.py's TrackDeduplicationStage.run) became a thin FFI delegation to the Rust stage, so the track-dedup leaf kernel stays single-source in temper-drc-rs (the D6 delegation boundary, VERIFICATION.md D6 section).
define_zone_layout	wired from temper-orchestration zone_geometry_stage.rs (Phase D batch D2, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the deterministic leaf kernel stays single-source in design-bundle (the D2 delegation boundary, VERIFICATION.md D2 section).
dist_py	wired from temper-orchestration clearance.rs (Phase E batch E3, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (domain_clearance.py's audit_domain_clearance) became a thin FFI delegation to the Rust audit stage, so the Dekker-vector_norm Euclidean-distance leaf kernel stays single-source in temper-geometry (the E3 delegation boundary, VERIFICATION.md E3 section).
dfm_adjacent_layer_py	PR #749 DFM cluster -- Rust proven, shim not written
dfm_board_bounds_py	PR #749 DFM cluster -- Rust proven, shim not written
dfm_power_pour_bounds_py	PR #749 DFM cluster -- Rust proven, shim not written
dfm_rect_polygon_py	PR #749 DFM cluster -- Rust proven, shim not written
dfm_thermal_via_positions_py	PR #749 DFM cluster -- Rust proven, shim not written
dfm_via_segment_index_py	PR #749 DFM cluster -- Rust proven, shim not written
escape_is_position_valid_py	PR #839 -- #829's kernel defect is fixed, but the signature is a differential-only probe (implicit identity component transform, no rotation/side); generate_escape_vias delegates wholesale so the real _is_position_valid became dead code and was removed. No real caller shape can use this probe contract.
evaluate_quality_py	SURFACED by PR #839's AST rescan of this gate -- the previous text scan matched the symbol name in comments and docstrings and reported it wired. Genuinely has no production caller. Reason for being unwired NOT yet established; needs triage (deliberate omission vs a migration that stopped at 'the differential is green').
explain_decision_trace_history	wired from temper-orchestration explainability.rs (Phase-A U8) DecisionTrace.history via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; single-source NL-generation kernel kept in io-types (the U8 delegation boundary, VERIFICATION.md U8 section).
explain_decision_trace_summary	wired from temper-orchestration explainability.rs (Phase-A U8) DecisionTrace.summary via runtime PyModule::import -- invisible to the Python AST scan; single-source aggregation kernel kept in io-types (VERIFICATION.md U8 section).
explain_decision_trace_why	wired from temper-orchestration explainability.rs (Phase-A U8) DecisionTrace.why via runtime PyModule::import -- invisible to the Python AST scan; single-source NL-generation kernel kept in io-types (VERIFICATION.md U8 section).
explain_decision_trace_why_not	wired from temper-orchestration explainability.rs (Phase-A U8) DecisionTrace.why_not via runtime PyModule::import -- invisible to the Python AST scan; single-source NL-generation kernel kept in io-types (VERIFICATION.md U8 section).
explain_render_component_report	orphaned 2026-08-10 by Phase-A U8 -- the MarkdownReport renderers were ported to temper-orchestration explainability.rs (render_component_report/render_markdown_report, byte-pinned by the explainability differentials); io-types renderers superseded, retained for the existing test_markdown_rust_differential.py hasattr arm.
explain_render_markdown_report	orphaned 2026-08-10 by Phase-A U8 -- the MarkdownReport renderers were ported to temper-orchestration explainability.rs (render_component_report/render_markdown_report, byte-pinned by the explainability differentials); io-types renderers superseded, retained for the existing test_markdown_rust_differential.py hasattr arm.
explain_trace_why	wired from temper-orchestration explainability.rs (Phase-A U8) Trace.why via runtime PyModule::import -- invisible to the Python AST scan; single-source NL-generation kernel kept in io-types (VERIFICATION.md U8 section).
extract_config_refs_py	SURFACED 2026-08-06 by the path-qualified registration fix. Registered as `wrap_pyfunction!(<module>::<fn>)`, so the old regex recorded the Rust MODULE name and never tracked this symbol at all -- it has been unwatched, not wired. Zero mentions in any production Python. Needs triage: deliberate omission vs a migration that stopped at 'the differential is green'.
extract_medial_axis_py	Exposed for the differential only. Production calls the SINGLE form extract_medial_axis_single_py (channel_skeleton.py:262); the batch form is invoked internally in Rust across the polygon list. Wiring it would mean inventing a Python caller, not serving one.
fence_samples_py	wired from temper-orchestration grid_fence.rs (Phase D batch D3, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the U3 fence-sample leaf kernel stays single-source in temper-geometry (the D3 delegation boundary, VERIFICATION.md D3 section).
fixed_copper_local_pad_half_py	wired from the design-bundle FixedCopperBuilder (Phase E batch E2, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (fixed_copper.py's _local_pad_half) became a thin FFI delegation to the Rust builder, so the pad half-extent leaf kernel stays single-source in temper-geometry (the E2 delegation boundary, VERIFICATION.md E2 section).
fixed_copper_other_pad_item_geom_py	wired from the design-bundle FixedCopperBuilder (Phase E batch E2, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (fixed_copper.py's _other_component_pad_item) became a thin FFI delegation to the Rust builder, so the pinned-pad obstacle-geometry leaf kernel stays single-source in temper-geometry (the E2 delegation boundary, VERIFICATION.md E2 section).
fixed_copper_pin_copper_layers_py	wired from the design-bundle FixedCopperBuilder (Phase E batch E2, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python callers (fixed_copper.py's _pin_copper_layers) became a thin FFI delegation to the Rust builder, so the pin-copper-layer leaf kernel stays single-source in temper-geometry (the E2 delegation boundary, VERIFICATION.md E2 section).
fixed_copper_point_rect_distance_py	orphaned 2026-08-10 by Phase E batch E2 (plan 2026-08-09-001): the fixed_copper.py shim dropped the dead `_point_rect_distance` wrapper one-liners (the pre-migration exact_clearance_mm had already migrated to the combined fixed_copper_exact_clearance_mm_py dispatch, leaving these wrappers unreferenced); no live caller shape remains -- differential/export-only or delete.
fixed_copper_point_segment_distance_py	orphaned 2026-08-10 by Phase E batch E2 (plan 2026-08-09-001): the fixed_copper.py shim dropped the dead `_point_segment_distance` wrapper one-liners (the pre-migration exact_clearance_mm had already migrated to the combined fixed_copper_exact_clearance_mm_py dispatch, leaving these wrappers unreferenced); no live caller shape remains -- differential/export-only or delete.
fixed_copper_rect_rect_gap_py	orphaned 2026-08-10 by Phase E batch E2 (plan 2026-08-09-001): the fixed_copper.py shim dropped the dead `_rect_rect_gap` wrapper one-liners (the pre-migration exact_clearance_mm had already migrated to the combined fixed_copper_exact_clearance_mm_py dispatch, leaving these wrappers unreferenced); no live caller shape remains -- differential/export-only or delete.
fixed_copper_rect_segment_distance_py	orphaned 2026-08-10 by Phase E batch E2 (plan 2026-08-09-001): the fixed_copper.py shim dropped the dead `_rect_segment_distance` wrapper one-liners (the pre-migration exact_clearance_mm had already migrated to the combined fixed_copper_exact_clearance_mm_py dispatch, leaving these wrappers unreferenced); no live caller shape remains -- differential/export-only or delete.
fixed_copper_segment_item_geom_py	wired from the design-bundle FixedCopperBuilder (Phase E batch E2, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (fixed_copper.py's _segment_item) became a thin FFI delegation to the Rust builder, so the trace-obstacle-geometry leaf kernel stays single-source in temper-geometry (the E2 delegation boundary, VERIFICATION.md E2 section).
fixed_copper_segments_intersect_py	orphaned 2026-08-10 by Phase E batch E2 (plan 2026-08-09-001): the fixed_copper.py shim dropped the dead `_segments_intersect` wrapper one-liners (the pre-migration exact_clearance_mm had already migrated to the combined fixed_copper_exact_clearance_mm_py dispatch, leaving these wrappers unreferenced); no live caller shape remains -- differential/export-only or delete.
fixed_copper_via_item_geom_py	wired from the design-bundle FixedCopperBuilder (Phase E batch E2, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (fixed_copper.py's _via_item) became a thin FFI delegation to the Rust builder, so the via-obstacle-geometry leaf kernel stays single-source in temper-geometry (the E2 delegation boundary, VERIFICATION.md E2 section).
fixed_copper_zone_item_rect_py	wired from the design-bundle FixedCopperBuilder (Phase E batch E2, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (fixed_copper.py's _zone_item) became a thin FFI delegation to the Rust builder, so the zone-obstacle-bbox leaf kernel stays single-source in temper-geometry (the E2 delegation boundary, VERIFICATION.md E2 section).
hv_lv_area_check	wired from temper-orchestration hv_lv_partition_stage.rs (Phase D batch D7, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (hv_lv_partition.py's HvLvPartitionStage.run) became a thin FFI delegation to the Rust stage, so the per-bucket area-decision leaf kernel stays single-source in design-bundle (the D7 delegation boundary, VERIFICATION.md D7 section).
hv_lv_classify	wired from temper-orchestration hv_lv_partition_stage.rs (Phase D batch D7, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (hv_lv_partition.py's HvLvPartitionStage.run) became a thin FFI delegation to the Rust stage, so the safety-category classification + creepage-max leaf kernel stays single-source in design-bundle (the D7 delegation boundary, VERIFICATION.md D7 section).
hypergraph_coo_matvec_py	reached at runtime through a Rust-side getattr: temper-design-bundle hypergraph_contracts.rs Coo::__matmul__ does tg.getattr("hypergraph_coo_matvec_py") (the Phase-A U7 typed Coo boundary; hypergraph.py's Python call was collapsed into the Rust pyclass on 2026-08-10). The Python AST scan cannot see Rust getattr call sites, so this entry is a gate blind spot, not a real unwiring -- same shape as component_reach_py.
is_available_py	availability probe returning constant true, registered in BOTH temper-quality-oracle lib.rs:921 and temper-constraints lib.rs:427; only referenced by importability smoke tests (tests/rust_integration/test_rust_constraints.py:40, test_quality_oracle.py:30). Probe/export-only; no live caller shape.
is_signal_net_v6	exported probe from the net_classification io-types migration (merged 2026-08-08); net_classification.py has no signal-net helper call site (signal classification flows through NetClassSpec.classify_net in core/net_types.py) -- differential/export-only, no production caller.
is_via_position_valid	renamed probe (pyo3 name, via_placement.rs:106); router_v6/via_placement.py does its own thing -- no caller under either name (2026-08-08 unwired-kernel gate fix)
net_currents	SURFACED by PR #839's AST rescan of this gate -- the previous text scan matched the symbol name in comments and docstrings and reported it wired. Genuinely has no production caller. Reason for being unwired NOT yet established; needs triage (deliberate omission vs a migration that stopped at 'the differential is green').
net_priority_key_py	PR #839 -- NetPriority is no longer constructed anywhere: order_nets delegates wholesale to order_nets_py and nothing else in this repo builds or compares a NetPriority. Wiring this comparison probe would mean inventing a caller, not serving one.
net_priority_lt_py	PR #839 -- same as net_priority_key_py: NetPriority has no real caller now that order_nets delegates wholesale.
normalize_rotation_index_py	orphaned by the Phase-A U5 marshaler collapse (2026-08-10): the dict-building marshalers (_placement_to_board_dict) were their only callers; DrcBoardSnapshot.from_state builds the snapshot Rust-side. Differential-oracle-only today.
normalized_bundle_json	one-shot full-pipeline convenience export (design-bundle lib.rs:201) from the initial design-bundle boundary commit (41e51114); zero references in any Python, prod or test. Production reaches the bundle via modular pieces instead (preflight_identity at io/design_bundle_preflight.py:66 and the parse_engine submodule used by io/_parse_board.py:29). Superseded convenience; delete candidate.
numpy_pairwise_sum_py	explicit differential-only probe: its own docstring says it exists so the differential can pin np.sum bit-for-bit (quality-oracle lib.rs:648-651); only referenced by tests (test_quality_rust_differential.py:216, test_quality_rust_pbt.py:293). No live caller shape.
pad_radius_py	orphaned by the Phase-A U5 marshaler collapse (2026-08-10): only used by the retired dict builder; from_state path computes radii Rust-side. Differential-oracle-only today.
parse_capacitance_rs	exported probe from the loop-extractor classify migration (2026-08-09); the Python loop_extractor shim routes capacitance parsing through a different path -- no production caller (differential-only).
pin_world_position_at_py	orphaned by the Phase-A U5 marshaler collapse (2026-08-10): only used by the retired dict builder; from_state resolves positions Rust-side. Differential-oracle-only today.
place_via_with_clearance	renamed probe (via_placement.rs:123); router_v6/via_placement.py does its own thing -- no caller under either name (2026-08-08 unwired-kernel gate fix)
placement_find_affected_py	orphaned 2026-08-08 by retiring the dead router_v6/placement_suggestions.py shim; no production caller. Differential/export-only or delete.
placement_suggested_position_py	orphaned 2026-08-08 by retiring the dead placement_suggestions.py shim; no production caller. Differential/export-only or delete.
placement_suggestions_generate_py	orphaned 2026-08-08 by retiring the dead placement_suggestions.py shim; no production caller. Differential/export-only or delete.
point_distance_py	unwired at time of recording; no reason given
points_centroid_winding	duplicate of polygon_centroid -- polygon.rs:115-117 shows points_centroid_winding just calls polygon_centroid (same shoelace formula). Production wires polygon_centroid (geometry/polygon.py:74) and points_centroid (primitives.py:68); zero Python references to this winding alias. Superseded duplicate; delete candidate.
py_sum	SURFACED by PR #839's AST rescan of this gate -- the previous text scan matched the symbol name in comments and docstrings and reported it wired. Genuinely has no production caller. Reason for being unwired NOT yet established; needs triage (deliberate omission vs a migration that stopped at 'the differential is green').
recompute_plane_assignments	wired from temper-orchestration power_plane_stage.rs (Phase D batch D7, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (power_plane.py's PowerPlaneStage.run) became a thin FFI delegation to the Rust stage, so the three-pass plane reassignment leaf kernel stays single-source in design-bundle (the D7 delegation boundary, VERIFICATION.md D7 section).
record_loss	ConvergenceChecker pyclass method from the Phase-1 orchestration migration (2026-08-09); pre-migration Python had no production caller either (only docstring + tests) -- exposed for the Phase-2 PipelineRunner; differential/PBT-only today.
required_clearance_py	quality-oracle lib.rs:544 IPC-2221 required-clearance kernel; only referenced by tests/rust_integration/test_quality_oracle.py:39-41,319 as a differential against Python creepage_check._calculate_required_creepage (creepage_check.py:443). Production creepage flows through that Python twin (clearance_engine.py:266) and the temper-drc-rs kernels, never this export. Differential-only probe.
scale_zone_bounds	wired from temper-orchestration zone_geometry_stage.rs (Phase D batch D2, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the deterministic leaf kernel stays single-source in design-bundle (the D2 delegation boundary, VERIFICATION.md D2 section).
slop_angle_between_py	PR #750 quality-metrics cluster -- metrics/slop_linter.py's public lint_* functions now delegate straight to their aggregate lint kernels (2026-08-06 wiring), which resolve tracing/geometry internally in Rust; the matching private scalar helper this symbol replaced had no caller outside those now-replaced bodies and was removed rather than left dead.
slop_distance_mm_py	PR #750 quality-metrics cluster -- metrics/slop_linter.py's public lint_* functions now delegate straight to their aggregate lint kernels (2026-08-06 wiring), which resolve tracing/geometry internally in Rust; the matching private scalar helper this symbol replaced had no caller outside those now-replaced bodies and was removed rather than left dead.
slop_load_traces_by_net_py	PR #750 quality-metrics cluster -- metrics/slop_linter.py's public lint_* functions now delegate straight to their aggregate lint kernels (2026-08-06 wiring), which resolve tracing/geometry internally in Rust; the matching private scalar helper this symbol replaced had no caller outside those now-replaced bodies and was removed rather than left dead.
slop_order_traces_py	PR #750 quality-metrics cluster -- metrics/slop_linter.py's public lint_* functions now delegate straight to their aggregate lint kernels (2026-08-06 wiring), which resolve tracing/geometry internally in Rust; the matching private scalar helper this symbol replaced had no caller outside those now-replaced bodies and was removed rather than left dead.
slop_vector_py	PR #750 quality-metrics cluster -- metrics/slop_linter.py's public lint_* functions now delegate straight to their aggregate lint kernels (2026-08-06 wiring), which resolve tracing/geometry internally in Rust; the matching private scalar helper this symbol replaced had no caller outside those now-replaced bodies and was removed rather than left dead.
summarize_violations_py	wired from temper-orchestration drc_validation_stage.rs (Phase D batch D6, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (drc_validation.py's _log_summary) became a thin FFI delegation to the Rust stage, so the count-by-type leaf kernel stays single-source in temper-drc-rs (the D6 delegation boundary, VERIFICATION.md D6 section).
threshold_decision_py	wired from temper-orchestration drc_validation_stage.rs (Phase D batch D6, plan 2026-08-09-001) via runtime PyModule::import -- the Python AST scan cannot see cross-extension Rust-to-Python calls; the pre-migration Python caller (drc_validation.py's run threshold decision) became a thin FFI delegation to the Rust stage, so the raise-decision leaf kernel stays single-source in temper-drc-rs (the D6 delegation boundary, VERIFICATION.md D6 section).
tokenize	PR #839 -- no production caller. Hidden by the old text scan: the only production mention is the word 're-tokenize' in core/netlist.py's docstring, and the word-boundary regex treated the hyphen as a boundary.
track_midpoint_py	orphaned by the Phase-A U5 marshaler collapse (2026-08-10): only used by the retired dict builder. Differential-oracle-only today.
version_py	orphaned 2026-08-09 by retiring the dead pcl/rust_bridge.py wrapper: its `_call_rust("version_py")` dispatch was the only production reference to this symbol, and the tracked copy is quality-oracle lib.rs:880 (temper-constraints' own version_py at lib.rs:410 is outside this gate's packages/*/src glob and untrackable). Probe/export-only; no live caller shape remains.
via_count_get_board_bbox_py	PR #750 quality-metrics cluster -- quality/via_count.py's classify_vias_from_parse (the real caller, used by validation/human_reference_extractor.py) is handed an already-parsed ParseResult, and the kernel's board-source boundary accepts only a scenario dict or a .kicad_pcb path, not a pre-parsed object; wiring here would break that caller or require hand-marshalling ParseResult into a scenario dict, duplicating an unused Rust-side conversion path. classify_vias_from_parse and this symbol's Python counterpart (_get_board_bbox) are left unwired together (2026-08-06 wiring); via_count_classify_vias_py itself IS wired, via the path-taking top-level functions.
via_count_get_component_bboxes_py	PR #750 quality-metrics cluster -- same ParseResult-vs-path wire-format gap as via_count_get_board_bbox_py above (2026-08-06 wiring); left unwired alongside classify_vias_from_parse.
via_count_is_via_in_bbox_py	PR #750 quality-metrics cluster -- same ParseResult-vs-path wire-format gap as via_count_get_board_bbox_py above (2026-08-06 wiring); left unwired alongside classify_vias_from_parse.
via_count_is_via_near_board_edge_py	PR #750 quality-metrics cluster -- same ParseResult-vs-path wire-format gap as via_count_get_board_bbox_py above (2026-08-06 wiring); left unwired alongside classify_vias_from_parse.
via_distance	renamed probe (via_placement.rs:98); no caller under either name -- via distance computed in-line by the router (2026-08-08 unwired-kernel gate fix)
