# Registered Rust kernels with no production caller.
#
# Shrink-only. A new entry is a hard failure -- wire the kernel or add it
# here WITH A REASON. An entry that becomes wired is also a failure, so
# paid-down debt shows up in a diff instead of rotting on the books.
#
# Generated by scripts/check_unwired_kernels.py --write-inventory
# <symbol>\t<reason>
geom_on_segment_py	requirements geometry bindings are intentionally test-only after the production validator helper was retired; exercised by the requirements validator corpus and differential tests
geom_orientation_py	requirements geometry bindings are intentionally test-only after the production validator helper was retired; exercised by the requirements validator corpus and differential tests
geom_point_distance_py	requirements geometry bindings are intentionally test-only after the production validator helper was retired; exercised by the requirements validator corpus and differential tests
geom_point_in_rect_py	requirements geometry bindings are intentionally test-only after the production validator helper was retired; exercised by the requirements validator corpus and differential tests
geom_point_to_polyline_distance_py	requirements geometry bindings are intentionally test-only after the production validator helper was retired; exercised by the requirements validator corpus and differential tests
geom_point_to_segment_distance_py	requirements geometry bindings are intentionally test-only after the production validator helper was retired; exercised by the requirements validator corpus and differential tests
geom_polyline_length_py	requirements geometry bindings are intentionally test-only after the production validator helper was retired; exercised by the requirements validator corpus and differential tests
geom_polyline_min_distance_py	requirements geometry bindings are intentionally test-only after the production validator helper was retired; exercised by the requirements validator corpus and differential tests
geom_polylines_intersect_py	requirements geometry bindings are intentionally test-only after the production validator helper was retired; exercised by the requirements validator corpus and differential tests
geom_rects_overlap_py	requirements geometry bindings are intentionally test-only after the production validator helper was retired; exercised by the requirements validator corpus and differential tests
geom_segment_to_segment_distance_py	requirements geometry bindings are intentionally test-only after the production validator helper was retired; exercised by the requirements validator corpus and differential tests
geom_segments_intersect_py	requirements geometry bindings are intentionally test-only after the production validator helper was retired; exercised by the requirements validator corpus and differential tests
