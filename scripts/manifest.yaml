_meta:
  last_audit_date: '2026-08-05'
  total_scripts: 119
  counts:
    keep: 115
    ticket: 0
  ticket_scripts_deleted: 40
scripts:
- path: add_power_planes.py
  purpose: Manually add power plane geometry to a KiCad PCB
  owner: unassigned
  last_run: '2026-06-22'
  category: keep
  disposition: utility
  imports:
  - pathlib
  - re
  - sys
  - uuid
- path: batch_pipeline_validate.py
  purpose: Batch pipeline validation — feed corpus boards through parse→placement→route and quarantine
    failures
  owner: bennet
  last_run: '2026-07-23'
  category: keep
  disposition: utility
  imports:
  - __future__
  - argparse
  - pathlib
  - sys
  - time
  - yaml
- path: bench_coarse_to_fine.py
  purpose: 'A/B benchmark: compare baseline vs coarse-to-fine corridor routing on temper.kicad_pcb'
  owner: router-v6-closure-rate
  last_run: '2026-06-29'
  category: keep
  disposition: utility
  imports:
  - __future__
  - argparse
  - json
  - pathlib
  - sys
  - time
  - typing
- path: bench_rust_constraints.py
  purpose: Benchmark gate for Rust constraint engine extraction — compares Rust vs Python loss computation
    wall-clock performance
  owner: bennet
  last_run: '2026-07-23'
  category: keep
  disposition: utility
  imports:
  - __future__
  - argparse
  - json
  - pathlib
  - statistics
  - sys
  - time
- path: bench_rust_geometry.py
  purpose: Benchmark Rust-backed geometry functions vs pure-Python equivalents
  owner: bennet
  last_run: '2026-07-23'
  category: keep
  disposition: utility
  imports:
  - math
  - statistics
  - temper_geometry
  - time
- path: bless_baselines.py
  purpose: Re-extract baseline metrics after intentional quality improvements (requires Ceiling-Approval)
  owner: pipeline
  last_run: '2026-06-22'
  category: keep
  disposition: utility
  imports:
  - argparse
  - json
  - pathlib
  - subprocess
  - sys
  - typing
  - yaml
- path: bmc_adoption_gate.py
  purpose: 'CI gate: enforce ESL + BMC test coverage for every Constraint subclass'
  owner: pipeline
  last_run: '2026-06-29'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - ast
  - pathlib
  - sys
- path: board_defect_mutator.py
  purpose: 'Board-defect mutation corpus (R38, plan 2026-08-02-024): deterministic seeded transforms that
    produce a mutated copy of pcb/temper.kicad_pcb for each defect class (component off-board, pad short,
    creepage crossing). The committed board is never modified; every mutation writes a run-time copy.'
  owner: bennet
  last_run: '2026-08-02'
  category: keep
  disposition: utility
  imports:
  - __future__
  - argparse
  - dataclasses
  - hashlib
  - kiutils.board
  - kiutils.items.common
  - pathlib
  - shutil
  - sys
  - typing
- path: calibrate_drc_ceiling.py
  purpose: Calibrate DRC violation ceiling for Rust DRC engine
  owner: pipeline
  last_run: '2026-06-30'
  category: keep
  disposition: utility
  imports:
  - argparse
  - collections
  - json
  - pathlib
  - sys
- path: capacity_budget_gate.py
  purpose: 'CI gate: design-capacity budget check over the fault-tree''s SN74HC4075 fan-in -- reachability
    over elec/build/default.net, not pin-occupancy counting; fails closed on missing/empty netlist or
    BOM, an absent required package, or a real fault source wired into a dead-output gate (docs/evidence/2026-07-26-design-capacity-budget-gate.md)'
  owner: bennet
  last_run: '2026-07-26'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - csv
  - dataclasses
  - pathlib
  - sys
  - yaml
- path: check_board_containment.py
  purpose: 'Board-containment gate (R26 invariant, owning gate of the board-defect corpus''s off-board
    class): every footprint''s pad copper must lie inside the Edge.Cuts outline. Closes the class no gate
    covered -- DRC counts FALL when a component leaves the board, so no count-delta can detect it. Fails
    closed (exit 2) on a missing/unparseable board or an undeterminable outline.'
  owner: bennet
  last_run: '2026-08-04'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - dataclasses
  - pathlib
  - sys
- path: check_board_defect_corpus.py
  purpose: 'Board-defect mutation corpus runner (R38, plan 2026-08-02-024): for each defect class, re-derive
    the mutated board from the committed board and assert its owning gate fails (count-delta); assert
    the clean board passes the corpus gates (anti-vacuity, scoped to gates green on the clean board).
    Fails closed (exit 2) without kicad-cli or the REQ-SAFE-01 inputs.'
  owner: bennet
  last_run: '2026-08-02'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - collections
  - dataclasses
  - json
  - pathlib
  - re
  - shutil
  - sys
  - tempfile
  - typing
- path: check_board_sync_stamp.py
  purpose: 'Board-sync-stamp freshness CI gate (2026-08-13 board/netlist desync postmortem, gap C):
    fails closed unless the committed pcb/temper.kicad_pcb.source-digest stamp -- written by write_board_sync_stamp.py,
    which itself refuses to run unless check_netlist_board_reconciliation.py currently passes -- matches
    the board''s and netlist''s CURRENT content. Complements the reconciliation gate with a cheap, git-diff-visible
    provenance claim rather than re-deriving its verdict; also requires the netlist itself be proven fresh
    against elec/src/** before trusting any digest comparison against it.'
  owner: bennet
  last_run: '2026-08-13'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.freshness
  - _lib.github_summary
  - _lib.repo
  - argparse
  - check_domain_partition
  - pathlib
  - sys
- path: check_bom_source_reconciliation.py
  purpose: 'BOM<->source reconciliation CI gate (R14): parses docs/hardware/BOM.md component tables and
    elec/src/*.ato physical-component instantiations, matches by normalized designator, and reports costed-with-no-circuit,
    wired-but-uncosted, and same-designator-different-MPN findings. Heuristic string-identity matching
    (documented limitation, no built netlist involved); see the module docstring.'
  owner: bennet
  last_run: '2026-08-07'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - dataclasses
  - pathlib
  - re
  - sys
- path: check_copper_net_consistency.py
  purpose: 'Board-copper / netlist consistency gate: catch corrupted net ordinals.'
  owner: bennet
  last_run: '2026-07-30'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.freshness
  - _lib.github_summary
  - _lib.repo
  - argparse
  - dataclasses
  - json
  - pathlib
  - re
  - sys
  - typing
- path: check_coverage_gate.py
  purpose: 'Coverage gate: fail CI on zero-coverage public functions'
  owner: pipeline
  last_run: '2026-06-22'
  category: keep
  disposition: ci-gate
  imports:
  - _lib.gate_allowlist
  - _lib.repo
  - argparse
  - ast
  - json
  - pathlib
  - rich.console
  - sys
- path: check_creepage_clearance_drift.py
  purpose: 'Safety-constant SSOT drift gate: compare declared creepage and clearance figures across electrical
    source, scripts, packages, and configuration.'
  owner: bennet
  last_run: '2026-07-30'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - ast
  - dataclasses
  - pathlib
  - re
  - sys
- path: check_dead_parameter_inputs.py
  purpose: 'Dead-parameter & physics-input standing check (plan 2026-08-02-019, U3): wires then unwires
    every declared live gate input (acceptance-gate inner surface) and every registered physics parameter
    (physics_parameter_map.yaml) and requires the output to change — verdict flip for gate consumers,
    delta-over-noise-floor for threshold-less consumers; fails on a dead input, an unregistered physics
    parameter, or a registry validation error'
  owner: bennet
  last_run: '2026-08-02'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - json
  - pathlib
  - sys
  - temper_placer.validation.dead_parameter_probe
  - temper_placer.validation.gate_input_registry
- path: check_derived_doc_drift.py
  purpose: 'Derived-document drift gate: fail when a summary table silently drops a qualifier, column,
    or value restated from docs/FUNCTIONAL_TEST_CRITERIA.md'
  owner: bennet
  last_run: '2026-07-26'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - dataclasses
  - datetime
  - pathlib
  - re
  - rich.console
  - sys
  - yaml
- path: check_doc_path_citations.py
  purpose: 'Doc-path citation gate (spike proof, docs/brainstorms/2026-08-12-referential-integrity-options.md):
    fail when a docs/plans/*.md or docs/evidence/*.md path cited in a comment/docstring/doc does not
    resolve. Measured 47 dangling citations (27 never-existed, 20 deleted) on origin/main at spike time.
    Built and proven failing on the real repo; deliberately NOT wired into any workflow yet -- see the
    spike doc for the advisory-landing recommendation before this disposition is changed to ci-gate.'
  owner: bennet
  last_run: '2026-08-12'
  category: keep
  disposition: utility
  imports:
  - __future__
  - _lib.repo
  - argparse
  - dataclasses
  - pathlib
  - re
  - subprocess
  - sys
  - yaml
- path: check_domain_partition.py
  purpose: 'Netlist domain-partition CI gate: verify the compiled netlist''s HV/SELV domains are actually
    disjoint (docs/hardware/IEC60335_CRITICAL_COMPONENTS.md Sec 2) against elec/domain_manifest.yaml;
    fail-closed on missing/empty/stale netlist or manifest'
  owner: bennet
  last_run: '2026-07-26'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.freshness
  - _lib.github_summary
  - _lib.repo
  - argparse
  - collections
  - dataclasses
  - json
  - pathlib
  - re
  - sys
  - typing
- path: check_drc_ceiling_approval.py
  purpose: 'CI gate: fail a PR that raises any power_pcb_dataset/drc_ceiling.json ceiling value (aggregate
    error_ceiling/warning_ceiling, or any violations_by_type/warnings_by_type rule) relative to the merge
    base unless a commit in the PR carries a Ceiling-Approval: trailer AND the raise satisfies the R27
    measurement-evidence contract (a NEW non-empty _march entry naming the cause -- the _march log is
    the single cause authority -- plus a fresh measured-live provenance record on the raised board: resolvable
    commit, clean tree, recorded kicad-cli version, >=120 samples for the nondeterministic clearance category,
    input hash matching pcb/temper.kicad_pcb) -- wires DrcRatchet.detect_ceiling_raise / find_ceiling_raises
    / validate_raise_evidence (temper_placer.regression.drc_ratchet), which existed and was unit-tested
    but had no CI caller, into the Provenance & Anti-Vacuity Gates job'
  owner: bennet
  last_run: '2026-08-02'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.repo
  - argparse
  - json
  - pathlib
  - rich.console
  - subprocess
  - sys
- path: check_drc_determinism.py
  purpose: 'DRC determinism harness: runs the KiCad DRC through the CI gate''s own path N times and reports
    whether each violation category returns a stable count and a stable violation SET. Backs the kicad-cli
    worker-pool pinning -- clearance and shorting_items were a thread-pool race, creepage a pointer-keyed
    dedup (upstream kicad#20048). Carries --inject-variance so the harness is provably able to report
    nondeterminism, not only absence of it.'
  owner: bennet
  last_run: '2026-08-05'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - collections
  - hashlib
  - os
  - pathlib
  - re
  - sys
- path: check_erc_off_grid_consequence.py
  purpose: 'Classify ERC endpoint_off_grid warnings (163 of 498 on pcb/temper.kicad_sch as of 2026-08-07)
    by electrical consequence: cross-checks each off-grid pin''s actual schematic net (kicadxml export,
    exact-coordinate connectivity) against its atopile-compiled net member-for-member, and tags the net''s
    domain (HV/mains, SELV, protection/fault-chain) via elec/domain_manifest.yaml. Answers "does this
    off-grid endpoint sit on a net that matters, and is it actually connected to what it looks connected
    to" mechanically instead of by eyeballing coordinates. Wired into CI (2026-08-07): python-tests.yml
    consistency-gates job regenerates the ERC-JSON/netlist-XML inputs via the pinned kicad-cli in the
    temper-ci container immediately before invoking this gate (not runnable in every local sandbox --
    see docs/evidence/2026-08-07-erc-off-grid-endpoint-analysis.md for how 10.0.5 was obtained here).'
  owner: bennet
  last_run: '2026-08-07'
  category: keep
  disposition: utility
  imports:
  - __future__
  - argparse
  - collections
  - json
  - pathlib
  - re
  - sys
  - xml.etree.ElementTree
- path: check_evidence_provenance.py
  purpose: 'Fail-closed provenance gate for docs/evidence/: every evidence file must declare how its numbers
    were produced'
  owner: bennet
  last_run: '2026-07-27'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.gate_allowlist
  - _lib.github_summary
  - _lib.repo
  - argparse
  - json
  - pathlib
  - re
  - rich.console
  - subprocess
  - sys
- path: check_fault_list_consistency.py
  purpose: 'CI cross-check: verify manifest.json fault codes are a subset of generated FAULT_LIST'
  owner: firmware
  last_run: '2026-06-29'
  category: keep
  disposition: ci-gate
  imports:
  - json
  - pathlib
  - re
  - sys
  - yaml
- path: check_firmware_board_contract.py
  purpose: 'Firmware-assumption contract oracle (plan 2026-08-02-027, R18): reverse-checks every firmware
    constant with a board derivation against the ACTUAL board (pcb/temper.kicad_pcb), not against another
    declaration -- hardware and firmware cannot disagree about a load-bearing value. Iterates firmware/tools/board_derivations.yaml
    (the registry), extracts the board inputs by Sheetpath property, classifies a registered component
    OUTSIDE the Edge.Cuts outline as missing (the off-board tank.c_tank3 defect class a refdes/value lookup
    would pass), applies the shared formula library firmware/tools/board_derivation_lib.py, and fails
    with the constant and both values named on disagreement / missing component / UNMEASURED. Advisory
    in CI until the off-outline tank cap is placed (landing contract); the unit tests are hard.'
  owner: bennet
  last_run: '2026-08-02'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - board_component_extractor
  - board_derivation_lib
  - check_pll_range_consistency
  - dataclasses
  - math
  - pathlib
  - re
  - sys
  - yaml
- path: check_firmware_invariants.py
  purpose: 'CI gate (R29, plan 2026-08-02-028 U7): rerun firmware/tools/invariants.py from source over
    the shared transition model (firmware/tools/transition_model.py) on every invocation and fail on any
    violated load-bearing safety invariant (over-temp disables the power stage, sensor faults block heating,
    faulted states cannot re-enter heating without the reset path, and the runaway interlock reaches a
    safe/de-energized state from every state) -- named with the invariant id and offending edge/path evidence.
    Separately fails (DRIFT, exit 3) if the committed firmware/tools/proof_record.json does not byte-match
    a fresh regeneration, so the proof record can never silently go stale relative to the manifest. Wired
    into CI (2026-08-07): firmware-tests.yml.'
  owner: bennet
  last_run: '2026-08-07'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - gen_proof_record
  - invariants
  - pathlib
  - sys
  - transition_model
- path: check_footprint_drift.py
  purpose: 'Footprint-drift gate: catch a footprint corrected in elec/src/*.ato that never propagated
    into pcb/temper.kicad_pcb, matched by sheetpath (designator-renumbering-safe), not refdes.'
  owner: bennet
  last_run: '2026-07-30'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.freshness
  - _lib.github_summary
  - _lib.repo
  - argparse
  - dataclasses
  - json
  - pathlib
  - re
  - sys
  - typing
- path: check_gate_mutations.py
  purpose: 'R42 gate-mutation runner: the canary-flip oracle. For every (gate, mutation, canary) triple
    in ci-corpus/mutations.yaml, runs the unmutated gate against the canary (baseline must reject the
    seed, pass the pristine), applies the mutation via gate_mutate.py to a scratch temp copy, substitutes
    it into sys.modules for one call, and asserts the canary flips from pass to fail -- KILLED when it
    does, SURVIVED when the weakened gate still rejects the seed (a blind spot), UNVERIFIED on a broken
    baseline or a crashing mutant. Fails closed on zero registered triples, any SURVIVED, or any UNVERIFIED;
    never continue-on-error.'
  owner: bennet
  last_run: '2026-08-07'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.repo
  - argparse
  - dataclasses
  - gate_mutate
  - importlib.util
  - pathlib
  - sys
  - tempfile
  - yaml
- path: check_hash_order_determinism.py
  purpose: 'CI gate: fail on any NEW site under packages/*/src that records a PYTHONHASHSEED-chosen order
    -- a set/frozenset iteration materialised, accumulated over, indexed, first-element-selected or appended
    in a loop body, or a call to the salted hash() builtin outside __hash__. Six such sites shipped (issue
    #752 defects 6 and 7; PR #730), producing component coordinates, a geometric waypoint and a report
    order that differed between processes on identical input. Known sites are ratcheted in .hash-order-inventory;
    a stale entry is a failure so paid-down debt must be recorded.'
  owner: bennet
  last_run: '2026-08-05'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - ast
  - dataclasses
  - pathlib
  - sys
- path: check_hv_netclass_coverage.py
  purpose: 'CI gate: assert every elec/domain_manifest.yaml HV-domain net has a TEMPER_NET_ASSIGNMENTS
    entry, and every declared netclass (TEMPER_NET_CLASSES union pcb/temper.kicad_pro-described classes,
    minus the structural Default/Differential classes) has at least one positive rule in scripts/generate_kicad_dru.py''s
    generated output -- catches the +170V_BUS-resolves-to-no-netclass and HighVoltageIsolated-enforces-nothing
    defect class confirmed on origin/main (docs/evidence/2026-07-29-hv-netclass-coverage-gate.md)'
  owner: bennet
  last_run: '2026-07-29'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - check_domain_partition
  - collections.abc
  - dataclasses
  - json
  - pathlib
  - re
  - sys
  - typing
- path: check_isolation_keepout.py
  purpose: 'Physical mains<->SELV isolation-barrier CI gate: verify a dedicated keepout region spans all
    copper layers, is >=8.0mm wide (REINFORCED creepage) throughout, bisects the board into HV/SELV sides,
    and that no copper (trace/via/pad/zone) intrudes -- pcb/temper.kicad_pcb has zero keepout zones today,
    so this barrier exists only as declarations/after-the-fact checks; fail-closed on missing/empty board
    or manifest, zero HV or SELV pads found, or a missing barrier zone (docs/evidence/2026-07-28-isolation-keepout.md)'
  owner: bennet
  last_run: '2026-07-28'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - dataclasses
  - pathlib
  - sys
  - temper_placer.core.isolation_constants
  - temper_placer.core.pad_geometry
  - temper_placer.geometry.kicad_transform
- path: check_layer_plane_emission_coverage.py
  purpose: 'CI gate: assert every layer pcb/temper.kicad_pcb declares with a power-plane role has both
    (1) a parser that preserves the role token (parse_engine.rs raw_board_from_tree) and (2) a zone-emission
    code path reachable from route_pcb that can actually target it (router_v6/_zone_pour_stitch.py _zone_layers_for_net)
    -- catches the In1.Cu/In2.Cu declared-but-never-poured defect (commit c4956df66; docs/evidence/2026-08-11-correspondence-gates.md).
    Advisory (continue-on-error) until both underlying bugs are fixed -- see the evidence doc for the
    exact path to blocking.'
  owner: bennet
  last_run: '2026-08-11'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - dataclasses
  - pathlib
  - re
  - sys
- path: check_layer_utilization_gate.py
  purpose: 'CI gate: compute declared signal-layer count (temper_placer.core.board_layer_roles,
    read from pcb/temper.kicad_pcb''s (layers ...) block) against live routing demand (bbox/fill-factor
    geometry from the real board+netlist, resource_bound.py''s method) and fail closed above a
    utilization threshold. Reproduces the 1.31-utilization infeasibility PR #1172 measured after a
    real router failure (40/139 nets, zero legal path) and would have caught it before any router ran
    -- docs/evidence/2026-08-13-layer-architecture-decision.md. Blocking from introduction (2026-08-13):
    the real board''s 6-layer declaration (landed the same PR) passes at 0.657.'
  owner: bennet
  last_run: '2026-08-13'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - dataclasses
  - pathlib
  - sys
- path: check_manifest_gate.py
  purpose: 'CI gate: enforce scripts/manifest.yaml <-> filesystem consistency'
  owner: pipeline
  last_run: '2026-06-22'
  category: keep
  disposition: ci-gate
  imports:
  - argparse
  - pathlib
  - sys
- path: check_measurement_provenance.py
  purpose: 'CI gate: fail closed when a measurement artifact (power_pcb_dataset/drc_ceiling.json today)
    declares input-file content hashes that no longer match reality -- catches the DRC ceiling recording
    91 errors for a board three commits (c6b1b463, 556ccf4f, 65bd0159) had since changed to ~710, with
    the ceiling file byte-identical throughout (docs/evidence/2026-07-27-drc-truth-gate-discrepancy.md,
    docs/evidence/2026-07-28-measurement-provenance.md); STALE is a GATE ERROR, not a violation, generalizing
    the netlist-freshness precedent in check_domain_partition.py'
  owner: bennet
  last_run: '2026-07-28'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.gate_allowlist
  - _lib.github_summary
  - _lib.measurement_provenance
  - _lib.repo
  - argparse
  - check_evidence_provenance
  - dataclasses
  - json
  - pathlib
  - sys
  - typing
  - yaml
- path: check_migration_narrowing.py
  purpose: 'CI gate: detect a Rust port silently narrowing a Python capability -- const-ification of a
    module-level Python constant that stops reaching the delegate call (H_CONV_BACKGROUND), or numeric
    narrowing at the pyo3 boundary (Option<i64> extraction where Python applies float()) -- neither shape
    is visible to differential suites since they compare on generated inputs, not perturbed constants
    or fractional values (2026-08-04/07 triage, fix/main-triage)'
  owner: bennet
  last_run: '2026-08-07'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - ast
  - dataclasses
  - pathlib
  - re
  - sys
- path: check_net_classification.py
  purpose: 'CI gate: fail when safety-relevant net classification (HV/SELV) is decided by plain substring
    match against a hand-maintained keyword list, in parallel to elec/domain_manifest.yaml -- the defect
    class confirmed three times in this repo (creepage_check.py false positives, clearance_check.py false
    negatives, clearance_engine.py bare HV/AC substrings) (docs/evidence/2026-07-27-net-classification-gate.md)'
  owner: bennet
  last_run: '2026-07-27'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - ast
  - dataclasses
  - fnmatch
  - pathlib
  - sys
- path: check_router_clearance_floor.py
  purpose: 'CI gate: the clearance the router reserves for an unclassified net must
    never be below the clearance kicad-cli DRC grades that net against. Between
    051152e7c (2026-07-12) and 2026-08-12 router_v6/_pipeline_core.py lowered
    design_rules.default_clearance_mm to 0.15 while generate_kicad_dru.py RULE 10
    "Default routing" emitted 0.2mm -- Stage 4 A* reserves trace_width + clearance,
    so two 0.25mm Default tracks landed 0.40mm centre-to-centre, an edge gap of
    exactly 0.1500mm against a 0.2000mm rule. 285 of the heatsink candidate board''s
    505 clearance errors were that defect (docs/evidence/
    2026-08-12-clearance-congestion-band.md). Five properties, P4 checked by AST so a
    reformatted reintroduction is still caught. Mutation-verified: P4 fails on
    the pre-fix _pipeline_core.py (c87492f38:84).'
  owner: bennet
  last_run: '2026-08-12'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - ast
  - json
  - pathlib
  - re
  - sys
  - yaml
- path: check_netclass_class_param_correspondence.py
  purpose: 'CI gate: assert every netclass declared in BOTH pcb/temper.kicad_pro''s net_settings.classes
    and design_rules.py''s TEMPER_NET_CLASSES agrees on all four scalar routing fields (clearance, trace_width/track_width,
    via_diameter, via_drill) -- the dimension PR #1023/#1025 (both titled as syncing kicad_pro against
    design_rules.py) and sync_kicad_netclass_assignments.py never checked: all three only ever compare/write
    netclass_assignments (which net maps to which class), never a class''s own parameter values. Currently
    VIOLATION on origin/main: HighVoltage.clearance 6.0mm (design_rules.py) vs 2.0mm (kicad_pro), plus
    4 Power-class field mismatches (docs/evidence/ 2026-08-12-gate-vacuity-structural-prevention.md).
    Advisory (continue-on-error) until reconciled.'
  owner: bennet
  last_run: '2026-08-12'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - dataclasses
  - json
  - pathlib
  - sys
  - typing
- path: check_netclass_map_board_correspondence.py
  purpose: 'CI gate: assert every net-name key in a hand-maintained top-level net_classes: YAML mapping
    (discovered under configs/*.yaml and packages/temper-placer/configs/*.yaml) is a real net name on
    pcb/temper.kicad_pcb -- the generalisation of the netclass_assignments-drift defect class (#1023/#1025
    fixed the kicad_pro<->TEMPER_NET_ASSIGNMENTS pair; this gate covers the OTHER hand-maintained copies
    found by survey: configs/temper_deterministic_config.yaml, configs/temper_production_config.yaml,
    packages/temper-placer/configs/temper_constraints.yaml, packages/temper-placer/configs/ gate_driver_constraints.yaml
    all currently have stale/case-mismatched keys that silently no-op on Netlist.apply_net_class_mapping''s
    exact-key lookup) (docs/evidence/2026-08-11-correspondence-gates.md). Advisory (continue-on-error)
    until the four files are fixed.'
  owner: bennet
  last_run: '2026-08-11'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - dataclasses
  - pathlib
  - re
  - sys
  - yaml
- path: check_netlist_board_reconciliation.py
  purpose: 'Netlist<->board reconciliation CI gate (plan 2026-08-02-021, R16): compares the netlist extracted
    from pcb/temper.kicad_pcb against the compiled design netlist keyed by instance path (Sheetpath) and
    net membership, not refdes -- wholesale renumbering, missing components, reused refdes, and dropped
    nets fail regardless of refdes overlap; the tank-cap class is PASS-for-missing on the current board
    (tank.c_tank3 is present off-outline, owned by the R26 plan).'
  owner: bennet
  last_run: '2026-08-02'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - pathlib
  - sys
- path: check_netlist_stage_checks.py
  purpose: 'Netlist-stage checks CI gate (docs/STRATEGY.md build order step 6): single-pin nets, unconnected
    power pins (declared power/ground pins resolved from elec/src/components.ato, never net-name substrings),
    and voltage-domain compatibility (component voltage_rating vs. the explicit voltage named in a domain
    net''s own name, e.g. "+170V_BUS" -> 170V) -- imports check_domain_partition.py''s netlist parser,
    freshness check and domain-manifest loader rather than re-deriving them; isolation-domain reachability
    itself is already covered by check_domain_partition.py and deliberately not re-implemented here. 27
    real, open single-pin-net findings are a dated backlog allowlist entry class (backlog: true + seeded
    pair, netlist-stage-checks-allowlist.yaml), reported every run but not blocking. Wired into CI (2026-08-07):
    python-tests.yml consistency-gates job.'
  owner: bennet
  last_run: '2026-08-07'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - check_domain_partition
  - dataclasses
  - pathlib
  - re
  - sys
- path: check_netlist_mutation_corpus.py
  purpose: 'Netlist-mutation corpus CI runner (plan 2026-08-02-021, R39/U7): applies the renumber/dropped-net/reused-refdes
    mutations to a copy of the design netlist and asserts each class fails its owning identity check (renumber
    must PASS the 95% refdes-overlap check while the sheetpath oracle reports RENUMBERED) with the clean
    netlist green (anti-vacuity); fail-closed on a stale netlist or missing tool.'
  owner: bennet
  last_run: '2026-08-02'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - pathlib
  - sys
  - tempfile
- path: check_no_raw_rotation_trig.py
  purpose: 'CI gate: fail if any of the 12 files that have already carried KiCad''s R(+theta)/R(-theta)
    footprint-child rotation sign bug (see docs/evidence/2026-07-29-cross-domain-creepage-rotation-convention.md)
    contains a raw math.cos/math.sin/np.cos/np.sin call instead of importing temper_placer.geometry.kicad_transform,
    the single sanctioned implementation those 12 sites were consolidated into'
  owner: bennet
  last_run: '2026-07-30'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - ast
  - dataclasses
  - pathlib
  - sys
- path: check_oracle_hashes.py
  purpose: 'CI gate (oracle content-hash gate, decision 2026-08-06, issues #754 P2-2 / #758): every _*_py_oracle.py
    under packages/ is a VERBATIM pin of a pre-migration Python implementation, and the Wave-4 differential
    suites compare the Rust kernels against them -- so an oracle edit that is not a deliberate re-pin
    silently redefines the reference and the differentials keep passing against the wrong target. The
    gate compares each oracle''s sha256 against the committed registry (scripts/oracle_hashes.json) and
    fails on DRIFTED / DELETED / UNREGISTERED (a new oracle with no registry entry -- the anti-vacuity
    direction). Regeneration is the migration PR''s job (scripts/update_oracle_hashes.py, keep-in-sync
    with the oracle change in the same PR); this script is deliberately NOT a generator, so a drifted
    tree cannot launder its drift into the registry.'
  owner: wave4-migration
  last_run: '2026-08-05'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - dataclasses
  - hashlib
  - json
  - pathlib
  - sys
- path: check_pad_orientation.py
  purpose: 'Pad-orientation gate: catch footprint rotations that never reached the pads. Check 1 -- a
    footprint rotated by a non-multiple of 180 deg whose pads ALL carry absolute angle 0 is the signature
    of a serializer that rotated pad positions but not pad bodies (docs/evidence/2026-07-29-intra-component-shorts-root-cause.md).
    Check 2 -- separating-axis test for intra-footprint copper overlap between different-net pads. Reads
    the board bytes directly (no kiutils, no kicad-cli) so the ABSENCE of an optional angle token is visible.
    Fail-closed on a missing/unparseable board, zero footprints, zero pads, or zero pad pairs compared.
    NOT YET CI-WIRED: check 1 passes on the regenerated board but check 2 still reports K1, whose two
    F.Fab-only pads hit the "no declared copper layer -> assume sharing" fail-closed rule -- a gap in
    this gate''s own contract, analysed in docs/evidence/2026-07-29-board-regeneration-corrected-footprints.md
    Sec 7. Wire it in the change that closes that gap.'
  owner: bennet
  last_run: '2026-07-29'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - dataclasses
  - math
  - pathlib
  - re
  - sys
  - temper_placer.geometry.kicad_transform
- path: check_pcl_config_board_correspondence.py
  purpose: 'CI gate: assert every component reference in a PCL placement-constraint config (default packages/temper-placer/configs/constraints/temper_induction_cooker.yaml)
    resolves to a real board component (direct match, or via temper_constraints.references.yaml component_aliases
    -- with unresolved_components taking precedence over a coincidental direct match, e.g. Q1/Q2), and
    every declared zone lies within pcb/temper.kicad_pcb''s own Edge.Cuts outline -- catches nonexistent
    connector references and off-board zone geometry (PR #1026; docs/evidence/2026-08-11-correspondence-gates.md).
    Advisory (continue-on-error) until the config is fixed -- see the evidence doc for the exact path
    to blocking.'
  owner: bennet
  last_run: '2026-08-11'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - dataclasses
  - pathlib
  - sys
  - typing
  - yaml
- path: check_pd2_compartment_evidence.py
  purpose: 'CI gate: if the tree currently claims PD2/8.0mm reinforced creepage (generate_kicad_dru.py
    HV_CREEPAGE_ENFORCED_MM), a compartment-evidence artifact (docs/specs/pd2_compartment_evidence.yaml)
    must exist with real, non-placeholder cover/gasket/partition/airflow-routing/inspection fields, cross-checked
    against a real named zone in pcb/temper.kicad_pcb -- catches the PD2 exception being claimed without
    the sealed compartment it requires (docs/evidence/2026-08-11-pd2-decision-record.md). Advisory (continue-on-error)
    until the compartment is built and the evidence file is committed -- see the evidence doc for the
    exact path to blocking. Expected to VIOLATE on origin/main today: the compartment does not exist yet.'
  owner: bennet
  last_run: '2026-08-11'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - dataclasses
  - pathlib
  - re
  - sys
  - yaml
- path: check_physics_provenance.py
  purpose: 'Physics provenance gate: require # source: comments on module-level float constants in physics/'
  owner: pipeline
  last_run: '2026-07-22'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.gate_allowlist
  - _lib.repo
  - argparse
  - ast
  - pathlib
  - re
  - rich.console
  - sys
- path: check_plane_condemnation_quantifier.py
  purpose: 'Plane-condemnation quantifier gate: catch a MINORITY of a copper layer''s zones (fraction
    < 0.5) silently condemning the whole physical layer via _extract_stackup''s any()-quantifier classification,
    which routing_space.py then excludes wholesale -- docs/solutions/logic-errors/single-zone-condemns-whole-copper-layer-plane-2026-07-29.md.
    Independently re-derives per-layer zone fractions via kiutils (reusing _is_plane_required_net as the
    SSOT, not a second copy) and cross-references against temper_placer''s own real classification. Every
    currently-condemned layer must be on the dated plane-condemnation-allowlist.yaml or the gate fails
    -- so a NEW layer joining the condemned set, not just the two (F.Cu/B.Cu) already known and tracked,
    is caught. Fail-closed on a missing/unparseable board, zero copper layers, zero net-bearing zones,
    or a malformed allowlist. Wired into CI (2026-08-07): python-tests.yml consistency-gates job.'
  owner: bennet
  last_run: '2026-08-07'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - dataclasses
  - pathlib
  - sys
- path: check_pll_range_consistency.py
  purpose: 'CI gate: fail when firmware/components/control/pll_control.h''s PLL_MIN/MAX/DEFAULT_FREQ_HZ
    and elec/src/main.ato''s declared f_switching/f_pll_tracking_min/max disagree -- main.ato asserted
    a 20-100kHz tracking range 5x wider than the firmware''s real 30-50kHz clamp, and PLL_DEFAULT_FREQ_HZ
    (35000) started the bridge at the frequency an independent ZVS analysis found loses 100.7% of ZVS
    margin, while main.ato had already moved to 47kHz (docs/evidence/2026-07-28-pll-defaults-and-range-gate.md).
    EXTENDED 2026-07-29: also DERIVES the required PLL floor from main.ato''s declared l_tank_assumed
    / l_pan_loaded_ratio / c_tank_total / l_tank_tolerance (1.05 x the worst-case loaded resonance) and
    fails if PLL_MIN_FREQ_HZ is below it -- the previous checks only compared declarations against each
    other and so were blind to both files agreeing on 30kHz, 7.6kHz BELOW the tank''s loaded resonance,
    where a series-resonant bridge hard-switches (docs/evidence/2026-07-29-pll-floor-above-resonance.md).
    Also cross-checks main.ato''s c_tank_total mirror against modules.ato''s c_tank1+c_tank2'
  owner: bennet
  last_run: '2026-07-29'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - board_derivation_lib
  - dataclasses
  - math
  - pathlib
  - re
  - sys
- path: check_refdes_identity_stability.py
  purpose: Discovers safety-relevant test/fixture assertions keyed to a ref-designator string literal
    (e.g. "U3", "K1") instead of instance_path, and cross-checks real-board-bound ones against the current
    compiled netlist plus elec/domain_manifest.yaml to flag an ACTUAL identity mismatch, not just presence
    -- built after a ZCD-optocoupler deletion reshuffled U-prefix refs and left test_the_seven_known_intra_footprint_blockers_are_now_visible
    checking different real components than its own name/docstring claim while still passing. Reports
    a full denominator (raw literals, safety-keyword context, assert-reachable, load-bearing, real-board-bound)
    at every stage; never wired into CI (false-positive rate unmeasured on first pass) -- see docs/evidence/2026-07-30-refdes-identity-stability-gate.md.
  owner: bennet
  last_run: '2026-07-30'
  category: keep
  disposition: utility
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - ast
  - check_domain_partition
  - dataclasses
  - pathlib
  - re
  - sys
- path: check_regression.py
  purpose: Functional regression check (called by Makefile)
  owner: unassigned
  last_run: '2026-06-22'
  category: keep
  disposition: shell-invoked
  imports:
  - sys
- path: check_required_checks.py
  purpose: 'Always-on pull-request aggregator: require the candidate Python Tests contexts when a manifest
    trigger path matches, while treating workflow-level path filtering as an explicit legitimate skip.
    A `skipped` required context passes only when the checker verifies the skip against the PR changed-file
    list and the manifest per-job trigger sets (fail-closed otherwise).'
  owner: bennet
  last_run: '2026-08-02'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - collections.abc
  - dataclasses
  - json
  - os
  - pathlib
  - re
  - sys
  - time
  - typing
  - urllib.error
  - urllib.parse
  - urllib.request
- path: check_rotation_quadrant_arithmetic.py
  purpose: 'CI gate: fail when Component.initial_rotation_quadrant (a 0-3 quarter-turn INDEX, renamed
    2026-08-13 from initial_rotation) is divided by a number, or passed directly to math.radians()/.to_radians()
    with no prior "* 90", the exact shapes behind three independent wrong safety-geometry answers in one
    day plus a fourth live bug this change fixed in router_v6/_pipeline_verify.py. See
    scripts/tests/test_check_rotation_quadrant_arithmetic.py for the proof it fires on those shapes and
    not on the many correct call sites.'
  owner: bennet
  last_run: '2026-08-13'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - pathlib
  - re
  - sys
- path: check_rust_drc_presence.py
  purpose: 'CI gate: fail-closed presence/freshness check for the temper_drc_rs PyO3 extension -- a bare
    import cannot detect a stale wheel missing a symbol the current lib.rs registers (2026-07-26 incident:
    verify_route_clearance silently absent, differential test silently skipped); TEMPER_REQUIRE_RUST_DRC=1
    (CI) makes any gap fatal, unset (local dev) makes it a warning'
  owner: bennet
  last_run: '2026-07-26'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - importlib
  - os
  - pathlib
  - re
  - sys
- path: check_script_sunset.py
  purpose: 'CI gate: warn on stale scripts per the 30-day sunset clock'
  owner: pipeline
  last_run: '2026-06-22'
  category: keep
  disposition: ci-gate
  imports:
  - argparse
  - datetime
  - json
  - pathlib
  - sys
- path: check_sil_coverage.py
  purpose: 'CI gate: SIL fault-injection coverage over (fault class, origin state) -- fails on any pair
    the transition table (plus documented C-only interlock call sites) defines but firmware/test/traces/manifest.json
    does not exercise (plan 2026-08-02-031, U1/U4). Thin wrapper around firmware/test/test_sil_coverage.py
    --gate. Wired into CI (2026-08-07): firmware-tests.yml, immediately after the sil_fault_tests ctest
    step.'
  owner: firmware
  last_run: '2026-08-07'
  category: keep
  disposition: ci-gate
  imports:
  - pathlib
  - subprocess
  - sys
- path: check_stackup_copper_weight_gate.py
  purpose: 'CI gate: assert pcb/temper.kicad_pcb''s (setup (stackup ...)) block declares the same per-role
    copper weight (2oz outer / 1oz inner) that docs/hardware/TRACE_WIDTH_CALCULATIONS.md''s §1 design
    parameters assume, for both F.Cu/B.Cu (outer) and In1.Cu/In2.Cu (inner). Closes the gap where the
    board declared no stackup at all (docs/evidence/2026-08-13-jlcpcb-fab-capability-envelope.md) --
    every IPC-2221B current-capacity derivation in that doc was contingent on a fab order-form note
    nothing in the repo enforced. Blocking from introduction (2026-08-13): both inputs were reconciled
    in the same change that added the gate.'
  owner: bennet
  last_run: '2026-08-13'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - dataclasses
  - pathlib
  - re
  - sys
- path: check_stale_extensions.py
  purpose: 'CI gate: fail-closed freshness check for every pyo3/maturin Rust extension crate under packages/
    -- an installed .so older than its own crate''s source (or a crate whose extension is missing entirely)
    is reported; zero crates discovered is always a gate error, never a vacuous pass (2026-07-27 incident:
    missing .cargo/config.toml broke every `maturin develop` on macOS, so temper_io_types.cpython-312-darwin.so
    stayed frozen while lib.rs gained ConfigBoardMismatchError, silently failing two test modules'' collection;
    see commit 02e907b9 and docs/evidence/2026-07-27-stale-extension-gate.md)'
  owner: bennet
  last_run: '2026-07-27'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.freshness
  - _lib.github_summary
  - _lib.repo
  - argparse
  - dataclasses
  - datetime
  - hashlib
  - importlib.util
  - os
  - pathlib
  - sys
  - tomllib
- path: check_venv_integrity.py
  purpose: 'CI gate: asserts every editable-install `.pth` file and local `direct_url.json` in the checked
    venv''s site-packages resolves under the expected repo root, and not into a different git worktree
    or an unrelated checkout -- catches the shared venv identity being silently hijacked (2026-08-11 incident:
    `maturin develop --active` run from an agent worktree rewrote the SHARED venv''s editable pointers
    to that worktree, so every measurement in that window ran against the wrong code; see AGENTS.md "Worktree
    .venv: shared vs. isolated"). Complements, and runs before, check_stale_extensions.py -- freshness
    is only a coherent question once the venv''s identity is trusted.'
  owner: bennet
  last_run: '2026-08-11'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - dataclasses
  - json
  - pathlib
  - subprocess
  - sys
  - urllib.parse
  - urllib.request
- path: check_stash_stack_gate.py
  purpose: Detector for git stash activity the reference-transaction hook cannot block (apply, and pop/drop
    of a non-terminal entry) -- snapshots the stash reflog and flags additions/removals since the last
    run. Not a CI gate; run manually or on a timer against the shared dev machine.
  owner: bennet
  last_run: '2026-07-28'
  category: keep
  disposition: utility
  imports:
  - argparse
  - json
  - pathlib
  - subprocess
  - sys
- path: check_state_machine_model.py
  purpose: 'CI gate (R28, plan 2026-08-02-028 U4): runs, in one invocation, the exhaustive reachability
    report over the transition model (firmware/tools/transition_model.py -- 9 states x 25 events including
    the 2 KTD2 runaway-interlock wildcard events, closure from STATE_INIT), the P1-P4 unsafe-state/transition-property
    checks (firmware/tools/transition_model_checks.py), and the production-vs-test-generator manifest
    cross-check (firmware/tools/transition_manifest_crosscheck.py) -- fails closed on any unreachable
    state, undocumented interlock-only state, property violation, or manifest divergence not covered by
    the documented KTD2 wildcard state-set exception OR a dated backlog entry (state-machine-crosscheck-allowlist.yaml).
    Writes the reachability report artifact to firmware/tools/state_machine_reachability_report.json on
    every run. Wired into CI (2026-08-07): firmware-tests.yml. One real, pre-existing manifest divergence
    ((STATE_FAULT, EVENT_FAULT_RESET_PERSISTS)) is a dated backlog entry, deferred to a firmware maintainer.'
  owner: bennet
  last_run: '2026-08-07'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - dataclasses
  - json
  - pathlib
  - re
  - sys
  - transition_manifest_crosscheck
  - transition_model
  - transition_model_checks
- path: check_test_baseline_writes.py
  purpose: 'CI gate: fail closed when a test source writes a committed measurement artifact (baselines,
    drc_ceiling.json, goldens, recorded metrics). Static half of the 2026-08-04 fix for test_update_baseline_yaml,
    a maintenance routine named test_* that pytest collected on ordinary runs and that rewrote temper_production_baseline.yaml
    via yaml.safe_dump -- stripping all ~235 comment lines including the ~200-line rationale header, and
    leaving the rewrite for git add -A to commit. Complements the runtime guard in scripts/_lib/pytest_artifact_guard.py:
    this half reads source, so it still fires when the offending test is deselected, unmarked or skipped
    for a missing kicad-cli'
  owner: bennet
  last_run: '2026-08-04'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.protected_artifacts
  - _lib.repo
  - argparse
  - ast
  - dataclasses
  - pathlib
  - sys
- path: check_traceability.py
  purpose: Verify @req() tags in code match plan requirements
  owner: pipeline
  last_run: '2026-08-12'
  category: keep
  disposition: utility
  imports:
  - __future__
  - argparse
  - pathlib
  - subprocess
  - sys
- path: check_transition_table_mutations.py
  purpose: 'CI gate (R40/P1, runs in the Firmware Tests workflow): fail on any live transition-table mutant,
    mutation error, or skipped row reported by firmware/test/mutate_transition_table.py --sweep, and fail
    closed on a stale report (generated_at older than --max-report-age-seconds) so a scheduled or committed
    report can never satisfy the gate -- the mutation suite proves every row of the test-side TRANSITIONS
    manifest (firmware/test/gen_transition_table.py) is exercised by tests that can fail'
  owner: bennet
  last_run: '2026-08-02'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - datetime
  - json
  - pathlib
  - sys
- path: check_typecheck_gate.py
  purpose: Type-check gate with per-file monotonic-shrink allowlist baseline
  owner: pipeline
  last_run: '2026-06-29'
  category: keep
  disposition: ci-gate
  imports:
  - argparse
  - collections
  - pathlib
  - re
  - subprocess
  - sys
  - tempfile
- path: check_undeclared_imports.py
  purpose: 'CI gate: fail when a module-level import in scripts/ or a Python test tree is not resolvable
    in the synced environment (undeclared third-party dependency) -- catches the jinja2/sympy class of
    incident (docs/evidence/2026-07-27-undeclared-import-gate.md)'
  owner: bennet
  last_run: '2026-07-27'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - ast
  - dataclasses
  - fnmatch
  - importlib.util
  - pathlib
  - sys
- path: check_unwired_kernels.py
  purpose: 'CI gate: fail when a Rust kernel is registered into a Python module but no non-test code calls
    it. Closes the gap that let 4,096 LOC of proven-but-inert kernels merge across PRs #749/#750/#751
    -- the differential compares oracle vs Rust and passes whether or not the shipped module delegates.
    Shrink-only ledger in .unwired-kernel-inventory; NEW_UNWIRED and STALE_ENTRY both fail.'
  owner: pipeline
  last_run: '2026-08-06'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - ast
  - pathlib
  - re
  - sys
- path: check_vacuous_gates.py
  purpose: 'CI gate: reject unguarded all() in gate and validator modules (docs/METHODOLOGY.md Sec 4/5,
    anti-vacuous-truth); any() exempt (already fail-closed); asserts zero, no allowlist. Also rejects
    tautological assert expressions (X or not X, X or True, X is X, literal X == X, bare assert True/1
    with nothing preceding it) anywhere in packages/scripts, including test_*.py -- added 2026-07-28 after
    test_clearance.py shipped one uncaught.'
  owner: bennet
  last_run: '2026-07-28'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - ast
  - pathlib
  - re
  - rich.console
  - sys
- path: check_verdict_coverage.py
  purpose: 'Wave 4 R7 coverage gate: every Python file under the roots in docs/wave4-verdicts.yaml must
    match exactly one ledger entry with a recorded verdict (MIGRATE / RETIRE / JUSTIFIED-KEEP / UNDECIDED).
    Enforces D6 structurally -- a JUSTIFIED-KEEP without a named blocker fails, because "consolidation"
    alone is never a sufficient justification. Reports UNDECIDED LOC as the migration backlog rather than
    hiding it.'
  owner: bennet
  last_run: '2026-08-04'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - collections
  - fnmatch
  - os
  - pathlib
  - sys
- path: check_wire_format_fidelity.py
  purpose: 'CI gate: fail when a Rust kernel reimplements a shipped geometry helper but its wire format
    drops a field that helper reads. Catches the class where the differential cannot -- a field absent
    from every corpus row is absent from the proof. Validated by replaying the three 2026-08 defects (escape_via
    roundrect_ratio, net_ordering and congestion_analysis initial_side); all three are caught.'
  owner: pipeline
  last_run: '2026-08-06'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - pathlib
  - re
  - sys
- path: check_workflow_pr_triggers.py
  purpose: 'CI gate: ensure workflow files with push triggers also declare pull_request (issue #315)'
  owner: pipeline
  last_run: '2026-07-23'
  category: keep
  disposition: ci-gate
  imports:
  - argparse
  - pathlib
  - re
  - sys
  - yaml
- path: ci_check_drc.py
  purpose: 'CI entry point: DRC ratchet check'
  owner: pipeline
  last_run: '2026-06-22'
  category: keep
  disposition: ci-gate
  imports:
  - argparse
  - pathlib
  - sys
- path: ci_check_erc.py
  purpose: 'CI entry point: ERC (kicad-cli sch erc) ratchet check -- errors hard-fail, warnings ratchet
    against power_pcb_dataset/erc_ceiling.json'
  owner: pipeline
  last_run: '2026-08-07'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - contextlib
  - dataclasses
  - json
  - pathlib
  - shutil
  - subprocess
  - sys
  - tempfile
  - typing
- path: ci_closure_test.py
  purpose: 'CI entry point: closure test (Benders -> Router -> DRC)'
  owner: pipeline
  last_run: '2026-06-22'
  category: keep
  disposition: ci-gate
  imports:
  - argparse
  - os
  - pathlib
  - sys
- path: ci_identity_check.py
  purpose: 'CI enforcement of the board identity gate (plan 2026-07-15-001, U7): rejects the quarantined
    fixture, verifies the production board once it exists'
  owner: bennet
  last_run: '2026-07-15'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - pathlib
  - sys
- path: classify_changed_paths.py
  purpose: 'Workflow-side per-job run decisions for python-tests.yml path-conditional jobs (change-driven
    CI): applies check_required_checks.job_should_run to the PR changed-file list against the manifest
    per-job trigger sets, so a job skipped in the workflow is exactly a skip the checker verifies.'
  owner: bennet
  last_run: '2026-08-02'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - check_required_checks
  - json
  - os
  - pathlib
  - subprocess
  - sys
- path: compare_drc_reports.py
  purpose: Compare DRC reports across runs (shell-invoked)
  owner: unassigned
  last_run: '2026-06-22'
  category: keep
  disposition: shell-invoked
  imports:
  - dataclasses
  - json
  - pathlib
  - sys
- path: constraint_mutation_gate.py
  purpose: 'CI gate: every constraint encoding carries a non-empty, triaged kill set in power_pcb_dataset/constraint_kill_sets.yaml
    (R32)'
  owner: bennet
  last_run: '2026-08-02'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - ast
  - importlib
  - pathlib
  - sys
- path: constraint_mutation_runner.py
  purpose: Apply R4 bug-class mutations (sign flip, dropped term, loosened bound, off-by-one, double-count)
    to CP-SAT encoders at source level and classify killed/survived against their existing defenses; regenerates
    the kill-set register
  owner: bennet
  last_run: '2026-08-02'
  category: keep
  disposition: utility
  imports:
  - __future__
  - argparse
  - ast
  - dataclasses
  - pathlib
  - sys
  - types
- path: deadcode-baseline.py
  purpose: 'Vulture whitelist/baseline data file: one accepted dead-code finding per line (name + comment),
    diffed against by vulture_gate.py. Needs a .py extension because vulture parses whitelist files as
    Python source; moved here from the repo root 2026-07-29 (scripts/test_root_hygiene.py flags any tracked
    *.py file at root).'
  owner: pipeline
  last_run: '2026-07-29'
  category: keep
  disposition: utility
  imports: []
- path: debug_diff_pair_path.py
  purpose: Debug differential pair path (shell-invoked)
  owner: unassigned
  last_run: '2026-06-22'
  category: keep
  disposition: shell-invoked
  imports:
  - pathlib
  - sys
  - temper_placer.io.kicad_parser
- path: extract_corpus_baselines.py
  purpose: Extract baseline metrics for a single corpus board (subprocess-called by bless_baselines.py)
  owner: pipeline
  last_run: '2026-06-22'
  category: keep
  disposition: utility
  imports:
  - sys
- path: full_pipeline_profile.py
  purpose: Full closure-test profile with per-net A* logging; surfaces the actual SM1 baseline on temper.kicad_pcb
  owner: router-v6-closure-rate
  last_run: '2026-06-23'
  category: keep
  disposition: utility
  imports:
  - __future__
  - argparse
  - cProfile
  - io
  - json
  - logging
  - os
  - pathlib
  - pstats
  - sys
  - time
  - typing
- path: gate_mutate.py
  purpose: 'R42 gate-mutation engine: applies one declarative mutation (guard-strip, condition-invert,
    scope-remove, violation-discard, comparison-flip, threshold-set, return-stub -- chosen to model observed
    failure mechanisms, not generic arithmetic mutants) to an in-memory copy of a gate script''s AST and
    writes the mutated source to a fresh scratch tempfile; never touches the committed gate. Every mutation
    is validated against a line_hint drift guard before being applied, and reported NOT APPLICABLE (not
    silently skipped) when the source has moved since the manifest entry was authored. Consumed by check_gate_mutations.py.'
  owner: bennet
  last_run: '2026-08-07'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.repo
  - argparse
  - ast
  - dataclasses
  - pathlib
  - sys
  - typing
- path: gen_architecture_poster.py
  purpose: Generate a deterministic SVG architecture poster from monorepo build manifests
  owner: pipeline
  last_run: '2026-07-13'
  category: keep
  disposition: utility
  imports:
  - __future__
  - argparse
  - json
  - math
  - os
  - pathlib
  - re
  - sys
  - tomllib
  - typing
- path: gen_domain_models.py
  purpose: Cross-language domain model codegen — NetClassRules SSOT from YAML manifest to Python+Rust
    (plan 2026-07-22-004)
  owner: bennet
  last_run: '2026-07-22'
  category: keep
  disposition: ci-gate
  imports:
  - argparse
  - jinja2
  - os
  - pathlib
  - sys
  - typing
  - yaml
- path: gen_oracle_freeze.py
  purpose: 'FREEZE oracle-retirement generator (U4, migration-pipeline wire-and-retire plan): run a _py_oracle.py
    differential oracle over a declared corpus, bake golden vectors into a Rust regression test with a
    non-vacuity guard, and (via --check) gate drift. --spec selects one of scripts/oracle_freeze_specs/*.py;
    --list enumerates them; --all runs every registered spec'
  owner: pipeline
  last_run: '2026-08-11'
  category: keep
  disposition: utility
  imports:
  - __future__
  - _lib.oracle_freeze
  - argparse
  - importlib
  - pathlib
  - pkgutil
  - sys
- path: gen_pcb_skeleton.py
  purpose: Generate a production KiCad PCB skeleton from the atopile netlist (CI-gate --check mode)
  owner: bennet
  last_run: '2026-07-23'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - collections
  - dataclasses
  - hashlib
  - json
  - pathlib
  - re
  - subprocess
  - sys
  - tempfile
  - typing
- path: gen_repo_state.py
  purpose: Generate README.md and docs/plans/README.md state blocks from source; --check fails closed
    on drift so hand-maintained state cannot go stale
  owner: pipeline
  last_run: '2026-07-26'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - pathlib
  - subprocess
  - sys
- path: gen_schematics.py
  purpose: Generate KiCad schematics from atopile netlist (default.net), with per-designator part identity
    from default.csv (default.net's libsource is footprint-aliased); --check is the schematic-drift CI
    gate
  owner: bennet
  last_run: '2026-07-15'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - collections
  - csv
  - dataclasses
  - hashlib
  - json
  - pathlib
  - re
  - subprocess
  - sys
  - tempfile
  - typing
- path: gen_wasm_test_registry.py
  purpose: 'Generate a crate''s src/wasm_test_registry.rs -- the flat (name, fn()) list that lets a wasm32
    entry point call each #[test] individually, since cargo test''s libtest harness cannot target wasm32-unknown-unknown.
    --crate selects the crate (temper-drc-rs, temper-geometry); --check fails closed on drift AND on a
    test module that could register but is not listed; --census explains every exclusion'
  owner: pipeline
  last_run: '2026-08-06'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - dataclasses
  - pathlib
  - re
  - sys
- path: generate_kicad_dru.py
  purpose: Generate KiCad design rules from config (used by temper-drc tests)
  owner: unassigned
  last_run: '2026-06-22'
  category: keep
  disposition: utility
  imports:
  - pathlib
  - temper_placer.core.design_rules
- path: human_reference_compare.py
  purpose: Generate human-reference PR comment comparing optimizer metrics to human placement
  owner: pipeline
  last_run: '2026-07-01'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - pathlib
  - sys
  - yaml
- path: import_linter_gate.py
  purpose: Import boundary enforcement CI gate wrapper
  owner: pipeline
  last_run: '2026-06-22'
  category: keep
  disposition: ci-gate
  imports:
  - _lib.github_summary
  - _lib.repo
  - argparse
  - datetime
  - pathlib
  - re
  - subprocess
  - sys
- path: install_git_stash_guard.py
  purpose: Install the shared-hooks reference-transaction hook (scripts/git-hooks/reference-transaction)
    that blocks git stash push/save/clear repo-wide, since the stash stack is shared across all concurrent
    worktrees on this .git directory. See AGENTS.md "Git Stash Guard".
  owner: bennet
  last_run: '2026-07-28'
  category: keep
  disposition: utility
  imports:
  - argparse
  - pathlib
  - shutil
  - subprocess
  - sys
- path: kicad_fill_zones.py
  purpose: Fill zones via KiCad's pcbnew API before DRC (kicad-cli lacks --refill-zones pre-CI's KiCad
    8.0.9)
  owner: zone-pour-u3-measurement
  last_run: '2026-07-21'
  category: keep
  disposition: utility
  imports:
  - sys
- path: kicad_pad_rotation_oracle.py
  purpose: 'Oracle for the rotation-convention differential tests (packages/temper-placer/tests/requirements/safety/test_rotation_convention_oracle.py):
    places a pad via real pcbnew (KiCad''s own footprint-placement engine) and reports where it actually
    lands, given a local offset and a footprint rotation. Must run under a Python interpreter with KiCad''s
    pcbnew bindings, not the project uv venv.'
  owner: bennet
  last_run: '2026-07-30'
  category: keep
  disposition: utility
  imports:
  - __future__
  - json
  - sys
- path: known_failure_pins.py
  purpose: 'CI gate + shared helper: validate known-failure-pins.yaml (a known-red test''s declared failure
    signature) as a monotonic-shrink ratchet -- live-pin cap, forced expiry/renewal, required evidence-doc
    link, orphan-nodeid detection -- and provide annotate_failure()/check_signature() for a test to compare
    its actual failure signature against the declared one, distinguishing "known reason" from "changed
    reason" without xfail (docs/solutions/best-practices/pin-known-failure-reasons-2026-07-30.md, the
    PR #460 near-miss).'
  owner: bennet
  last_run: '2026-07-30'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - dataclasses
  - datetime
  - pathlib
  - sys
  - typing
  - yaml
- path: literal_removal_check.py
  purpose: 'Advisory check: flags literals removed by a diff that are still referenced elsewhere in the
    repo'
  owner: unassigned
  last_run: '2026-07-20'
  category: keep
  disposition: utility
  imports:
  - argparse
  - pathlib
  - re
  - subprocess
  - sys
- path: measure_cross_domain_creepage.py
  purpose: 'Pairwise HV<->SELV creepage measurement tool (NOT a gate, not wired into CI): enumerates every
    cross-domain pad pair, measures exact pad-edge-to-pad-edge distance, classifies each violation body_free/body_crossing/unknown
    against F.Fab/F.CrtYd body outlines, and supports a --compare-to-mm differential so the PD2 (8.0mm)
    vs PD3 (12.6mm) pollution-degree decision has a re-runnable, parameterised cost measurement instead
    of throwaway per-session scripts (see docs/evidence/2026-07-29-cross-domain-creepage-pd2-vs-pd3.md)'
  owner: bennet
  last_run: '2026-08-01'
  category: keep
  disposition: utility
  imports:
  - __future__
  - _lib.path_setup
  - _lib.provenance
  - _lib.repo
  - argparse
  - dataclasses
  - math
  - pathlib
  - sys
  - temper_placer.core.isolation_constants
  - temper_placer.core.pad_geometry
- path: measure_router_output_baseline.py
  purpose: Measure the router-output DRC constants in the environment that enforces them. PRODUCTION_ROUTER_OUTPUT_*
    in test_regression_drc.py is a ratchet whose provenance records macOS arm64 measurements, while the
    gate runs inside the temper-ci container, and kicad-cli's geometric counts differ between the two
    (+107 on total, measured). Reports only -- asserts nothing and writes no constant; landing a re-baseline
    stays a human decision under AGENTS.md.
  owner: bennet
  last_run: '2026-08-04'
  category: keep
  disposition: measurement
  imports:
  - __future__
  - argparse
  - hashlib
  - json
  - os
  - platform
  - statistics
  - subprocess
  - sys
  - tempfile
  - time
- path: mpn_fabrication_gate.py
  purpose: 'Fabricated-MPN CI gate: fails closed on non-E-series resistor/capacitor values and MPN/declared-value
    disagreement in elec/src/*.ato'
  owner: pipeline
  last_run: '2026-07-28'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - collections
  - collections.abc
  - dataclasses
  - math
  - pathlib
  - re
  - sys
- path: netlist_mutator.py
  purpose: 'Deterministic seeded mutation harness for the compiled design netlist (plan 2026-08-02-021,
    R39/U5): applies one named mutation per class -- renumber (set-preserving ref permutation), drop-net
    (a net''s nodes removed), reuse (one ref assigned to two components) -- to a copy of elec/build/default.net
    and serialises a parseable mutated netlist for the mutation corpus.'
  owner: bennet
  last_run: '2026-08-02'
  category: keep
  disposition: utility
  imports:
  - __future__
  - argparse
  - check_domain_partition
  - dataclasses
  - pathlib
  - random
  - re
  - sys
- path: packages/temper-placer/scripts/gen_config_reference.py
  purpose: Generate docs/reference/config-reference.md from Pydantic model introspection (plan 2026-07-22-005)
  owner: bennet
  last_run: '2026-07-22'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - sys
  - pathlib
  - typing
  - pydantic
  - temper_placer.constraint_types.config
- path: part_stress_gate.py
  purpose: 'Part stress gate: applied stress vs. absolute-maximum rating, per part, data-driven against
    scripts/part_stress_limits.yaml (docs/hardware/PART_STRESS_AUDIT.md). Fails on any non-backlogged
    FAIL (margin < 0); the 3 real over-rating findings found at CI-wiring time (2026-08-07) are dated
    backlog entries (backlog: true + seeded pair), still reported every run, deferred to a hardware maintainer
    redesign call.'
  owner: bennet
  last_run: '2026-08-07'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.repo
  - argparse
  - csv
  - dataclasses
  - pathlib
  - re
  - sys
  - yaml
- path: phase5_batch1_mutations.py
  purpose: 'Wave-4 Phase-5 mutation campaign driver (deterministic grid_utils/via_placement): applies
    each Rust-kernel mutant, rebuilds temper-geometry, runs the four Batch-1 suites, expects failure,
    reverts. Reproducible anti-vacuity evidence.'
  owner: wave4-migration
  last_run: '2026-08-04'
  category: keep
  disposition: utility
  imports:
  - __future__
  - pathlib
  - subprocess
  - sys
- path: phase5_batch2_mutations.py
  purpose: 'Wave-4 Phase-5 mutation campaign driver (deterministic leaf stages: sequential_routing_dataclasses,
    layer_assignment, power_plane, component_assignment, fine_pitch_escape, validator slot-grid, DRC-check
    leaf kernels, connectivity_validation): applies each Rust-kernel mutant, rebuilds the owning crate,
    runs the differential/PBT suites, expects failure, reverts and verifies pristine. Reproducible anti-vacuity
    evidence. (routing_metrics dropped -- its Rust pyclasses were deleted as orphaned; see chore/delete-orphaned-routing-metrics.)'
  owner: wave4-migration
  last_run: '2026-08-06'
  category: keep
  disposition: utility
  imports:
  - __future__
  - pathlib
  - subprocess
  - sys
- path: phase5_cli_adapters_workflow_mutations.py
  purpose: 'Wave-4 Phase-5 anti-vacuity mutation campaign driver for the cli/adapters/temper-workflow
    slice (temper-orchestration crate): applies 11 one-line mutants to the Rust kernels, rebuilds, runs
    the six differential/PBT suites expecting failure, reverts, and rebuilds from the reverted source
    so the installed extension is always left correct. Two round-1 survivors were closed with discriminating
    cases (recorded in packages/temper-orchestration/VERIFICATION.md).'
  owner: wave4-migration
  last_run: '2026-08-05'
  category: keep
  disposition: utility
  imports:
  - __future__
  - pathlib
  - subprocess
  - sys
- path: phase5_hubs_mutations.py
  purpose: 'Wave 4 Phase 5 (deterministic hubs) anti-vacuity driver: applies one-line mutants to packages/temper-design-bundle/src/deterministic_hubs.rs,
    rebuilds the extension, runs the six hubs differential + PBT suites expecting failure, reverts. M11
    (index tie-break) is a recorded provable-equivalent survivor. Run manually by the migrating agent;
    the differentials it exercises run in CI.'
  owner: bennet
  last_run: '2026-08-05'
  category: keep
  disposition: utility
  imports:
  - __future__
  - pathlib
  - subprocess
  - sys
- path: phase5_final_leaves_mutations.py
  purpose: 'Wave-4 Phase-5 final-leaves anti-vacuity campaign driver (temper-design-bundle deterministic_phase
    kernels + temper-drc-rs via kernels): applies 13 one-line Rust-source mutants, rebuilds the owning
    crate, runs the owning differential suite expecting a kill, reverts, verifies pristine, and ends with
    a pristine rebuild of both crates + the full differential/PBT set green. Reproducible anti-vacuity
    evidence.'
  owner: wave4-migration
  last_run: '2026-08-06'
  category: keep
  disposition: utility
  imports:
  - __future__
  - os
  - pathlib
  - subprocess
  - sys
- path: physics_soundness_register_gate.py
  purpose: 'CI gate: enforce the physics soundness-proof register (R20, plan 2026-08-02-004) — every physics-gated
    constraint encoder must carry a Chebyshev-style soundness proof (or classified-error proof) linked
    from power_pcb_dataset/physics_soundness_register.yaml; AST scan of the physics modules detects new
    physics-gated surfaces'
  owner: bennet
  last_run: '2026-08-02'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - ast
  - collections.abc
  - dataclasses
  - pathlib
  - re
  - sys
  - typing
  - yaml
- path: pipeline_metrics.py
  purpose: Record pipeline metrics to JSON for trend analysis (CI-invoked)
  owner: pipeline
  last_run: '2026-06-22'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - datetime
  - json
  - math
  - pathlib
  - sys
- path: pipeline_report.py
  purpose: Generate self-contained static HTML pipeline report (R7, R8)
  owner: pipeline
  last_run: '2026-06-29'
  category: keep
  disposition: utility
  imports:
  - __future__
  - json
  - pathlib
  - sys
- path: pr_perf_compare.py
  purpose: Compare PR performance metrics against main-branch JSONL baseline
  owner: pipeline
  last_run: '2026-06-29'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - json
  - math
  - pathlib
  - statistics
  - sys
  - typing
- path: pr_scorecard.py
  purpose: 'PR pipeline scorecard: compare pipeline_metrics.jsonl files, produce markdown delta table'
  owner: pipeline
  last_run: '2026-06-29'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - json
  - logging
  - pathlib
  - sys
  - typing
- path: profile_router_v6_sampling.py
  purpose: Profile router_v6 on a small subset of nets for sampling analysis
  owner: router-v6-closure-rate
  last_run: '2026-06-23'
  category: keep
  disposition: utility
  imports:
  - __future__
  - argparse
  - cProfile
  - io
  - pathlib
  - pstats
  - sys
  - time
- path: profile_rust_topology.py
  purpose: Compare Rust vs Python SAT solver backend performance on closure test
  owner: unassigned
  last_run: '2026-06-28'
  category: keep
  disposition: utility
  imports:
  - __future__
  - json
  - os
  - pathlib
  - subprocess
  - sys
  - time
- path: pytest_guard.py
  purpose: Run pytest and fail if fewer than --min-tests actually executed; catches a suite silently collapsing
    to zero (missing path, mass skip, bad deselect)
  owner: pipeline
  last_run: '2026-07-27'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - pathlib
  - subprocess
  - sys
  - tempfile
  - xml.etree.ElementTree
- path: quarantine_report.py
  purpose: Report on dead-letter quarantine status and taxonomy distribution (CI gate)
  owner: pipeline
  last_run: '2026-06-30'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - json
  - pathlib
  - sys
- path: reconcile_metrics.py
  purpose: Reconcile pipeline metrics across multiple runs
  owner: pipeline
  last_run: '2026-06-29'
  category: keep
  disposition: utility
  imports:
  - json
  - pathlib
  - sys
- path: regen_derived.py
  purpose: Regenerate every derived artifact in one command (repo state, wasm test registry, oracle-hash
    registry) and REFUSE where regenerating would hide a defect -- a hash-order NEW_SITE or a drifted
    oracle pin; --check is the report-only form CI's gates would see
  owner: pipeline
  last_run: '2026-08-06'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - pathlib
  - subprocess
  - sys
- path: resync_pcb_netlist.py
  purpose: Resync pcb/temper.kicad_pcb nets/designators/footprints against elec/build/default.net without
    moving any existing footprint
  owner: bennet
  last_run: '2026-07-27'
  category: keep
  disposition: utility
  imports:
  - __future__
  - argparse
  - copy
  - dataclasses
  - gen_pcb_skeleton
  - json
  - kiutils.board
  - kiutils.footprint
  - kiutils.items.common
  - kiutils.items.zones
  - pathlib
  - sys
- path: route_board.py
  purpose: The routing entry point, and what `make route` invokes; supersedes internal_route.py, which
    was deleted 2026-08-04 as import-dead (see module docstring). --output is mandatory and refuses to
    match --pcb, so it can never overwrite the input board. --runs N routes N times from identical input
    and reports the completion spread -- the measurement instrument for the router's run-to-run non-determinism.
  owner: bennet
  last_run: '2026-07-28'
  category: keep
  disposition: utility
  imports:
  - __future__
  - argparse
  - json
  - os
  - pathlib
  - statistics
  - subprocess
  - sys
  - tempfile
  - time
  - typing
- path: run_feedback_loop.py
  purpose: Run iterative feedback loop (shell-invoked)
  owner: unassigned
  last_run: '2026-06-22'
  category: keep
  disposition: shell-invoked
  imports:
  - argparse
  - json
  - logging
  - pathlib
  - subprocess
  - sys
  - temper_placer.deterministic
  - temper_placer.deterministic.feedback
  - temper_placer.io.config_loader
  - temper_placer.io.kicad_metadata
  - temper_placer.io.kicad_parser
  - temper_placer.io.kicad_writer
  - temper_placer.io.zone_manager
- path: scan_external_corpus.py
  purpose: Scan downloads_digital_iot/ for .kicad_pcb files, profile parse success and difficulty tiers
  owner: pipeline
  last_run: '2026-06-30'
  category: keep
  disposition: utility
  imports:
  - __future__
  - argparse
  - pathlib
  - sys
  - time
  - yaml
- path: slo_evaluator.py
  purpose: SLO definition schema and evaluation engine
  owner: pipeline
  last_run: '2026-06-29'
  category: keep
  disposition: utility
  imports:
  - __future__
  - dataclasses
  - typing
  - yaml
- path: spc_rules.py
  purpose: Western Electric SPC rules — pure Python, no external stats dependency
  owner: pipeline
  last_run: '2026-06-29'
  category: keep
  disposition: utility
  imports:
  - __future__
  - math
- path: sweep_plan_status.py
  purpose: One-shot supersession sweep of docs/plans/ frontmatter status (2026-07-25); see docs/plans/README.md
  owner: bennet
  last_run: '2026-07-25'
  category: keep
  disposition: utility
  imports:
  - __future__
  - argparse
  - collections
  - pathlib
  - re
  - subprocess
- path: sync_kicad_netclass_assignments.py
  purpose: 'Sync pcb/temper.kicad_pro net_settings.netclass_assignments from TEMPER_NET_ASSIGNMENTS (design_rules.py,
    the Python-side SSOT) -- generator for the defect class PR #1023 hand-fixed four instances of and
    found ~15 more of, unmeasured; --check mode is the CI tripwire against future drift'
  owner: bennet
  last_run: '2026-08-11'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - json
  - pathlib
  - re
  - sys
  - temper_placer.core.design_rules
- path: test_root_hygiene.py
  purpose: 'CI gate: reject root-level artifact files that should live in subdirectories'
  owner: pipeline
  last_run: '2026-07-13'
  category: keep
  disposition: ci-gate
  imports:
  - pathlib
  - subprocess
- path: trace_invocations.py
  purpose: Build scripts/invocation_graph.json from CI/shell/Makefile/top-level .py/packages/ references
  owner: pipeline
  last_run: '2026-06-22'
  category: keep
  disposition: ci-gate
  imports:
  - ast
  - json
  - pathlib
  - re
  - yaml
- path: update_oracle_hashes.py
  purpose: 'Generator half of the oracle content-hash gate (decision 2026-08-06, issues #754 P2-2 / #758):
    recompute scripts/oracle_hashes.json from the current _*_py_oracle.py files under packages/. Deliberately
    NOT wired into CI -- check_oracle_hashes.py is the gate, and it compares the committed registry against
    the files. The generator is the human/agent step a migration PR runs in the SAME PR as an oracle add
    or re-pin, so a deliberate re-pin is a visible registry diff next to the oracle change (the keep-in-sync
    convention, same as the config-reference / .pyi regeneration steps). Output is deterministic (sorted
    paths, no timestamp) so regeneration is idempotent and the diff shows exactly which pins moved. Refuses
    to write a zero-entry registry.'
  owner: wave4-migration
  last_run: '2026-08-05'
  category: keep
  disposition: utility
  imports:
  - __future__
  - _lib.repo
  - argparse
  - hashlib
  - json
  - pathlib
  - sys
- path: update_production_routing_baseline.py
  purpose: Regenerate the router_v6_routing block of power_pcb_dataset/baselines/temper_production_baseline.yaml
    -- the only writer of that file. Runs the routing measurement test with TEMPER_ROUTING_RECORD_OUT
    pointed at a scratch path, then splices just that one block back in via _lib.yaml_block_patch so the
    file's ~235 comment lines survive byte-for-byte. Replaces test_update_baseline_yaml, which did the
    same job from inside pytest and destroyed the comments every run
  owner: bennet
  last_run: '2026-08-04'
  category: keep
  disposition: utility
  imports:
  - __future__
  - _lib.repo
  - _lib.yaml_block_patch
  - argparse
  - datetime
  - difflib
  - json
  - os
  - pathlib
  - subprocess
  - sys
  - tempfile
- path: update_regression_cache.py
  purpose: Update .regression-cache.json from regression reports for incremental corpus regression
  owner: pipeline
  last_run: '2026-06-29'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - json
  - pathlib
  - subprocess
  - sys
- path: validate_footprints.py
  purpose: Validate footprint assignments (shell-invoked)
  owner: unassigned
  last_run: '2026-06-22'
  category: keep
  disposition: shell-invoked
  imports:
  - argparse
  - dataclasses
  - pathlib
  - re
- path: verify_proofs.py
  purpose: Verify PROOFS.toml completeness — CI gate
  owner: pipeline
  last_run: '2026-06-29'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - pathlib
  - re
  - sys
- path: verify_regenerated_board.py
  purpose: 'R3 board-regeneration producer verification: assert a regenerated KiCad PCB parses, is structurally
    equivalent (order-independent canonical sets) to the committed board, and has DRC counts within the
    committed ceiling (regenerates DRU first, N=12, --all-track-errors)'
  owner: wasm-tier
  last_run: '2026-08-07'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - json
  - pathlib
  - subprocess
  - sys
  - tempfile
  - typing
- path: vulture_gate.py
  purpose: 'Vulture dead-code CI gate: diff wrapper with baseline allowlist'
  owner: pipeline
  last_run: '2026-06-22'
  category: keep
  disposition: ci-gate
  imports:
  - _lib.github_summary
  - _lib.repo
  - argparse
  - pathlib
  - re
  - subprocess
  - sys
- path: worktree_report.py
  purpose: 'Worktree hygiene report: inventory each git worktree with HEAD, age, distance behind main,
    and unique unmerged commits (read-only, prunes nothing)'
  owner: bennet
  last_run: '2026-07-26'
  category: keep
  disposition: utility
  imports:
  - __future__
  - _lib.repo
  - argparse
  - datetime
  - json
  - pathlib
  - rich.console
  - rich.table
  - shutil
  - subprocess
  - sys
- path: write_board_sync_stamp.py
  purpose: 'Write-side of the board-sync-stamp mechanism (2026-08-13 board/netlist desync postmortem,
    gap C): records a content digest of pcb/temper.kicad_pcb + elec/build/default.net beside the board,
    so check_board_sync_stamp.py can later prove neither side changed since. Refuses to write unless
    check_netlist_board_reconciliation.py currently passes (0 findings) -- a stamp on a desynced board
    would be worse than no stamp.'
  owner: bennet
  last_run: '2026-08-13'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.freshness
  - argparse
  - check_netlist_board_reconciliation
  - pathlib
  - sys
- path: write_build_stamp.py
  purpose: 'Build-side half of content-hash freshness: record the SHA-256 digest of a build''s inputs
    beside its artifact, so freshness gates can compare content instead of mtimes and a restored cache
    is no longer permanently STALE (see scripts/_lib/freshness.py)'
  owner: bennet
  last_run: '2026-07-28'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.freshness
  - argparse
  - pathlib
  - sys
- path: write_extension_stamps.py
  purpose: 'Build-side half of content-hash freshness for the pyo3/maturin Rust extension crates: record
    a SHA-256 digest of each crate''s source set beside its installed .so (keyed on that .so''s own bytes)
    so check_stale_extensions.py can compare content instead of mtimes -- a restored .venv cache or a
    prebuilt wheel baked into the CI image is no longer permanently STALE, which in that gate is unconditionally
    fatal. Stamps only crates the gate already considers fresh, so it can never launder a stale artifact
    (see scripts/_lib/freshness.py)'
  owner: bennet
  last_run: '2026-07-28'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.freshness
  - _lib.repo
  - argparse
  - check_stale_extensions
  - pathlib
  - sys
- path: check_ceiling_raise_evidence_corpus.py
  purpose: DRC-ceiling-raise-evidence fault-injection corpus (STRATEGY.md build order)
  owner: bennet
  last_run: '2026-08-08'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - copy
  - hashlib
  - pathlib
  - sys
  - typing
- path: check_component_defect_corpus.py
  purpose: Component-value/MPN-fabrication defect corpus runner
  owner: bennet
  last_run: '2026-08-08'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - dataclasses
  - pathlib
  - sys
  - tempfile
  - typing
- path: check_corpus_fixture_realboard_divergence.py
  purpose: 'CI gate: every power_pcb_dataset/corpus/manifest.yaml board that declares a real_board_path
    must also declare a role (real-board-snapshot, checked and BLOCKING on component-count drift; or independent-fixture,
    divergence reported for information only) -- an undeclared role fails closed. Motivated by power_pcb_dataset/corpus/temper/temper.kicad_pcb
    (33 components, frozen 2026-07-11) being mislabeled "the real golden-board corpus" while pcb/temper.kicad_pcb
    grew to 169 components, undetected for 27 days. See docs/evidence/2026-08-11-golden-fixture-regeneration-decision.md.'
  owner: bennet
  last_run: '2026-08-11'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - _lib.github_summary
  - _lib.repo
  - argparse
  - check_pcl_config_board_correspondence
  - dataclasses
  - pathlib
  - sys
  - typing
  - yaml
- path: check_corpus_specificity.py
  purpose: Corpus specificity run (STRATEGY.md build order step 5)
  owner: bennet
  last_run: '2026-08-08'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - dataclasses
  - pathlib
  - sys
  - typing
- path: component_defect_mutator.py
  purpose: Deterministic component-value/MPN-defect mutations on a synthetic fixture
  owner: bennet
  last_run: '2026-08-08'
  category: keep
  disposition: utility
  imports:
  - __future__
  - argparse
  - dataclasses
  - hashlib
  - pathlib
  - re
  - sys
  - typing
- path: rcm_blocking_diag.py
  purpose: Problem-3 spot-check diagnostic for the combined (Fix A + Fix B) router
  owner: bennet
  last_run: '2026-08-08'
  category: keep
  disposition: utility
  imports:
  - __future__
  - argparse
  - json
  - pathlib
  - route_board
  - sys
  - time
- path: rcm_empty_blocker_diag.py
  purpose: RCM empty-blocker diagnostic
  owner: bennet
  last_run: '2026-08-08'
  category: keep
  disposition: utility
  imports:
  - __future__
  - json
  - pathlib
  - sys
  - time
- path: rcm_pin_positions.py
  purpose: RCM pin-position analysis
  owner: bennet
  last_run: '2026-08-08'
  category: keep
  disposition: utility
  imports:
  - __future__
  - collections
  - json
  - pathlib
  - sys
  - temper_placer.io.kicad_parser
- path: rcm_spatial_analysis.py
  purpose: RCM spatial analysis
  owner: bennet
  last_run: '2026-08-08'
  category: keep
  disposition: utility
  imports:
  - argparse
  - collections
  - json
  - pathlib
- path: generate_ground_plane.py
  purpose: Ground-plane keepout-before-pour generation (router_v6._ground_plane)
  owner: bennet
  last_run: '2026-08-11'
  category: keep
  disposition: utility
  imports:
  - __future__
  - argparse
  - pathlib
  - sys
- path: spike_2026_08_11_inprocess_netbatch.py
  purpose: 'Spike: in-process net-batching measurement (2026-08-11)'
  owner: bennet
  last_run: '2026-08-11'
  category: keep
  disposition: spike
  imports:
  - __future__
  - json
  - pathlib
  - resource
  - sys
  - time
- path: generate_power_islands.py
  purpose: 'Generate the In2.Cu power-island pours (+3V3/vcc/+15V/V_BUS_SENSE) and
    write the result to a new file -- CLI wrapper around router_v6._power_islands.generate_power_islands_content
    (the In2.Cu counterpart to generate_ground_plane.py''s In1.Cu/gnd); never writes
    --pcb in place, does not itself run DRC/zone-fill verification on the output.
    Unregistered since #1047 (2026-08-12 hygiene paydown).'
  owner: bennet
  last_run: '2026-08-12'
  category: keep
  disposition: utility
  imports:
  - __future__
  - argparse
  - pathlib
- path: check_landed_board_shape.py
  purpose: 'Hard gate on a placed-and-routed board before it may be landed as pcb/temper.kicad_pcb
    -- anti-#1049 invariant (segments > 0 AND zones > 0 AND footprints == 168, absolute)
    plus a tolerance-bounded baseline-agreement check against the verified recipe
    baseline, loaded from scripts/board_shape_baseline.json (2,514/22/76/168,
    pinned-engine + corrected-write-path re-measurement -- see that file''s _march
    log; the previously hardcoded 3,349/56/70 baseline, and four other values before
    it, were all void). Not currently invoked by any workflow step -- a manual/CLI
    check run as part of the board-landing review, not (yet) CI-wired; board-landing
    track, not this PR''s cluster (2026-08-12 hygiene paydown: manifest entry only).'
  owner: board-landing
  last_run: '2026-08-12'
  category: keep
  disposition: utility
  imports:
  - __future__
  - argparse
  - json
  - pathlib
  - re
  - sys
- path: check_wasm_covered.py
  purpose: 'Resolve `wasm-covered:` workflow annotations against the wasm registry at the current commit
    (the `wasm_test_registry.rs` corpus, computed with gen_wasm_test_registry.py''s own collection -- NOT the
    deployed Workers'' census). Dual role: CLI resolver (exit 0 = every claimed mirror is registered and every
    Python test it would reduce still exists; exit 1 = stale annotation, fail-closed) and a pytest plugin
    (`-p check_wasm_covered`) that drops an annotated cluster''s hypothesis tests to a token max_examples when
    the workflow sets WASM_COVERED_<CLUSTER> after a successful resolution. First cluster: timing T1-T4
    (p7-p10 x 20 seeds + compare_stage_guards_zero_baseline mirror Python test_t1..test_t4). See
    docs/solutions/workflow-issues/wasm-covered-annotation-resolver-2026-08-12.md.'
  owner: pipeline
  last_run: '2026-08-12'
  category: keep
  disposition: ci-gate
  imports:
  - __future__
  - argparse
  - dataclasses
  - os
  - pathlib
  - re
  - sys
- path: verify_pumpkin_engine.py
  purpose: 'Pumpkin-engine binary identity gate: the resolved pumpkin_engine CP solver
    (target-shared/release/pumpkin_engine, gitignored and untracked) must match a pinned
    sha256, or the pipeline must refuse to proceed -- catches six regenerations of the
    "same" recipe producing boards differing by hundreds to thousands of routed segments
    because an unpinned binary silently picked up an unmerged branch''s build (docs/evidence/2026-08-12-engine-binary-pinning.md,
    2026-08-12-candidate-board-not-landed-engine-provenance.md). Not currently invoked
    by any workflow step; board-landing track, not this PR''s cluster (2026-08-12 hygiene
    paydown: manifest entry only, added after a rebase picked up #1060).'
  owner: board-landing
  last_run: '2026-08-12'
  category: keep
  disposition: utility
  imports:
  - __future__
  - _lib.repo
  - argparse
  - dataclasses
  - hashlib
  - json
  - os
  - pathlib
  - subprocess
  - sys
- path: measure_uncapped_drc.py
  purpose: "Uncapped DRC violation counting for kicad-cli-saturated categories"
  owner: bennet
  last_run: "2026-08-13"
  category: keep
  disposition: utility
  imports: []
