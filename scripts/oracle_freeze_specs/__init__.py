"""FREEZE specs (U4). One module per retired oracle; each exports ``SPEC``.

See ``scripts/_lib/oracle_freeze.py`` for the shared machinery and
``scripts/gen_oracle_freeze.py`` for the CLI driver.
"""
